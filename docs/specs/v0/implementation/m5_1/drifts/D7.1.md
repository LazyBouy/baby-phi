<!-- Last verified: 2026-05-09 by Claude Code (CH-17 P-seal — Status flipped `discovered` → `remediated`; SSE tail endpoint shipped at `GET /api/v0/sessions/:id/events`; tokio::broadcast tap on BabyPhiSessionRecorder per ADR-0055 §D55.1; CLI default-tail flips from deferred-message print to real stream consumption (cli/src/commands/session.rs `--no-tail` flag added; the deferred-to-M7 message is retired); cycle hex `40c4d759`.) -->

# D7.1 — Live SSE tail deferred to M7 (ADR-0031 D4 path-(a) not taken at M5)

## Identification
- **ID**: D7.1
- **Phase of origin**: M5/P7
- **Discovery source**: `plan-archive-ledger`
- **Date discovered**: 2026-04-24 (M5/P7 close)
- **Status**: `remediated`
- **Bucket**: A — load-bearing scope gap
- **Severity**: HIGH
- **Tags**: `sse`, `cli-ux`, `deferred-to-M7`, `depends-on-D4.2`, `concept-contradiction`, `closed-at-CH-17`
- **Blocks**: CLI live-tail UX; R-ADMIN-14-N* "tail events live" requirement
- **Blocked-by**: D4.2 (no real agent_loop → nothing to stream)

## Concept alignment
- **Concept doc(s)**: [`concepts/permissions/05-memory-sessions.md`](../../../concepts/permissions/05-memory-sessions.md) §"session lifecycle — live events" (operator observes live transcript)
- **Concept claim (verbatim or close paraphrase)**: Operators observe session execution via a live event stream. The stream is the canonical live-inspection surface.
- **Contradiction**: **Contradicts concept**. No `/events` SSE endpoint exists; CLI's `phi session launch` prints "(live tail deferred to M7)" per [`session.rs:228`](../../../../../../modules/crates/cli/src/commands/session.rs#L228).
- **Classification**: `contradicts-concept` (transport shape is concept-implied; enforcement deferred)
- **phi-core leverage status**: `direct-reuse` (predicted) — when SSE lands, `phi_core::types::event::AgentEvent` will be imported for payload deserialisation (≤1 new import line).

## Plan vs. reality
- **Plan said** (plan-archive line 1388, §Part 4 P7 §Deliverables item 1 + D4 decision): *"`phi session launch` — default tails events live via SSE to `GET /api/v0/sessions/:id/events`; `--detach` returns `session_id` + first `loop_id`."*
- **Reality (shipped state at current HEAD)**: No SSE endpoint. `launch.rs` ships synthetic replay feeder (D4.2). CLI prints "live tail deferred to M7 — `phi session show --id <id>` inspects terminal state" at [`session.rs:228`](../../../../../../modules/crates/cli/src/commands/session.rs#L228). `--detach` preserved on clap surface for wire stability; every launch is effectively detached.
- **Root cause**: `cascading-upstream-deferral` (depends on D4.2)

## Where visible in code
- **File(s)**: [`modules/crates/cli/src/commands/session.rs`](../../../../../../modules/crates/cli/src/commands/session.rs) lines 51 (module doc acknowledges synthetic), 228 (deferral message in on-success print)
- **Test evidence**: Completion-regression test pins 5 subcommands but doesn't exercise streaming behaviour (no stream to test).
- **Grep for regression**: `grep -n "live tail deferred to M7" modules/crates/cli/src/commands/session.rs` — expect 1 hit while drift open; 0 post-remediation.

## Remediation scope (estimate only)
- **Approach (sketch)**: After D4.2 closes: add `tokio::broadcast` to `BabyPhiSessionRecorder`; new axum SSE handler `GET /api/v0/sessions/:id/events` streams `RecorderEvent` payloads; flip CLI `launch_impl` to consume the stream by default; import `phi_core::types::event::AgentEvent` for payload deserialisation. Covered in prior plan-v1 as M5.1/P4 (~1 day after D4.2).
- **Implementation chunk this belongs to**: CH-17
- **Dependencies on other drifts**: D4.2 is the hard prerequisite.
- **Estimated effort**: 1 engineer-day (after D4.2).
- **Risk to concept alignment if deferred further**: MEDIUM — concept's "live transcript observation" surface absent; operator UX degraded; compounds as M5.2 listener bodies start persisting real data operators want to observe.

## Prior documentation locations (pre-M5.1)
- Plan archive lines: 1386–1392
- Code comments: [`session.rs`](../../../../../../modules/crates/cli/src/commands/session.rs) module doc §phi-core leverage note + on-success print
- ADR references: ADR-0031 D4 (originally chose path-(a) SSE; not taken at M5)

## Lifecycle history
- 2026-04-24 — `discovered` — logged in M5/P7 close drift addendum
- 2026-04-24 — `classified` — migrated to M5.1 drift catalogue; Bucket A HIGH; cascade from D4.2 pinned
- 2026-05-09 — `remediated` — CH-17 chunk-seal — SSE tail endpoint shipped at `GET /api/v0/sessions/:id/events`; `tokio::broadcast::Sender<phi_core::AgentEvent>` tap on `BabyPhiSessionRecorder.broadcast_tx` per [ADR-0055](../../m5_2/decisions/0055-sse-broadcast-fanout-and-keepalive.md) §D55.1; sibling permission-builder `build_session_observe_manifest` ships at `domain/src/permissions/builders/session_observe.rs` per ADR-0055 §D55.5 (gates SSE on `Action::Observe` over `session_object`); migration `0016_template_a_session_object_grant_add_observe.surql` walks legacy Template A grants per ADR-0055 §D55.9; CLI default-tail flips from deferred-message print to real stream consumption (the `(live tail deferred to M7 — …)` print at `cli/src/commands/session.rs` is retired; new `--no-tail` flag preserves prior detach-equivalent behaviour). Forbidden-grep `grep -n "live tail deferred to M7" modules/crates/cli/src/commands/session.rs` → 0 hits at chunk seal. CHK8S-D-10 ledger entry filed for the M7b Redis-pub/sub-backed cross-pod fan-out swap. Cycle hex `40c4d759`.
