<!-- Last verified: 2026-04-24 by Claude Code -->

# D6.3 — "Standard" vs "org-specific" bucketing needs a union filter because fixture system agents pre-date canonical slugs

## Identification
- **ID**: D6.3
- **Phase of origin**: M5/P6
- **Discovery source**: `plan-archive-ledger`
- **Date discovered**: 2026-04-23 (M5/P6 close)
- **Status**: `discovered`
- **Bucket**: B — underspecified shape choice
- **Severity**: MEDIUM
- **Tags**: `bucketing-filter`, `fixture-drift`
- **Blocks**: none (fixture-side drift; robust filter covers it)
- **Blocked-by**: none

## Concept alignment
- **Concept doc(s)**: [`concepts/system-agents.md`](../../../concepts/system-agents.md) §"standard system agents vs org-specific" (canonical memory-extraction + agent-catalog are platform-standard; others are org-specific)
- **Concept claim (verbatim or close paraphrase)**: Two canonical system agents ship per-org (memory-extraction + agent-catalog). Additional system agents are org-specific. The bucketing distinguishes these categories.
- **Contradiction**: None at shipped state — union filter correctly buckets fixture + production rows. Plan-implied "strict slug match" would misclassify fixture rows as org-specific.
- **Classification**: `concept-silent-plan-filled-gap` (concept mandates bucketing; plan picked a slug-match filter that M3-era fixtures don't honor; shipped union filter is robust)
- **phi-core leverage status**: `N/A — no phi-core overlap`

## Plan vs. reality
- **Plan said** (plan-archive line 1355, §Part 4 P6 §Deliverables): bucketing by profile_ref slug. Implied: M3 org-creation provisions standard system agents with canonical M5 slugs (`"system-memory-extraction"` and `"system-agent-catalog"`).
- **Reality (shipped state at current HEAD)**: Fixture (`acceptance_common::admin::spawn_claimed_with_org`) provisions system agents with M3-era slugs that don't match canonical M5 constants. Strict slug filter would leave them in `org_specific` — wrong. Shipped filter at [`system_agents/list.rs:60-65`](../../../../../../modules/crates/server/src/platform/system_agents/list.rs#L60-L65) uses 3-way union: canonical slug OR membership in `Organization.system_agents` registry OR `AgentRole::System` on the agent row.
- **Root cause**: `reality-feedback-contradicted-estimate` (fixture pre-dated M5 canonical slugs; plan didn't audit fixture state).

## Where visible in code
- **File(s)**: [`modules/crates/server/src/platform/system_agents/list.rs`](../../../../../../modules/crates/server/src/platform/system_agents/list.rs) lines 55–70 (union-filter comment + 3-way predicate)
- **Test evidence**: `acceptance_system_agents::list_standard_agents_includes_fixture_seeded_rows` (within the 8 P6 scenarios) — exercises the union.
- **Grep for regression**: `grep -n "system_agent_ids.contains\|AgentRole::System" modules/crates/server/src/platform/system_agents/list.rs` — expect ≥2 hits.

## Remediation scope (estimate only)
- **Approach (sketch)**: Confirm-in-place. M7b hardening could reserve canonical slugs at the repo tier if operators start reusing them; low priority.
- **Implementation chunk this belongs to**: CH-19
- **Dependencies on other drifts**: none
- **Estimated effort**: 1 day at M7b if slug-reservation is pursued; 0 at M5.1 (ratification only).
- **Risk to concept alignment if deferred further**: low.

## Prior documentation locations (pre-M5.1)
- Plan archive lines: 1353–1359
- Code comments: [`list.rs`](../../../../../../modules/crates/server/src/platform/system_agents/list.rs) inline comment on the union filter
- ADR references: none

## Lifecycle history
- 2026-04-23 — `discovered` — logged in M5/P6 close drift addendum
- 2026-04-24 — `classified` — migrated to M5.1 drift catalogue
