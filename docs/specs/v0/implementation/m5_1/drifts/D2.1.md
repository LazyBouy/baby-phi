<!-- Last verified: 2026-04-24 by Claude Code -->

# D2.1 — `persist_session` / `append_loop_record` / `append_turn` use CREATE (not UPDATE-as-upsert)

## Identification
- **ID**: D2.1
- **Phase of origin**: M5/P2
- **Discovery source**: `plan-archive-ledger`
- **Date discovered**: 2026-04-23 (M5/P2 close)
- **Status**: `discovered`
- **Bucket**: C — convention/pattern decision
- **Severity**: LOW
- **Tags**: `persistence`, `surrealdb-idiom`
- **Blocks**: D4.4 (profile create-vs-upsert uses same idiom)
- **Blocked-by**: none

## Concept alignment
- **Concept doc(s)**: [`concepts/ontology.md`](../../../concepts/ontology.md) (persistence layer is below concept granularity)
- **Concept claim (verbatim or close paraphrase)**: Concepts specify node creation semantics (fresh id, duplicate-rejection on UNIQUE violations) but not the SurrealDB DDL verb. Concepts treat persistence as an opaque substrate.
- **Contradiction**: None. Shipped CREATE semantics honor the "duplicate session id rejected" contract inherited from ontology.
- **Classification**: `concept-silent-plan-filled-gap`
- **phi-core leverage status**: `N/A — no phi-core overlap` (repo layer writes baby-phi wraps; no phi-core surface duplication)

## Plan vs. reality
- **Plan said** (plan-archive line 1159, §P2 Deliverables + §C6 + §G5): 9 session-tier repo methods described as "persist / append" without specifying CRUD verb. Implicit convention from M3/M4 `upsert_*` helpers (`UPDATE type::thing('<table>', $id) CONTENT $body` as create-or-update idiom).
- **Reality (shipped state at current HEAD)**: `persist_session` uses `CREATE type::thing('session', $id) CONTENT $body RETURN NONE` ([`repo_impl.rs:2238`](../../../../../../modules/crates/store/src/repo_impl.rs#L2238)) — mapping SurrealDB's unique-violation error to `RepositoryError::Conflict`. `append_loop_record` + `append_turn` also use `CREATE` (fresh ids so CREATE never conflicts). Discovery driver: UPDATE-as-upsert silently writes nothing on non-existent records with SCHEMAFULL + FLEXIBLE fields.
- **Root cause**: `reality-feedback-contradicted-estimate` (SurrealDB DDL semantics contradicted the plan's implicit UPDATE-as-upsert assumption).

## Where visible in code
- **File(s)**: [`modules/crates/store/src/repo_impl.rs`](../../../../../../modules/crates/store/src/repo_impl.rs) lines 2222–2250 (`persist_session`) + `append_loop_record` + `append_turn` bodies
- **Test evidence**: `store/tests/session_repo_parity.rs` — all 18 parity tests pass with CREATE semantics; in-memory + SurrealDB parity proven.
- **Grep for regression**: `grep -n "UPDATE type::thing('session'" modules/crates/store/src/repo_impl.rs` — expect 0 hits (session writes must CREATE, not UPDATE).

## Remediation scope (estimate only)
- **Approach (sketch)**: Confirm-in-place convention. Document the CREATE-vs-UPDATE-vs-DELETE-then-UPSERT idiom matrix in a persistence-conventions doc for M6+ reviewers.
- **Implementation chunk this belongs to**: CH-20
- **Dependencies on other drifts**: D4.4 (profile create-vs-upsert uses same SurrealDB idiom)
- **Estimated effort**: 0 engineer-days (shipped correctly); ~0.25 day for the conventions doc at M6+ if needed.
- **Risk to concept alignment if deferred further**: none (concept-silent).

## Prior documentation locations (pre-M5.1)
- Plan archive lines: 1157–1170
- Code comments: [`repo_impl.rs`](../../../../../../modules/crates/store/src/repo_impl.rs) inline at `persist_session` + `upsert_agent_catalog_entry`
- ADR references: none

## Lifecycle history
- 2026-04-23 — `discovered` — logged in M5/P2 close drift addendum
- 2026-04-24 — `classified` — migrated to M5.1 drift catalogue
