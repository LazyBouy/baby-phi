<!-- Last verified: 2026-04-24 by Claude Code -->

# D7.5 — Web test count unchanged (no new page-component tests at P7)

## Identification
- **ID**: D7.5
- **Phase of origin**: M5/P7
- **Discovery source**: `plan-archive-ledger`
- **Date discovered**: 2026-04-24 (M5/P7 close)
- **Status**: `discovered`
- **Bucket**: B — underspecified shape choice
- **Severity**: MEDIUM
- **Tags**: `test-strategy`, `web-coverage`, `deferred-to-playwright`
- **Blocks**: none (Playwright at M5.2/P9 is the right layer)
- **Blocked-by**: none

## Concept alignment
- **Concept doc(s)**: concept docs are silent on test strategy (below concept granularity)
- **Concept claim (verbatim or close paraphrase)**: No concept claim targets test coverage directly.
- **Contradiction**: None at concept layer; plan-estimated test counts simply weren't met.
- **Classification**: `concept-silent-plan-filled-gap`
- **phi-core leverage status**: `N/A — no phi-core overlap`

## Plan vs. reality
- **Plan said** (plan-archive line 1420, §Part 5 Testing strategy): *"~20 Web tests added at M5."* — implied P5 + P7 component tests for each new page.
- **Reality (shipped state at current HEAD)**: 3 new pages (templates, system-agents, sessions/new) are SSR Server Components with inline Server Actions. Component testing requires mocking Next.js's server-action machinery + `cookies()` + `redirect()` flow — high cost for shallow JSX assertion. `npm run build` + `npm run typecheck` + `npm run lint` green. Web test count at P7 close: **79** (unchanged from P6; the +11 over M4's 68 landed at P1 from primitives component tests).
- **Root cause**: `reality-feedback-contradicted-estimate` (plan test-count estimate didn't weigh SSR Server Component testing difficulty).

## Where visible in code
- **File(s)**: `modules/web/__tests__/` directory — test files exist for M5/P1 primitives but no new page-component test files added at P5/P6/P7.
- **Test evidence**: `npm run test` reports 79 passing. No coverage of the 3 new pages at component level.
- **Grep for regression**: `ls modules/web/__tests__/*.test.tsx | wc -l` — expect same count post-M5.1.

## Remediation scope (estimate only)
- **Approach (sketch)**: Ratify as test-strategy decision via ADR. Coverage of the 3 new pages lands at M5.2/P9 seal via Playwright e2e (CLI → launch → web-render full path).
- **Implementation chunk this belongs to**: CH-24 (Playwright e2e scope)
- **Dependencies on other drifts**: none
- **Estimated effort**: 0 days at M5.1; ~1 day of Playwright work inside M5.2/P9.
- **Risk to concept alignment if deferred further**: low.

## Prior documentation locations (pre-M5.1)
- Plan archive lines: 1418–1423
- Code comments: none
- ADR references: none (candidate for test-strategy ADR at M5.2)

## Lifecycle history
- 2026-04-24 — `discovered` — logged in M5/P7 close drift addendum
- 2026-04-24 — `classified` — migrated to M5.1 drift catalogue; Bucket B MEDIUM (Playwright coverage at M5.2/P9)
