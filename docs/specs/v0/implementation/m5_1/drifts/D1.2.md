<!-- Last verified: 2026-04-24 by Claude Code -->

# D1.2 — `runs_session` RELATION retyped (REMOVE + DEFINE), not fresh DEFINE TABLE

## Identification
- **ID**: D1.2
- **Phase of origin**: M5/P1
- **Discovery source**: `plan-archive-ledger`
- **Date discovered**: 2026-04-23 (M5/P1 close)
- **Status**: `discovered`
- **Bucket**: C — convention/pattern decision
- **Severity**: LOW
- **Tags**: `persistence`, `schema-retype`
- **Blocks**: none
- **Blocked-by**: none

## Concept alignment
- **Concept doc(s)**: [`concepts/ontology.md`](../../../concepts/ontology.md) §"edges have a direction" + [`concepts/project.md`](../../../concepts/project.md) §"a session runs in a project" — the session→project directionality is conceptual.
- **Concept claim (verbatim or close paraphrase)**: A session runs within a project (session → project directional edge). The M1-era reverse direction (`agent → session`) was a scaffold never matched to concept.
- **Contradiction**: None at shipped state — D1.2's retype restores concept alignment. The transient contradiction was in 0001's scaffold direction, which predates M5 and had no writers.
- **Classification**: `concept-silent-plan-filled-gap` (plan was silent on retype mechanics; shipped code honors concept)
- **phi-core leverage status**: `N/A — no phi-core overlap` (runs_session is baby-phi governance-only)

## Plan vs. reality
- **Plan said** (plan-archive lines 1123, P1 Deliverables item 1 + §G4): *"DEFINE TABLE `runs_session` TYPE RELATION FROM `session` TO `project`."* — treated as fresh table creation.
- **Reality (shipped state at current HEAD)**: Migration 0001 line 327 shipped `DEFINE TABLE runs_session TYPE RELATION FROM agent TO session;` — semantically backwards. The 0001 scaffold had zero production writers. Migration 0005 uses `REMOVE TABLE runs_session; DEFINE TABLE runs_session TYPE RELATION FROM session TO project;` — forward-only retype matching the `uses_model` retype pattern in the same migration.
- **Root cause**: `concept-doc-not-consulted` (plan didn't audit 0001's prior `runs_session` direction).

## Where visible in code
- **File(s)**: [`modules/crates/store/migrations/0001_initial.surql`](../../../../../../modules/crates/store/migrations/0001_initial.surql) line 327 (original reverse direction); [`modules/crates/store/migrations/0005_sessions_templates_system_agents.surql`](../../../../../../modules/crates/store/migrations/0005_sessions_templates_system_agents.surql) §4 (retype)
- **Test evidence**: Safe-retype proven by `grep -rn 'runs_session' modules/crates/ --include='*.rs'` returning only edge-enum variant + migration files — zero RELATE writer statements under the old direction.
- **Grep for regression**: `grep -rn "RELATE .*agent.*runs_session.*session" modules/crates/` — expect 0 hits (no writer under the reverse direction).

## Remediation scope (estimate only)
- **Approach (sketch)**: Confirm-in-place. The retype is clean; the M4 `edges.rs::RunsSession` enum variant now matches the production direction.
- **Implementation chunk this belongs to**: CH-20
- **Dependencies on other drifts**: none
- **Estimated effort**: 0 engineer-days (already shipped correctly).
- **Risk to concept alignment if deferred further**: none.

## Prior documentation locations (pre-M5.1)
- Plan archive lines: 1121–1129
- Code comments: [`0005_sessions_templates_system_agents.surql`](../../../../../../modules/crates/store/migrations/0005_sessions_templates_system_agents.surql) §4 header
- ADR references: none

## Lifecycle history
- 2026-04-23 — `discovered` — logged in M5/P1 close drift addendum
- 2026-04-24 — `classified` — migrated to M5.1 drift catalogue
