<!-- Last verified: 2026-04-24 by Claude Code -->

# D7.3 — `phi session preview` ships as a 5th subcommand (unplanned scope addition)

## Identification
- **ID**: D7.3
- **Phase of origin**: M5/P7
- **Discovery source**: `plan-archive-ledger`
- **Date discovered**: 2026-04-24 (M5/P7 close)
- **Status**: `discovered`
- **Bucket**: C — convention/pattern decision
- **Severity**: LOW
- **Tags**: `cli-ux`, `scope-addition`
- **Blocks**: none
- **Blocked-by**: none

## Concept alignment
- **Concept doc(s)**: [`concepts/permissions/04-manifest-and-resolution.md`](../../../concepts/permissions/04-manifest-and-resolution.md) §"preview mode" (Decision trace as operator inspection surface)
- **Concept claim (verbatim or close paraphrase)**: Operators may inspect permission Decision before committing an action.
- **Contradiction**: None — adding `phi session preview` aligns with concept's "inspect before acting" intent (arguably strengthens alignment).
- **Classification**: `concept-silent-plan-filled-gap` (cheap operator win)
- **phi-core leverage status**: `N/A — no phi-core overlap`

## Plan vs. reality
- **Plan said** (plan-archive line 1404, §Part 4 P7 §Deliverables): 4 session subcommands (`launch`, `show`, `terminate`, `list`).
- **Reality (shipped state at current HEAD)**: 5th subcommand `preview` added at [`session.rs:101`](../../../../../../modules/crates/cli/src/commands/session.rs#L101) — wraps existing `POST /sessions/preview` route (built at P4 for Web but no CLI surface). `preview_impl` at [`session.rs:453`](../../../../../../modules/crates/cli/src/commands/session.rs#L453); completion regression pins it.
- **Root cause**: `implementer-unilateral-choice` (cheap operator win using existing HTTP route).

## Where visible in code
- **File(s)**: [`modules/crates/cli/src/commands/session.rs`](../../../../../../modules/crates/cli/src/commands/session.rs) lines 99–110 (clap variant), 143–148 (dispatcher), 453 (`preview_impl`)
- **Test evidence**: `completion_help::completion_session_subcommand_includes_preview` — regression-pinned.
- **Grep for regression**: `grep -n "SessionCommand::Preview\|preview_impl" modules/crates/cli/src/commands/session.rs` — expect ≥3 hits.

## Remediation scope (estimate only)
- **Approach (sketch)**: Confirm-in-place as ratified addition. Update CLI reference doc to list 5 subcommands.
- **Implementation chunk this belongs to**: CH-20
- **Dependencies on other drifts**: none
- **Estimated effort**: 0 days (shipped).
- **Risk to concept alignment if deferred further**: none.

## Prior documentation locations (pre-M5.1)
- Plan archive lines: 1402–1408
- Code comments: [`session.rs`](../../../../../../modules/crates/cli/src/commands/session.rs) `Preview` clap variant doc
- ADR references: none

## Lifecycle history
- 2026-04-24 — `discovered` — logged in M5/P7 close drift addendum
- 2026-04-24 — `classified` — migrated to M5.1 drift catalogue
