<!-- Last verified: 2026-04-24 by Claude Code -->

# D6.2 — CLI + Web for page 13 deferred to P7 (matching D5.1 precedent)

## Identification
- **ID**: D6.2
- **Phase of origin**: M5/P6
- **Discovery source**: `plan-archive-ledger`
- **Date discovered**: 2026-04-23 (M5/P6 close)
- **Status**: `remediated`
- **Bucket**: A — load-bearing scope gap (at discovery; now closed)
- **Severity**: MEDIUM (downgraded post-remediation)
- **Tags**: `scope-deferral`, `closed-at-p7`
- **Blocks**: none (precedent came from D5.1; D5.1 + D6.2 closed at P7 together)
- **Blocked-by**: D5.1 (precedent)

## Concept alignment
- **Concept doc(s)**: [`concepts/system-agents.md`](../../../concepts/system-agents.md) §"operator surfaces — tune, add, disable, archive" + admin page 13 requirements R-ADMIN-13-*
- **Concept claim (verbatim or close paraphrase)**: Operators tune / add / disable / archive system agents through first-class surfaces. Concept silent on per-surface sequencing.
- **Contradiction**: None. All three surfaces now ship.
- **Classification**: `concept-silent-plan-filled-gap`
- **phi-core leverage status**: `N/A — no phi-core overlap`

## Plan vs. reality
- **Plan said** (plan-archive line 1347, §Part 4 P6 §Goals + Deliverables items 4 & 5): *"Operators tune / add / disable / archive system agents via page 13 (HTTP + CLI + Web)."*
- **Reality (shipped state at current HEAD)**: P6 shipped HTTP only; CLI + Web landed at P7. Current HEAD: [`cli/src/commands/system_agent.rs`](../../../../../../modules/crates/cli/src/commands/system_agent.rs) + `modules/web/app/(admin)/organizations/[id]/system-agents/`/organizations/[id]/system-agents/). **Closed at HEAD.**
- **Root cause**: `implementer-unilateral-choice` (D5.1 precedent extended)

## Where visible in code
- **File(s)**: HTTP — [`modules/crates/server/src/platform/system_agents/`](../../../../../../modules/crates/server/src/platform/system_agents/) (7 files + 5 routes + 8 acceptance scenarios); CLI — [`modules/crates/cli/src/commands/system_agent.rs`](../../../../../../modules/crates/cli/src/commands/system_agent.rs); Web — `modules/web/app/(admin)/organizations/[id]/system-agents/`/organizations/[id]/system-agents/) (page.tsx + actions.ts)
- **Test evidence**: `acceptance_system_agents` + P7 completion-regression test covers `phi system-agent` tree.
- **Grep for regression**: `ls modules/crates/cli/src/commands/system_agent.rs modules/web/app/\(admin\)/organizations/\[id\]/system-agents/page.tsx` — expect both.

## Remediation scope (estimate only)
- **Approach (sketch)**: Already remediated at P7. No M5.1 action needed.
- **Implementation chunk this belongs to**: P7 (historical)
- **Dependencies on other drifts**: D5.1 (precedent)
- **Estimated effort**: 0 days (closed)
- **Risk to concept alignment if deferred further**: none

## Prior documentation locations (pre-M5.1)
- Plan archive lines: 1345–1351
- Code comments: [`system_agent.rs` CLI](../../../../../../modules/crates/cli/src/commands/system_agent.rs) module doc
- ADR references: none

## Lifecycle history
- 2026-04-23 — `discovered` — logged in M5/P6 close drift addendum
- 2026-04-24 — `remediated` — migrated to M5.1 catalogue; confirmed closed by P7 CLI + Web delivery
