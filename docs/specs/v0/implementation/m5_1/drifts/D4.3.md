<!-- Last verified: 2026-04-24 by Claude Code -->

# D4.3 — `resolve_agent_tools` returns `Vec<ToolSummary>`, not `Vec<Box<dyn AgentTool>>`

## Identification
- **ID**: D4.3
- **Phase of origin**: M5/P4
- **Discovery source**: `plan-archive-ledger`
- **Date discovered**: 2026-04-23 (M5/P4 close)
- **Status**: `discovered`
- **Bucket**: B — underspecified shape choice
- **Severity**: LOW
- **Tags**: `http-wire-shape`, `tool-resolver`, `phi-core-wrap`
- **Blocks**: none
- **Blocked-by**: D4.2 (real tools need real agent_loop call-site to exercise them)

## Concept alignment
- **Concept doc(s)**: [`concepts/permissions/07-templates-and-tools.md`](../../../concepts/permissions/07-templates-and-tools.md) §"tools are resources" + [`concepts/phi-core-mapping.md`](../../../concepts/phi-core-mapping.md) §AgentTool
- **Concept claim (verbatim or close paraphrase)**: Tools are typed resources bound to agents via `HAS_TOOL` edges. Tools are phi-core primitives (trait objects). Wire surface is a governance projection of the tool metadata.
- **Contradiction**: None at the wire layer — `ToolSummary { name, label, description, parameters_schema }` is the natural HTTP projection of `phi_core::AgentTool` trait methods.
- **Classification**: `concept-silent-plan-filled-gap` (concept is silent on wire shape; shipped projection is aligned)
- **phi-core leverage status**: `wrap` — `phi_core::types::tool::AgentTool` is compile-time-witnessed via `_is_phi_core_agent_tool_trait<T: AgentTool + ?Sized>` at [`tools.rs:80`](../../../../../../modules/crates/server/src/platform/sessions/tools.rs#L80). The trait itself is not re-implemented.

## Plan vs. reality
- **Plan said** (plan-archive line 1262, §Part 4 P4 §tools.rs): *"`resolve_agent_tools(repo, agent_id) -> Vec<Box<dyn AgentTool>>` + `GET /sessions/:id/tools` handler."*
- **Reality (shipped state at current HEAD)**: `pub async fn resolve_agent_tools(...) -> Result<Vec<ToolSummary>, SessionError>` at [`tools.rs:52`](../../../../../../modules/crates/server/src/platform/sessions/tools.rs#L52). `ToolSummary` struct at [line 37](../../../../../../modules/crates/server/src/platform/sessions/tools.rs#L37). The return list is empty for every agent at M5 — no `HAS_TOOL` edges are written yet.
- **Root cause**: `implementer-unilateral-choice` (HTTP boundary wants the summary shape; wrapping empty trait objects in a `Vec<Box<dyn>>` is purely ceremonial until real tools land).

## Where visible in code
- **File(s)**: [`modules/crates/server/src/platform/sessions/tools.rs`](../../../../../../modules/crates/server/src/platform/sessions/tools.rs) lines 37 (`ToolSummary` struct), 52 (fn signature), 80 (phi-core witness), 88 (test returning empty list)
- **Test evidence**: `resolve_agent_tools_returns_empty_list_at_m5` test ([line 88](../../../../../../modules/crates/server/src/platform/sessions/tools.rs#L88)) — passes; asserts the shape + the M5 "still empty" state.
- **Grep for regression**: `grep -n "fn resolve_agent_tools.*Vec<Box<dyn AgentTool>>" modules/crates/server/src/platform/sessions/tools.rs` — expect 0 hits (shape stays `Vec<ToolSummary>` until M7+).

## Remediation scope (estimate only)
- **Approach (sketch)**: When real tools land (M7+), either swap return to `Vec<Box<dyn AgentTool>>` with mapping at HTTP boundary, OR keep `ToolSummary` + add a parallel `resolve_agent_tool_impls` returning trait objects for the launch chain. Bounded-scope refactor (one file).
- **Implementation chunk this belongs to**: CH-20
- **Dependencies on other drifts**: D4.2 (real tools need real agent_loop call path)
- **Estimated effort**: 0 days at M5.1 (ratification only); 1 day at M7+ if return-shape migration chosen.
- **Risk to concept alignment if deferred further**: none while tools are empty; emerges when real tools are bound.

## Prior documentation locations (pre-M5.1)
- Plan archive lines: 1260–1266
- Code comments: [`tools.rs`](../../../../../../modules/crates/server/src/platform/sessions/tools.rs) module doc §Scope + `ToolSummary` doc
- ADR references: none

## Lifecycle history
- 2026-04-23 — `discovered` — logged in M5/P4 close drift addendum
- 2026-04-24 — `classified` — migrated to M5.1 drift catalogue; Bucket B LOW confirmed
