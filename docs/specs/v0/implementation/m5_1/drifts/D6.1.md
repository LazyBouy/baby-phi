<!-- Last verified: 2026-04-28 by Claude Code (CH-21: first call site shipped at memory-extraction listener body; both call sites now in production; Status flipped to `remediated`.) -->

# D6.1 — Listener callback extension ships the helper, not the call sites

## Identification
- **ID**: D6.1
- **Phase of origin**: M5/P6
- **Discovery source**: `plan-archive-ledger`
- **Date discovered**: 2026-04-23 (M5/P6 close)
- **Status**: `remediated`
- **Bucket**: A — load-bearing scope gap
- **Severity**: HIGH
- **Tags**: `listener-wiring`, `runtime-status-telemetry`, `deferred-to-P8`, `concept-contradiction`
- **Blocks**: R-ADMIN-13-R2 "live queue-depth + last-fired-at" surface — empty telemetry at M5
- **Blocked-by**: P8 listener bodies (memory-extraction + agent-catalog) — bodies are stubs at M5/P3; real call sites depend on them

## Concept alignment
- **Concept doc(s)**: [`concepts/system-agents.md`](../../../concepts/system-agents.md) §"runtime status telemetry" (queue-depth + last-fired are first-class system-agent observables) + [`concepts/coordination.md`](../../../concepts/coordination.md) §"event-driven reactivity" (listener-fire telemetry is the observability layer)
- **Concept claim (verbatim or close paraphrase)**: System agents expose runtime telemetry — queue depth, last-fired-at, effective parallelize, last error — updated on every listener fire.
- **Contradiction**: **Contradicts concept directly.** Helper is exported but never called; `SystemAgentRuntimeStatus` row is empty for every org at M5. Page 13's list endpoint reads this table and returns empty tiles.
- **Classification**: `contradicts-concept` (telemetry invariant violated)
- **phi-core leverage status**: `N/A — no phi-core overlap` (governance reactivity is baby-phi-native)

## Plan vs. reality
- **Plan said** (plan-archive line 1339, §Part 4 P6 §Deliverables item 3): *"all 5 listeners (Template A/C/D + memory-extraction + agent-catalog) call `repo.upsert_system_agent_runtime_status(...)` on each fire. Shared helper in `domain/src/events/listeners.rs`."*
- **Reality (shipped state at current HEAD)**: Helper shipped at [`listeners.rs:52`](../../../../../../modules/crates/domain/src/events/listeners.rs#L52) (`record_system_agent_fire`). Zero call sites. Rationale per drift text: Template A/C/D fires target grants (not system agents, no `(org, system_agent_id)` resolver on those fire paths); memory-extraction + agent-catalog ARE system-agent listeners but their bodies are stubs at M5/P3 with real implementation deferred to M5/P8.
- **Root cause**: `cascading-upstream-deferral` (listener bodies at P8 carry the natural call-site; shipping helper alone is compromise)

## Where visible in code
- **File(s)**: [`modules/crates/domain/src/events/listeners.rs`](../../../../../../modules/crates/domain/src/events/listeners.rs) lines 52–78 (helper); stub listener bodies at the same file (no calls)
- **Test evidence**: Helper itself has in-module test proving upsert semantics. **No acceptance test** asserts runtime-status tiles get populated on listener fires — drift is invisible to current test suite.
- **Grep for regression**: `grep -rn "record_system_agent_fire" modules/crates/` — expect 1 hit (definition only) while drift open; ≥3 hits post-remediation (definition + 2 call sites in P8 listener bodies).

## Remediation scope (estimate only)
- **Approach (sketch)**: Natural close at M5.2/P8 when memory-extraction + agent-catalog listener bodies land. Each body calls `record_system_agent_fire(repo, org, agent, effective_parallelize, last_error, now)` at the top after identity resolution. No new abstractions needed.
- **Implementation chunk this belongs to**: CH-21 (first call site, memory-extraction listener) + CH-22 (second call site, agent-catalog listener). Drift transitions to `remediated` only when BOTH call sites have shipped. CH-22 ships first chronologically per user-selected ordering; CH-21 is the terminal-closure owner.
- **Dependencies on other drifts**: P8 listener bodies exist first
- **Estimated effort**: 0.5 day inside P8 (add 2 call lines + 2 acceptance tests).
- **Risk to concept alignment if deferred further**: MEDIUM — extends the runtime-telemetry concept violation past M5 close. Bounded as long as M5.2 closes P8.

## Prior documentation locations (pre-M5.1)
- Plan archive lines: 1337–1343
- Code comments: [`listeners.rs`](../../../../../../modules/crates/domain/src/events/listeners.rs) helper module doc
- ADR references: none
- Other doc pointers: [`m5/architecture/system-agents.md`](../../m5/architecture/system-agents.md) §SystemAgentRuntimeStatus; [`m5/operations/system-agents-operations.md`](../../m5/operations/system-agents-operations.md) §"Listener upsert stale" playbook

## Lifecycle history
- 2026-04-23 — `discovered` — logged in M5/P6 close drift addendum
- 2026-04-24 — `classified` — migrated to M5.1 drift catalogue; Bucket A HIGH confirmed; concept contradiction pinned
- 2026-04-24 — `scoped` — assigned to CH-21 (first call site, memory-extraction listener) + CH-22 (second call site, agent-catalog listener) at M5.1/P3 close (backfill)
- 2026-04-27 — `in-chunk-plan` — CH-22 plan approved ([`build/ch-22-agent-catalog-listener-body-c5f201bb.md`](../../../../plan/build/ch-22-agent-catalog-listener-body-c5f201bb.md)); chunk now operating on second call site
- 2026-04-27 — `partial-closure` — CH-22 chunk-seal; second call site shipped (`AgentCatalogListener::on_event` body calls `record_system_agent_fire` on every fire — see [`listeners.rs`](../../../../../../modules/crates/domain/src/events/listeners.rs) and the runtime-status tile assertion in unit test `agent_catalog_listener_runtime_status_tile_advances_on_fire` + acceptance test `agent_create_populates_catalog_and_advances_runtime_status_tile` in [`acceptance_system_flows_s03.rs`](../../../../../../modules/crates/server/tests/acceptance_system_flows_s03.rs)). Status held at `in-chunk-plan` — final `remediated` transition logged at CH-21 seal when memory-extraction listener body ships the first call site. ADR-0035 ratifies the per-listener audit-mode convention used by the body. CH-22 also wired the six production emit sites (D35.5) — without those, the listener body would have been dead code from production's POV.
- 2026-04-28 — `remediated` — CH-21 chunk-seal; first call site shipped (`MemoryExtractionListener::on_event` body now calls `record_system_agent_fire` on every non-aborted fire after resolving the `memory-extraction-agent` system agent — see [`listeners.rs`](../../../../../../modules/crates/domain/src/events/listeners.rs) and the tile-advance assertion in unit test `memory_extraction_listener_advances_runtime_status_tile_for_extractor` + acceptance test `scenario_3_disabled_extractor_skips_both_extraction_and_telemetry` in [`acceptance_memory_extraction.rs`](../../../../../../modules/crates/server/tests/acceptance_memory_extraction.rs)). Both system-agent runtime-status tiles (catalog + memory-extractor) now advance on listener fire — the `SystemAgentRuntimeStatus` table is no longer empty. ADR-0040 ratifies the heuristic-v0 listener body that bound the call site; ADR-0041 ratifies the new `DomainEvent::MemoryExtracted` variant + `platform.memory.extracted` audit class shipped alongside.
