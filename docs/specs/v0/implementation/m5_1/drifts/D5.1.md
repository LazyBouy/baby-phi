<!-- Last verified: 2026-04-24 by Claude Code -->

# D5.1 — CLI + Web surfaces deferred from P5 to P7

## Identification
- **ID**: D5.1
- **Phase of origin**: M5/P5
- **Discovery source**: `plan-archive-ledger`
- **Date discovered**: 2026-04-23 (M5/P5 close)
- **Status**: `remediated`
- **Bucket**: A — load-bearing scope gap (at discovery; now closed)
- **Severity**: MEDIUM (was HIGH at discovery due to scope reduction within P5; downgraded post-remediation)
- **Tags**: `scope-deferral`, `closed-at-p7`
- **Blocks**: (was) D6.2 precedent-followed; now both closed
- **Blocked-by**: none

## Concept alignment
- **Concept doc(s)**: [`concepts/permissions/07-templates-and-tools.md`](../../../concepts/permissions/07-templates-and-tools.md) (Template A/B/C/D/E lifecycle) + admin page 12 requirement R-ADMIN-12-*
- **Concept claim (verbatim or close paraphrase)**: Templates are adopted, approved, denied, revoked through operator-facing surfaces. Concept is silent on per-surface (HTTP / CLI / Web) sequencing.
- **Contradiction**: None. All three surfaces now ship.
- **Classification**: `concept-silent-plan-filled-gap`
- **phi-core leverage status**: `N/A — no phi-core overlap`

## Plan vs. reality
- **Plan said** (plan-archive line 1305, §Part 4 P5 §Deliverables items 3 & 4): *"Operators can approve / deny / adopt-inline / revoke-cascade authority templates via page 12 (HTTP + CLI + Web)."* CLI: `phi template {list, approve, deny, adopt, revoke}`; Web: `/organizations/[id]/templates/`.
- **Reality (shipped state at current HEAD)**: P5 shipped HTTP surface only (7 files under `server/src/platform/templates/` + 5 routes + 7 acceptance tests). CLI + Web shipped later at P7. Current HEAD: [`cli/src/commands/template.rs`](../../../../../../modules/crates/cli/src/commands/template.rs) (D5.1 CLI closure) + `modules/web/app/(admin)/organizations/[id]/templates/`/organizations/[id]/templates/) (Web closure). **D5.1 is closed at HEAD.**
- **Root cause**: `implementer-unilateral-choice` (batched CLI + Web across P5 + P6 + P7 into a single phase for cleanliness).

## Where visible in code
- **File(s)**: HTTP: [`modules/crates/server/src/platform/templates/`](../../../../../../modules/crates/server/src/platform/templates/) (7 files). CLI: [`modules/crates/cli/src/commands/template.rs`](../../../../../../modules/crates/cli/src/commands/template.rs) (shipped at P7). Web: `modules/web/app/(admin)/organizations/[id]/templates/`/organizations/[id]/templates/) (page.tsx + actions.ts).
- **Test evidence**: `acceptance_authority_templates` (7 scenarios) + P7 completion-regression test covers `phi template` tree.
- **Grep for regression**: `ls modules/crates/cli/src/commands/template.rs modules/web/app/\(admin\)/organizations/\[id\]/templates/page.tsx` — expect both to exist.

## Remediation scope (estimate only)
- **Approach (sketch)**: Already remediated at P7. No M5.1 action needed; catalog this as historical reference. Status = `remediated`.
- **Implementation chunk this belongs to**: P7 (historical)
- **Dependencies on other drifts**: none
- **Estimated effort**: 0 days (closed).
- **Risk to concept alignment if deferred further**: none (already honored).

## Prior documentation locations (pre-M5.1)
- Plan archive lines: 1303–1309
- Code comments: [`template.rs` CLI](../../../../../../modules/crates/cli/src/commands/template.rs) module doc; templates page.tsx inline comment
- ADR references: none
- Other doc pointers: M5 Pre-P7 gate (plan archive lines ~1490+)

## Lifecycle history
- 2026-04-23 — `discovered` — logged in M5/P5 close drift addendum (material scope deferral)
- 2026-04-24 — `remediated` — migrated to M5.1 catalogue; confirmed closed by P7 CLI + Web delivery
