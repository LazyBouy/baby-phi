<!-- Last verified: 2026-04-24 by Claude Code -->

# D4.5 — Repo-trait `write_uses_model_edge` added as first-class method (not a generic `create_edge`)

## Identification
- **ID**: D4.5
- **Phase of origin**: M5/P4
- **Discovery source**: `plan-archive-ledger`
- **Date discovered**: 2026-04-23 (M5/P4 close)
- **Status**: `discovered`
- **Bucket**: C — convention/pattern decision
- **Severity**: LOW
- **Tags**: `repository-trait`, `typed-edge-writers`
- **Blocks**: none (future edges follow the typed-writer pattern)
- **Blocked-by**: none

## Concept alignment
- **Concept doc(s)**: [`concepts/ontology.md`](../../../concepts/ontology.md) §"edges are typed by their relation" (edge typing is first-class)
- **Concept claim (verbatim or close paraphrase)**: Edges have distinct types with distinct semantics. The repository API surface preserves type-safety at edge writes.
- **Contradiction**: None — typed-writer pattern matches concept's "edges are typed" claim more precisely than a generic `create_edge(&Edge)` would.
- **Classification**: `concept-silent-plan-filled-gap`
- **phi-core leverage status**: `N/A — no phi-core overlap`

## Plan vs. reality
- **Plan said** (plan-archive line 1278, §Part 4 P4 §launch_session Step 6): *"Compound tx: persist Session + first LoopRecord + `runs_session` edge + `uses_model` edge"* — implied a generic edge-writer path.
- **Reality (shipped state at current HEAD)**: `Repository::write_uses_model_edge(agent, model_runtime) -> EdgeId` is a dedicated typed method at [`repository.rs:1135`](../../../../../../modules/crates/domain/src/repository.rs#L1135). Default trait body returns `Backend(...)` so future impls fail fast until overridden; both current impls (in-memory + SurrealDB) override it; SurrealDB uses the LET-first RELATE pattern (D2.2).
- **Root cause**: `implementer-unilateral-choice` (preserves type safety at trait boundary over cost of slightly wider trait surface).

## Where visible in code
- **File(s)**: [`modules/crates/domain/src/repository.rs`](../../../../../../modules/crates/domain/src/repository.rs) lines 1135–1142 (trait method + default body)
- **Test evidence**: `C-M5-2` (UsesModel edge writer) closure test in `acceptance_sessions_m5p4`.
- **Grep for regression**: `grep -n "fn write_uses_model_edge" modules/crates/` — expect ≥3 hits (trait + 2 impls).

## Remediation scope (estimate only)
- **Approach (sketch)**: Confirm-in-place. Every first-written edge from a higher tier (Template C's MANAGES, Template D's HAS_AGENT_SUPERVISOR) gets its own typed writer method following this pattern.
- **Implementation chunk this belongs to**: CH-20
- **Dependencies on other drifts**: none
- **Estimated effort**: 0 days.
- **Risk to concept alignment if deferred further**: none.

## Prior documentation locations (pre-M5.1)
- Plan archive lines: 1276–1282
- Code comments: [`repository.rs`](../../../../../../modules/crates/domain/src/repository.rs) `write_uses_model_edge` doc comment
- ADR references: none

## Lifecycle history
- 2026-04-23 — `discovered` — logged in M5/P4 close drift addendum
- 2026-04-24 — `classified` — migrated to M5.1 drift catalogue
