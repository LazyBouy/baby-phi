<!-- Last verified: 2026-04-24 by Claude Code -->

# D7.2 — `--model-config-id` ships as a convenience flag alongside `--patch-json` (not replacing it)

## Identification
- **ID**: D7.2
- **Phase of origin**: M5/P7
- **Discovery source**: `plan-archive-ledger`
- **Date discovered**: 2026-04-24 (M5/P7 close)
- **Status**: `discovered`
- **Bucket**: B — underspecified shape choice
- **Severity**: LOW
- **Tags**: `cli-ux`, `backward-compat`, `C-M5-5-wire`
- **Blocks**: none
- **Blocked-by**: none

## Concept alignment
- **Concept doc(s)**: [`concepts/agent.md`](../../../concepts/agent.md) §"AgentProfile binds a ModelConfig" (C-M5-5 wire)
- **Concept claim (verbatim or close paraphrase)**: Agents bind to a model_config_id via their profile. The operator surface to update this binding is CLI + HTTP.
- **Contradiction**: None. Both paths reach the same HTTP wire; concept silent on flag shape.
- **Classification**: `concept-silent-plan-filled-gap`
- **phi-core leverage status**: `N/A — no phi-core overlap`

## Plan vs. reality
- **Plan said** (plan-archive line 1396, §Part 4 P7 §Deliverables item 2): *"extend `phi agent update` with `--model-config-id` flag (C-M5-5 wire)."*
- **Reality (shipped state at current HEAD)**: Both `--patch-json` + `--model-config-id` coexist as `Option<String>` ([`main.rs:234-235`](../../../../../../modules/crates/cli/src/main.rs#L234-L235)); mutual-exclusion enforced at `update_impl` top. Both reach `PATCH /api/v0/agents/:id/profile`. Completion-regression asserts both as surface-level invariants.
- **Root cause**: `implementer-unilateral-choice` (removing `--patch-json` would regress M4 operators; additive chose).

## Where visible in code
- **File(s)**: [`modules/crates/cli/src/main.rs`](../../../../../../modules/crates/cli/src/main.rs) lines 218–236 (`Update` variant clap surface with both flags + doc explaining XOR)
- **Test evidence**: `completion_help::completion_scripts_expose_m5_p7_agent_update_model_config_id_flag` pins both flags.
- **Grep for regression**: `grep -n "model_config_id.*Option\|patch_json.*Option" modules/crates/cli/src/main.rs` — expect ≥2 hits.

## Remediation scope (estimate only)
- **Approach (sketch)**: Confirm-in-place. Additive flag shape is forward-stable.
- **Implementation chunk this belongs to**: CH-19
- **Dependencies on other drifts**: none
- **Estimated effort**: 0.5 day (ADR ratifying the convention).
- **Risk to concept alignment if deferred further**: none.

## Prior documentation locations (pre-M5.1)
- Plan archive lines: 1394–1400
- Code comments: [`main.rs`](../../../../../../modules/crates/cli/src/main.rs) `Update` variant doc comment (lines 218–236)
- ADR references: none

## Lifecycle history
- 2026-04-24 — `discovered` — logged in M5/P7 close drift addendum
- 2026-04-24 — `classified` — migrated to M5.1 drift catalogue
