<!-- Last verified: 2026-04-24 by Claude Code -->

# D7.4 — Page 11 "Recent sessions" retrofit is web-side (no server-detail mutation)

## Identification
- **ID**: D7.4
- **Phase of origin**: M5/P7
- **Discovery source**: `plan-archive-ledger`
- **Date discovered**: 2026-04-24 (M5/P7 close)
- **Status**: `discovered`
- **Bucket**: B — underspecified shape choice
- **Severity**: LOW
- **Tags**: `web-ui`, `retrofit-location`, `wire-stability`
- **Blocks**: none (both paths render same UI)
- **Blocked-by**: none

## Concept alignment
- **Concept doc(s)**: [`concepts/project.md`](../../../concepts/project.md) §"project detail surfaces recent sessions" (project context includes session history)
- **Concept claim (verbatim or close paraphrase)**: Project detail surface includes recent session history. Concept is silent on server-detail vs web-parallel-fetch implementation.
- **Contradiction**: None.
- **Classification**: `concept-silent-plan-filled-gap`
- **phi-core leverage status**: `N/A — no phi-core overlap` (web UI layer)

## Plan vs. reality
- **Plan said** (plan-archive line 1412, §Part 4 P7 §Deliverables item 4): *"Page 11's 'Recent sessions' panel (M4 placeholder → real rows from `list_sessions_in_project`)."*
- **Reality (shipped state at current HEAD)**: Web-side path: Next.js page component fetches `/api/v0/projects/:id/sessions` in parallel with detail fetch and renders rows. Server-side `ProjectDetail.recent_sessions` stays `Vec::new()` at M5 (intentional, per `detail.rs:229`). Visible in `modules/web/app/(admin)/organizations/[id]/projects/[project_id]/page.tsx`, line 88 ("M5/P7 'Recent sessions' retrofit" comment) + line 94 (`listSessionsInProjectApi` call) + line 203 (`<h2>Recent sessions</h2>` render).
- **Root cause**: `implementer-unilateral-choice` (smaller blast radius; keeps M4 wire contract frozen).

## Where visible in code
- **File(s)**: `modules/web/app/(admin)/organizations/[id]/projects/[project_id]/page.tsx` lines 88–105, 203+ (web-side retrofit); `modules/crates/server/src/platform/projects/detail.rs:229` (server-side still returns empty)
- **Test evidence**: Web `npm run build` + `npm run typecheck` green; no Playwright e2e covers the full retrofit path yet (per D7.5).
- **Grep for regression**: `grep -n "listSessionsInProjectApi" modules/web/app/` — expect ≥1 hit.

## Remediation scope (estimate only)
- **Approach (sketch)**: Confirm-in-place. M7 project-detail hardening may promote to server-side (path (a)) and strip the web-side fetch. Both paths render the same UI.
- **Implementation chunk this belongs to**: CH-19
- **Dependencies on other drifts**: none
- **Estimated effort**: 0 days (shipped); ~0.5 day at M7 if server-side consolidation is pursued.
- **Risk to concept alignment if deferred further**: none.

## Prior documentation locations (pre-M5.1)
- Plan archive lines: 1410–1416
- Code comments: `page.tsx` inline comment block on the retrofit (line 88+)
- ADR references: none

## Lifecycle history
- 2026-04-24 — `discovered` — logged in M5/P7 close drift addendum
- 2026-04-24 — `classified` — migrated to M5.1 drift catalogue
