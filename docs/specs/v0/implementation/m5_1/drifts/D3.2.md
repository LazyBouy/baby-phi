<!-- Last verified: 2026-04-24 by Claude Code -->

# D3.2 — Listener wiring lives in `state::build_event_bus_with_m5_listeners` (free function), not `AppState::new`

## Identification
- **ID**: D3.2
- **Phase of origin**: M5/P3
- **Discovery source**: `plan-archive-ledger`
- **Date discovered**: 2026-04-23 (M5/P3 close)
- **Status**: `discovered`
- **Bucket**: C — convention/pattern decision
- **Severity**: LOW
- **Tags**: `event-bus`, `listener-wiring`, `app-state`
- **Blocks**: none (future listeners extend this helper, not `AppState::new`)
- **Blocked-by**: none

## Concept alignment
- **Concept doc(s)**: [`concepts/coordination.md`](../../../concepts/coordination.md) §"event-driven reactivity" (listener pattern concept)
- **Concept claim (verbatim or close paraphrase)**: Governance reactivity flows through an event bus; listeners subscribe to domain events and fire side-effects. Concept is silent on the constructor seam (constructor method vs free function).
- **Contradiction**: None.
- **Classification**: `concept-silent-plan-filled-gap`
- **phi-core leverage status**: `N/A — no phi-core overlap`

## Plan vs. reality
- **Plan said** (plan-archive line 1210, §Part 4 P3 Deliverables + §C11): *"`AppState::new` wires all 5 listeners (M4's Template A + the 4 new). `server/src/state.rs::tests::handler_count_at_m5` asserts the count."*
- **Reality (shipped state at current HEAD)**: `AppState` has no `::new()` constructor — it's a plain `#[derive(Clone)] pub struct` with a struct-literal construction site in `main.rs`. Listener wiring lives in `pub fn build_event_bus_with_m5_listeners(repo, audit) -> Arc<InProcessEventBus>` at [`state.rs:93`](../../../../../../modules/crates/server/src/state.rs#L93). Both `main.rs` and the state.rs test module call this helper. Test name: `handler_count_is_five_at_m5` at [`state.rs:143`](../../../../../../modules/crates/server/src/state.rs#L143) — asserts `bus.handler_count() == 5`.
- **Root cause**: `implementer-unilateral-choice` (cleaner seam for test visibility without bundling 5+ `Arc<...>` into a constructor signature).

## Where visible in code
- **File(s)**: [`modules/crates/server/src/state.rs`](../../../../../../modules/crates/server/src/state.rs) line 93 (`build_event_bus_with_m5_listeners`), line 143 (`handler_count_is_five_at_m5` test)
- **Test evidence**: `handler_count_is_five_at_m5` test passes — asserts the helper returns a bus with exactly 5 handlers.
- **Grep for regression**: `grep -n "fn new(.*) -> AppState" modules/crates/server/src/state.rs` — expect 0 hits; `grep -n "build_event_bus_with_m5_listeners" modules/crates/server/src/` — expect 2 hits (main.rs + state.rs).

## Remediation scope (estimate only)
- **Approach (sketch)**: Confirm-in-place. Single wiring site convention; any future listener extends the helper.
- **Implementation chunk this belongs to**: CH-20
- **Dependencies on other drifts**: none
- **Estimated effort**: 0 engineer-days.
- **Risk to concept alignment if deferred further**: none.

## Prior documentation locations (pre-M5.1)
- Plan archive lines: 1208–1214
- Code comments: [`state.rs`](../../../../../../modules/crates/server/src/state.rs) module-level doc + helper doc-comment
- ADR references: none
- Other doc pointers: [`m5/architecture/event-bus-m5-extensions.md`](../../m5/architecture/event-bus-m5-extensions.md) §"Listener registration"

## Lifecycle history
- 2026-04-23 — `discovered` — logged in M5/P3 close drift addendum
- 2026-04-24 — `classified` — migrated to M5.1 drift catalogue
