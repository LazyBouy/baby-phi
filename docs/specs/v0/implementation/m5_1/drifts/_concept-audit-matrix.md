<!-- Last verified: 2026-05-19 by Claude Code (CH-28 P-DOCS deliverable 6 — in the `concepts/agent.md` block: Parallelized Sessions row Code-evidence cell rewritten to cite the post-CH-28 storage location (`Blueprint` per-agent override row reached via `AGENT_USES_BLUEPRINT_OVERRIDE`; resolution via `read_agent_profile_via_blueprint_or_fallback` with override→template→default fallback); Covering-drift cell flipped to `M6+-OPEN-01 ✓` (closed via CH-28 / ADR-0063 §D63.1); NEW row added "Soul (Template Blueprint + per-agent overrides)" status `honored` capturing the N:1 cardinality + hybrid Blueprint design + concept-doc reframing. In the `concepts/ontology.md` block: "37 node types" row title + Status updated to "38 node types" (NEW `Blueprint` per ADR-0063 §D63.1); "71 edge types" row title + Status updated to "74 edge types" reflecting CH-25 (`OWNS` to 72) + CH-28 (+2 to 74); NEW row "Agent ↔ AgentProfile cardinality" status `honored` capturing the HAS_PROFILE → USES_PROFILE rename + N:1 cardinality flip + serde rename-aware deserialization. Cycle hex `0412eb06`.) -->
<!-- Last verified: 2026-05-11 by Claude Code (CH-24 P-DOCS — NEW row added in the `concepts/phi-core-mapping.md` block: "Session list query at API surface (page-11 recent_sessions panel)" — Status flipped from `silent-in-code` (M4 placeholder `Vec::new()` hardcoded at `detail.rs:229+654`; inline doc-comment at `detail.rs:33` promised M5 flip never landed) to `**honored**` via CH-24 P-FLIP-RECENT-SESSIONS (ADR-0059 §D59.1 LIMIT 10 query-side cap + §D59.2 dedicated `Repository::list_recent_sessions_for_project` method + §D59.3 6-field `RecentSessionEntry` view-shape struct at `domain::model::composites_m5`; `started_by_display_name` deferred per §D59.3-FOLLOWUP). Covering-drift cell cites `D-CH24-recent-sessions-api-flip ✓`. Mid-cycle scope-expansion approved at gate-2 user-lock; 2-of-3 gate-2.5 user-locks DIVERGENT from planner-recommendations. Cycle hex `5778bb77`.) -->
<!-- Last verified: 2026-05-10 by Claude Code (CH-20 P2 chunk-progress: 16 Bucket C drifts ratified via CH-20 / ADR-0058 (D1.1, D1.2, D1.3, D2.1, D2.2, D3.1, D3.2, D3.3, D3.4, D4.3, D4.4, D4.5, D4.6, D6.4, D7.3, D7.6) → 5 convention-docs at `v0/conventions/` (persistence.md / wrap-pattern.md / event-bus-wiring.md / cli-patterns.md / web-patterns.md). **NO new matrix rows added** — most C-bucket drifts are below matrix granularity (CH-19 / ADR-0057 §"Label-coverage rollup notes" precedent for D5.2 + D7.5 + D7.4 + D7.2). Single matrix-row update per CH-12 retro Row 1 P4 paperwork addendum: in the `concepts/phi-core-mapping.md` block, the "Session wrap" row Code-evidence cell extended to cite ADR-0058 §D58.2 wrap-pattern ratification (nested-`inner` form + `Arc<Mutex<_>>` interior-mut + cross-ref ADR-0029 §D29.1/§D29.2); Covering-drift cell flipped from `—` to `**D1.3 ✓ + D3.3 ✓**` (CH-07-style label-coverage rollup precedent). **15 of 16 drifts have no matrix row touched**: D1.1 / D1.2 / D2.1 / D2.2 / D3.1 / D3.2 / D3.4 / D4.4 / D4.5 (persistence + event-bus mechanics below matrix granularity; ratification recorded in convention-doc + ADR-0058 sub-decision + drift-file lifecycle); D4.3 (HTTP wire-shape projection — the existing `permissions/07` "14 Tool Authority Manifest examples" row stays `partially-honored` because that row is about TAM examples, not the wire-shape projection; matrix-side rollup is the "Session wrap" row Covering-drift extension); D4.6 (dual-mode discriminator — wrap-pattern subsection rolls up under same "Session wrap" row); D6.4 (audit-event placement cross-ref — no exact matrix row, ratification recorded in ADR-0058 §D58.5 + `event-bus-wiring.md` §"Audit-event placement cross-ref"; mirrors CH-19's D5.2 handling); D7.3 (CLI scope-addition discipline — below matrix granularity, ratification in `cli-patterns.md` + ADR-0058 §D58.6); D7.6 (Next.js server-action shape — below matrix granularity, ratification in `web-patterns.md` + ADR-0058 §D58.7). Cycle hex `240616a4`.) -->
<!-- Last verified: 2026-05-10 by Claude Code (CH-19 P2 chunk-progress: 10 Bucket B drifts ratified via CH-19 / ADR-0057; matrix-row updates applied per CH-12 retro Row 1 P4 paperwork addendum (copy-paste of CH-19 plan §2 target-column letter-for-letter). In the `concepts/ontology.md` block: "InboxObject/OutboxObject" row Status flipped letter-for-letter from `silent-in-code` to `**accepted-as-is**` (D-new-25 ✓ → ADR-0057 §D57.8 → M6-DEFERRED-02 deferral); "69 edge types" row title + Status flipped letter-for-letter from `partially-honored` to `**honored**` (D-new-21 ✓ → ADR-0057 §D57.7 → canonical count locked to 71 via test-asserted invariant). In the `concepts/token-economy.md` block: 3 rows ("Worth formula", "Rating window size=20", "Intern → Contract carry-forward") Status flipped letter-for-letter from `silent-in-code` to `**accepted-as-is**` (D-new-27 ✓ → ADR-0057 §D57.9 → M6-or-M7-DEFERRED). In the `concepts/system-agents.md` block: "Two v0 system agents per org" row Status stays `**honored**` (target column `honored`); Code-evidence cell extended to cite the 3-way union bucketing convention at `system_agents/list.rs:55-70` per ADR-0057 §D57.3; Covering-drift cell flipped to `D6.3 ✓`. In the `concepts/permissions/07-templates-and-tools.md` block: "Templates A–E via adoption AR" row Status stays `**honored**` (target column `honored`); Code-evidence cell extended to cite multi-AR-per-(org, kind) history + `find_adoption_ar` most-recent semantics per ADR-0057 §D57.2; Covering-drift cell flipped to `D5.3 ✓`. "Standard Org Template config" + "Standard Project Template config" rows Status flipped letter-for-letter from `concept-aspirational` to `**honored**` (D-new-30 ✓ → ADR-0057 §D57.10 → YAML-as-conceptual-contract / AR-as-implementation-surface mapping). **Label-coverage rollup notes per CH-07-style discipline**: the §2 plan-walk also names (a) §"Auth Request Lifecycle" line 7 + §"Interaction with Authority Templates" line 210 of `permissions/02-auth-request.md` (D5.3 primary) — rolls up under the existing "Templates A–E via adoption AR" row in the `permissions/07` block, no separate row in `permissions/02` block; (b) §"Soul" line 158 of `agent.md` (D7.2 — `--patch-json` + `--model-config-id` additive-flag convention) — no exact matrix row; D7.2 is CLI-flag-shape ratification, below matrix granularity per CH-19 planner §3.C MINIMAL user-facing impact; ratification recorded in ADR-0057 §D57.4 + the D7.2 drift file lifecycle; (c) §"Project (Node Type)" line 13 of `project.md` (D7.4 — recent-sessions UI surface) — no exact matrix row; concept-doc gets NEW sub-§ "Recent sessions UI surface (web-side at v0.1)" per ADR-0057 §D57.5; matrix-side rollup is the existing "Project fields" row + ADR cross-ref; (d) D7.5 — concept silent on test strategy below concept granularity per drift body line 18; no concept-doc refresh required + no matrix row required; ADR-0057 §D57.6 records the test-strategy decision + deferral to CH-24 Playwright; (e) D5.2 — concept silent on domain-tier-vs-platform-tier event-builder placement (below concept granularity per drift body line 19); no matrix row required; ADR-0057 §D57.1 records the convention `domain::audit::events::mX::*` for state-machine events + `server::platform::<page>::audit_events` for HTTP-handler events. Cycle hex `2c520ba7`.) -->
<!-- Last verified: 2026-05-09 by Claude Code (CH-18 P3: in the `permissions/02-auth-request.md` block, the "Per-state ACL matrix" row Status flipped letter-for-letter from `silent-in-code` to `**honored** — typed function `check_auth_request_access` captures the matrix verbatim; **17 production callsites** consult it (4 mutation + 8 submit + 2 list + 1 slot-fill read + 2 NO-OP-system-internal skips); admin/auditor read-at-every-state column partially-honoured per `D-CH18-FOLLOWUP-01` deferral note` per CH-12 retro Row 1 P4 paperwork addendum (copy-paste of CH-18 plan §2 row 1 target column); Code-evidence cell now cites `domain::auth_requests::access::check_auth_request_access` predicate body + `IntendedOp` 11-variant enum + `AuthRequestAccessError` 5-variant typed-error enum at `domain/src/auth_requests/access.rs` + 17 production wiring callsites across `server/src/platform/templates/{approve,deny,revoke}.rs` + `server/src/platform/projects/create.rs` (slot-fill mutation + slot-fill read) + `server/src/platform/orgs/{dashboard.rs:273,293, show.rs:63}` (list-side silent post-filter) + 8 submit-side defence-in-depth synthetic-Draft probe sites at `server/src/platform/{projects/create.rs:470, defaults/put.rs, secrets/add.rs, mcp_servers/{register,patch_tenants,archive}.rs, model_providers/{register,archive}.rs}` + 7 kernel-internal fast-path skip sites documented at ADR-0056 §D56.8 + new `auth_request.access_denied` Alerted-class audit-event builder at `domain/src/audit/events/m5_2/auth_request_access.rs` + `PrincipalRef` `PartialEq, Eq` derive + ADR-0056 §D56.1/§D56.2/§D56.3/§D56.5/§D56.6/§D56.7/§D56.8/§D56.9/§D56.10; Covering-drift cell flipped to `D-new-12 ✓`. New follow-up drifts filed: `D-CH18-FOLLOWUP-01` (admin/auditor role-discrimination M6+) + `D-CH18-FOLLOWUP-02` (adopt.rs submit-side wiring deferred — admin-on-behalf-of-CEO structural mismatch). Cycle hex `c77937bc`.) -->
<!-- Last verified: 2026-05-09 by Claude Code (CH-17 chunk-seal: in the `permissions/05-memory-sessions.md` block, NEW row added "Session lifecycle — live events (operator observes via SSE)" Status `honored` per CH-12 retro Row 1 P4 paperwork addendum (copy-paste of CH-17 plan §2 target column); Code-evidence cell cites `server::platform::sessions::events::open_live_stream` + `server::handlers::sessions::events` SSE handler + `domain::permissions::builders::build_session_observe_manifest` (`[Observe]` on `session_object`) + `BabyPhiSessionRecorder.broadcast_tx: Option<broadcast::Sender<AgentEvent>>` tap + `tokio::sync::broadcast::channel(64)` buffer + 30s keep-alive + `BroadcastStreamRecvError::Lagged` typed-event-then-close + `domain::audit::events::m5_2::session_live_stream::session_live_stream_denied` Alerted-class event + ADR-0055 §D55.1/§D55.3/§D55.4/§D55.5/§D55.6/§D55.8; Covering-drift cell `D7.1 ✓`. In the same block, the existing "Templates A/B/C/D auto-fire grants" row Code-evidence cell extended to cite the F5.B.subfork.a Template A mint extension `[Read, Inspect, List, Observe]` + migration `0016_template_a_session_object_grant_add_observe.surql` per ADR-0055 §D55.9; Covering-drift cell extended to include `D7.1 ✓` alongside any existing markers. In the `permissions/03-action-vocabulary.md` block, NEW row added "Observability action verbs (observe / log / attest) universal across fundamentals — first runtime exercise on session_object" Status `honored` per CH-12 retro Row 1 (copy-paste of CH-17 plan §2 row covering concept-doc 03 line 22 + line 44); Code-evidence cell cites `Action::Observe` at `domain/src/permissions/action.rs:73,282,322` (pre-existing per CH-04 / ADR-0043) + first runtime exercise via `build_session_observe_manifest` + `Action::CANONICAL.len() == 34` invariant preserved; Covering-drift `D7.1 ✓`.) -->
<!-- Last verified: 2026-05-08 by Claude Code (CH-14 chunk-seal gate-2 inline correction: in the `permissions/08-worked-example.md` block, the "Ad-hoc AR + revocation cascade" row Code-evidence cell extended to cite the new `domain::repository::CascadeResult` typed return + per-cascaded-AR `auth_request.revoked` emission + AR-state-transition logic + 7 acceptance tests (was 5); Covering-drift cell extended to include `D-CH14-FOLLOWUP-02 ✓` alongside `D-new-18 ✓`. Status stays `**honored**`.) -->
<!-- Last verified: 2026-05-08 by Claude Code (CH-14 chunk-seal: in the `concepts/README.md` block, the "Provenance" row Status flipped letter-for-letter from `partially-honored` to `**honored**` per CH-12 retro Row 1 P4 paperwork addendum (copy-paste of CH-14 plan §2 row 1 target); Code-evidence cell now cites `domain::permissions::axioms::{system_genesis_principal, is_bootstrap_ar}` + `Repository::walk_provenance_chain` + `Repository::revoke_grants_by_descends_from_recursive` + ADR-0053 §D53.1/§D53.2/§D53.3/§D53.4; Covering-drift cell flipped to `D-new-14 ✓`. In the `permissions/README.md` block, the "Provenance traversal — Bootstrap axiom chain" row Status flipped letter-for-letter from `partially-honored` to `**honored**` (Code-evidence cell cites the same axioms + walker surfaces; Covering-drift `D-new-14 ✓`). In the `permissions/02-auth-request.md` block, the "System Bootstrap Template + system:genesis" row Status flipped letter-for-letter from `partially-honored` to `**honored**` (Code-evidence cell cites the typed const + bootstrap claim flip at `bootstrap/claim.rs:192,204` + 5 acceptance tests + ADR-0053 §D53.1/§D53.2/§D53.6; Covering-drift `D-new-14 ✓`). In the `permissions/04-manifest-and-resolution.md` block, the "Provenance chain to bootstrap" row Status flipped letter-for-letter from `partially-honored` to `**honored**` (Code-evidence cell cites `walk_provenance_chain` impls + `AuthRequest.descends_from_grant` field + migration 0014 + ADR-0053 §D53.3/§D53.5; Covering-drift `D-new-14 ✓`). In the `permissions/08-worked-example.md` block, the "Ad-hoc AR + revocation cascade" row Status flipped letter-for-letter from `silent-in-code` to `**honored**` (Code-evidence cell cites `revoke_grants_by_descends_from_recursive` BFS impls + Template-revoke handler flip + 5 acceptance tests covering 3-hop cascade + idempotency + depth-cap + ADR-0053 §D53.4; Covering-drift `D-new-18 ✓`). No new rows added.) -->
<!-- Last verified: 2026-05-07 by Claude Code (CH-08 P3 chunk-progress: in the `permissions/02` block, the "allocate vs transfer cardinality" row Status flipped letter-for-letter from `silent-in-code` to `**honored**` per CH-12 retro Row 1 P4 paperwork addendum (copy-paste of CH-08 plan §2 row 1 target); Code-evidence cell now cites `Repository::apply_transfer_grant` (repository.rs:1397) + InMemoryRepository impl (in_memory.rs:1868) + SurrealStore impl (store/src/repo_impl.rs:2691) + 6 atomicity tests + 3 SurrealStore integration tests + ADR-0052 §D52.1/§D52.5/§D52.6; Covering-drift cell flipped to `D-new-13 ✓`. In the `permissions/03` block, the "`allocate` umbrella" row Status flipped letter-for-letter from `silent-in-code` to `**honored**` (Code-evidence cell cites `domain::permissions::AllocateRefinement` (allocate_refinement.rs) + `Grant.allocate_refinement` (model/nodes.rs:701) with `#[serde(default)]` shielding + 3 unit tests + ADR-0052 §D52.2/§D52.3); Covering-drift `D-new-29 ✓`. No new rows added. Note: the §2 plan-walk also names §"`allocate` Scope Semantics" line 197 (refinement framing on permissions/02) as a third claim flipping at this chunk; the matrix does not carry a separate row for that §-anchor — the claim rolls up under the existing `allocate` umbrella row at line 165 of the matrix per CH-07-style label-coverage rollup. CH-08 implementer surfaced this label-coverage gap as a deviation in the P3 report, mirroring CH-07's pattern.) -->
<!-- Last verified: 2026-05-07 by Claude Code (CH-07 chunk-seal: in the `permissions/04` block, the "5-tier scope cascade" row Status flipped letter-for-letter from `partially-honored` to `honored` (Code-evidence cell now cites `engine.rs::step_5_scope_resolution` (full 2-tier cascade body) + `engine.rs::cascade_intersection_fallback` + ADR-0051 §D51.1 + §D51.2; Covering-drift cell flipped to `D-new-06 ✓`). In the `permissions/06` block, the "Unified resolve_scope cascade" row Status flipped letter-for-letter from `partially-honored` to `honored` (Code-evidence cell cites the same `step_5_scope_resolution` body + ADR-0051 §D51.4 + §D51.5; Covering-drift `D-new-06 ✓`); the "Contractor model" row Status flipped letter-for-letter from `silent-in-code` to `honored` (Code-evidence cell cites `engine.rs::step_2a_ceiling` membership-bound filter + ADR-0051 §D51.6; Covering-drift `D-new-20 ✓`). In the `permissions/08` block, the "Shape A/B/C resolution per worked examples" row Status flipped letter-for-letter from `partially-honored` to `honored` (Code-evidence cell cites the cascade body + acceptance tests at `tests/multi_scope_cascade_acceptance.rs`; Covering-drift `D-new-06 ✓`); the "Contractor scenario" row Status flipped letter-for-letter from `silent-in-code` to `honored` (Code-evidence cell cites the `step_2a_ceiling` membership-bound filter + Scenario 7 acceptance test; Covering-drift `D-new-20 ✓`). No new rows added. Note: the §2 plan-walk also names §"Key Invariants line 310" (permissions/04), §"Subject-Side Reach Is Bounded" (permissions/06), and §"Summary: Who Can Read What" (permissions/08); the matrix does not carry separate rows for those §-anchors — the claims roll up under the existing rows updated above. CH-07 implementer surfaced this label-coverage gap as a deviation in the P3 report.) -->
<!-- Last verified: 2026-05-04 by Claude Code (CH-13 chunk-seal: in the `permissions/07-templates-and-tools.md` block, the "audit_class composition: strictest wins" row Status flipped letter-for-letter from `silent-in-code` to `honored` per CH-12 retro Row 1 P4 paperwork addendum (copy-paste of CH-13 plan §2 row 1's "Target status at chunk-close"); Code-evidence cell now cites `permissions/audit_composition.rs` (composer fn `compose_audit_class` + `compose_audit_class_with_source` + `AuditClassSource` enum) + `events/listeners.rs:303,433,552` (TemplateA/C/D fire listeners' `resolve_composed_audit_class` calls) + audit-event builders at `audit/events/m4/templates.rs:30` + `m5/templates.rs:29,77`; Covering-drift cell flipped to `D-new-19 ✓`. No new rows added.) -->
<!-- Last verified: 2026-05-04 by Claude Code (CH-12 chunk-seal: in the `permissions/05` block, "Session tags frozen at creation" row flipped from `silent-in-code` to `honored` (Code-evidence cell now cites CH-12 / ADR-0049 — Rule E in `validate_published_manifest` + `validate_tag_write_on_session` + `SESSION_FROZEN_TAG_PREFIXES` + `tool.frozen_tag_write_rejected` audit-event builder; Covering-drift D-new-08 ✓), and "Session tag vocabulary" row's Status stays at `partially-honored` per plan split-axis framing — immutability-axis honored by CH-12 (Code-evidence cell now cites the SESSION_FROZEN_TAG_PREFIXES const), emission-axis remains aspirational (only `#kind:session` + `session:<id>` are auto-emitted today; the 6 M6+ categories deferred to a future M6+ chunk per `D-CH12-FOLLOWUP-01`); Covering-drift D-new-08 ✓ added (immutability axis). In the `permissions/09` block, "Reserved namespace write rejection" row's Code-evidence cell extended to cite CH-12's composite-case extension via Rule E + target_kinds; Covering-drift D-new-08 ✓ added alongside D-new-31. No new rows added.) -->
<!-- Last verified: 2026-05-03 by Claude Code (CH-11 chunk-seal: existing Per-Session-consent row in the `permissions/06` block flipped from `partially-honored` to `honored`; CH-11 / ADR-0048 evidence + D-new-17 ✓ added in the Code-evidence + Covering-drift cells. No separate ADR-0048 row was added — the Per-Session claim was already enumerated.) -->

# M5.1/P2 — Concept-audit matrix

Full `{concept_doc, §anchor, claim, status, code_evidence, covering_drift}`
matrix produced by walking every one of the 20 concept docs at
[`docs/specs/v0/concepts/`](../../../concepts/) claim-by-claim against
current HEAD. **Concept docs are the source of truth.** Every row is one
testable claim extracted from a concept doc.

## Column meanings

- **Status:** `honored` | `contradicted` | `partially-honored` | `silent-in-code` | `concept-aspirational`
- **Code evidence:** where the claim is (or is not) reflected — file:line-range or "none"
- **Covering drift:** existing `DX.Y` drift id, new `D-new-NN` drift id, or `—` for honored rows
- **phi-core leverage:** `direct-reuse` | `wrap` | `inherit-from-snapshot` | `reject-build-native` | `N/A` | `leverage-violation` (new column per Guardrail #6)

## Discovery summary

- **Rows audited**: ~95 claims across 20 concept docs
- **Honored**: ~53 rows
- **Partially-honored / silent-in-code**: ~28 rows (many flagged as `concept-aspirational` for explicitly-deferred M6+ scope)
- **Contradicted**: ~14 rows — each generated a new drift or maps to an existing one
- **New drifts minted**: **31** (`D-new-01` through `D-new-31`)
- **Proposals discarded after verification** (claim was false): **2**
  - D-new-system-agent-creation-missing → [`orgs/create.rs:366+`](../../../../../../modules/crates/server/src/platform/orgs/create.rs) actually seeds both standard system agents at org creation
  - D-new-permission-ceiling-enforcement → [`permissions/engine.rs:235`](../../../../../../modules/crates/domain/src/permissions/engine.rs) has `step_2a_ceiling` implemented
- **Proposals absorbed into existing drifts**: **2** (stub-listener proposals → covered by D4.2 + D6.1)
- **Agent-proposal pairs merged into single drift**: **4** (multi-scope cascade, manifest validator, frozen-tag enforcement, bootstrap template)

## Matrix rows by concept doc

### `concepts/README.md`

| § | Claim | Status | Code evidence | Covering drift | phi-core leverage |
|---|---|---|---|---|---|
| Core Insight | Permissions are actions on resources with constraints | honored | `permissions/engine.rs` | — | N/A |
| Canonical 5-tuple | Grant has (subject, action, resource, constraints, provenance) | honored | `nodes.rs:597-612` Grant struct | — | N/A |
| Subject in edge | Subject derived from HOLDS_GRANT edge, not stored on Grant | honored | Grant carries `holder: PrincipalRef` + `auth_request_id` | — | N/A |
| Provenance | Chain traces to bootstrap | **honored** | CH-14 (ADR-0053 §D53.1/§D53.2/§D53.3/§D53.4) ships typed `domain::permissions::axioms::{SYSTEM_GENESIS_PRINCIPAL, system_genesis_principal, is_bootstrap_ar, system_bootstrap_template_id}` + `Repository::walk_provenance_chain(grant) -> Vec<AuthRequest>` on both backends with depth cap 32 + `Repository::revoke_grants_by_descends_from_recursive(ar, at)` BFS algorithm + `AuthRequest.descends_from_grant: Option<GrantId>` field-add (migration 0014) + Template-revoke handler flip from single-hop to recursive at `server/src/platform/templates/revoke.rs:90`; bootstrap claim flipped to use `system_genesis_principal()` at `bootstrap/claim.rs:192,204`; 5 acceptance tests at `acceptance_authority_chain.rs` + 8 walker unit tests + 5 axiom unit tests | **D-new-14 ✓** | N/A |

### `concepts/agent.md`

| § | Claim | Status | Code evidence | Covering drift | phi-core leverage |
|---|---|---|---|---|---|
| Roles | 6 AgentRole variants | honored | `nodes.rs:229-236` | — | N/A |
| Roles | `is_valid_for(kind)` cross-kind guard | honored | `nodes.rs:266-271` | — | N/A |
| Roles | Role immutable post-creation | honored | rust-level guard at `update.rs:133-141` rejects `new_role.is_some()` returning `ImmutableFieldChanged("role")`; HTTP wire `UpdateAgentProfileRequest` does not include `role` (silent-drop); pinned by acceptance test `update_rejects_role_change_with_immutable_field_changed` (CH-01 P4) | **D-new-22 (remediated 2026-04-27 via CH-01)** | N/A |
| Parallelized Sessions | `AgentProfile.parallelize: u32` | **honored** | post-CH-28 (ADR-0063 §D63.1 F1.c lock): `parallelize` MOVED OFF the `AgentProfile` literal entirely and lives on the per-agent `Blueprint` override row (`blueprint.parallelize: option<int>`); reached via `AGENT_USES_BLUEPRINT_OVERRIDE` edge; effective per-agent value resolves via `read_agent_profile_via_blueprint_or_fallback` (override row → template row → default). Pre-CH-28 storage at `nodes.rs:301` (`AgentProfile.parallelize`) is removed by migration 0019 + 0020. See `m6/architecture/agent-profile-cardinality.md` §3 + ADR-0063 §D63.1 | **M6+-OPEN-01 ✓** (closed via CH-28 / ADR-0063) | wrap |
| Soul (Template Blueprint + per-agent overrides) | N:1 cardinality + hybrid Blueprint table (NEW post-CH-28) | **honored** | CH-28 (ADR-0063 §D63.1 + §D63.2) flips `Agent | HAS_PROFILE | AgentProfile | 1:1` → `Agent | USES_PROFILE | AgentProfile | N:1`; per-agent override fields (`parallelize`, `model_config_id`, `mock_response`) MOVE OFF `AgentProfile` and live on the NEW `Blueprint` table per-agent override row; UNIQUE-WHERE-agent_id-NOT-NONE index distinguishes template-rows from override-rows; concept doc `agent.md` §Soul lines 160–178 reframed at CH-28 P-DOCS; `ontology.md` line 98 + `(74 total)` header bumped at CH-28 P-DOCS | **M6+-OPEN-01 ✓** | wrap |
| Participation | HAS_AGENT edge, Project → Agent | honored | `edges.rs:196-198` | — | N/A |
| Identity (Emergent) | 4-field Identity node (self_description/lived/witnessed/embedding) | honored | CH-16 ships full struct + 4 repo methods + migration 0009 + eager creation in `apply_agent_creation` (ADR-0038) | **D-new-01 HIGH** (remediated) | N/A |

### `concepts/coordination.md`

| § | Claim | Status | Code evidence | Covering drift | phi-core leverage |
|---|---|---|---|---|---|
| Storage backend | v0 uses SurrealDB (RocksDB-embedded; remote ≥ 2.0 supported); architecture is configurable via `Arc<dyn domain::Repository>`; 7-criterion conforming-backend contract ratified | honored | concept refreshed at `coordination.md` §"Storage backend"; `store/Cargo.toml` has `surrealdb` dep; 9 SurrealQL migrations 0001–0009; `Repository` trait at `domain/src/repository.rs` (~36 async methods) is the swap surface | **D-new-02 remediated 2026-04-28 via CH-03 / ADR-0042** | N/A |
| Event hybrid | State + event sourcing | partially-honored | AgentEvent stream exists; no unified replay query | — | wrap (AgentEvent) |
| Memory types | 4 types: user/feedback/project/reference | silent-in-code | Memory has `tags: Vec<String>` but no type enum | D-new-28 | N/A |

### `concepts/human-agent.md`

| § | Claim | Status | Code evidence | Covering drift | phi-core leverage |
|---|---|---|---|---|---|
| No Identity | Human Agents have no system-computed Identity | honored | CH-16 BOTH-guards: defensive at `Repository::upsert_identity` + preventive at `apply_agent_creation` (ADR-0039) | D-new-23 (remediated) | N/A |
| Channel props | channel_id/type/address/status/priority/metadata | partially-honored | `nodes.rs:754-768` has id/agent_id/kind/handle/created_at (missing address/status/priority/metadata) | D-new-24 | N/A |
| HAS_CHANNEL edge | Human → Channel | honored | `edges.rs:126-130` | — | N/A |

### `concepts/ontology.md`

| § | Claim | Status | Code evidence | Covering drift | phi-core leverage |
|---|---|---|---|---|---|
| 38 node types | Exactly 38 NodeKind variants post-CH-28 (was 37; CH-28 adds NEW `Blueprint` per ADR-0063 §D63.1 hybrid Blueprint table) | **honored** | post-CH-28: `nodes.rs:41-136` (carry-forward 37 variants) + NEW `Blueprint` struct + `BlueprintId` newtype sibling to `AgentProfile` per ADR-0063 §D63.1; total 38 | **M6+-OPEN-01 ✓** (closed via CH-28 / ADR-0063) | N/A |
| Identity ontology | 4-field shape per spec | honored | CH-16 ships full struct (ADR-0038 §D38.2) — field names match `agent.md` lines 326-327 verbatim | D-new-01 (remediated) | N/A |
| InboxObject/OutboxObject | Carry AgentMessage value objects | **accepted-as-is** | shipped structs at `nodes.rs:772-784` have `id`, `agent_id`, `created_at` only — no `messages: Vec<AgentMessage>` field; CH-19 (ADR-0057 §D57.8) ratifies the deferral to M6-DEFERRED-02 (inter-agent messaging chunk); concept-doc `ontology.md` carries 1-line deferred-state footnote at the InboxObject/OutboxObject row referencing CH-19 + M6-DEFERRED-02 | **D-new-25 ✓** | N/A |
| 74 edge types | Per code-asserted invariant `EDGE_KIND_NAMES.len() == 74` at `edges.rs` (post-CH-28) | **honored** | CH-19 (ADR-0057 §D57.7) reconciled docstring + concept-doc count to invariant 71; CH-25 bumped to 72 (`OWNS` edge per ADR-0060); CH-28 (ADR-0063 §D63.10) bumps to 74 (`AGENT_PROFILE_USES_BLUEPRINT` + `AGENT_USES_BLUEPRINT_OVERRIDE` added per F1.c hybrid Blueprint table); `ontology.md` §"Edge Types" header bumped 71 → 74 at CH-28 P-DOCS; canonical count locked to the test-asserted `EDGE_KIND_NAMES: [&str; 74]` invariant | **D-new-21 ✓ + M6+-OPEN-01 ✓** | N/A |
| Agent ↔ AgentProfile cardinality | `USES_PROFILE` edge with N:1 cardinality (template-sharing); per-agent overrides on NEW `Blueprint` row | **honored** | CH-28 (ADR-0063 §D63.2 F2.b lock): renamed `HAS_PROFILE` → `USES_PROFILE` across 43 sites (PRODUCTION + TEST) + serde rename-aware deserialization for wire-format back-compat (`#[serde(rename = "USES_PROFILE", alias = "HAS_PROFILE")]`); migration 0019 DROPs UNIQUE on `agent_profile.agent_id` + DEFINEs `uses_profile` RELATION; migration 0020 rewrites `has_profile` rows → `uses_profile`; `ontology.md` line 98 cardinality flipped 1:1 → N:1 at CH-28 P-DOCS | **M6+-OPEN-01 ✓** | N/A |
| Grant shape | holder/action/resource/descends_from/delegable | honored | `nodes.rs:597-612` | — | N/A |
| AuthRequestState 9 variants | Draft/Pending/InProgress/Approved/Denied/Partial/Expired/Revoked/Cancelled | honored | `nodes.rs:677-690` | — | N/A |

### `concepts/organization.md`

| § | Claim | Status | Code evidence | Covering drift | phi-core leverage |
|---|---|---|---|---|---|
| Org node fields | vision/mission/consent_policy/default_audit_class/authority_templates_enabled/defaults_snapshot/system_agents | honored | `nodes.rs:335-383` | — | wrap (defaults_snapshot) |
| Permission hierarchy | Org caps project caps agent (top-down ceiling) | honored | `engine.rs:235` step_2a_ceiling | — | N/A |

### `concepts/phi-core-mapping.md`

| § | Claim | Status | Code evidence | Covering drift | phi-core leverage |
|---|---|---|---|---|---|
| Session wrap | baby-phi Session wraps phi_core::Session | honored | `nodes.rs:841+ inner: PhiCoreSession`; CH-20 (ADR-0058 §D58.2) ratifies the wrap-pattern conventions across all wrap sites: nested-`inner` form (NOT `#[serde(flatten)]`) per `wrap-pattern.md` §"Nested-inner form" + `Arc<Mutex<phi_core::SessionRecorder>>` interior-mutability wrap for `BabyPhiSessionRecorder` per `wrap-pattern.md` §"Interior-mutability wrap"; cross-ref ADR-0029 §D29.1 + §D29.2 (per-record-type predecessors, generalised across all wrap sites at CH-20) | **D1.3 ✓ + D3.3 ✓** | wrap |
| LoopRecord wrap | baby-phi LoopRecordNode wraps phi_core::LoopRecord | honored | `nodes.rs:909+` | — | wrap |
| Turn wrap | baby-phi TurnNode wraps phi_core::Turn | honored | `nodes.rs:922+` | — | wrap |
| AgentProfile wrap | baby-phi AgentProfile holds phi_core blueprint | honored | `nodes.rs:304` | — | wrap |
| ModelConfig/ToolDefinition reuse | phi-core types wrapped at node tier | partially-honored | scaffolds at `nodes.rs:942+`; full wrap deferred | D1.3 (related) | wrap (planned) |
| agent_loop direct-reuse | baby-phi calls phi_core::agent_loop for execution | honored | runtime call site at `launch.rs::spawn_agent_task` (CH-02 P3); `tokio::join!(agent_fut, drain_fut)` drives event flow into `BabyPhiSessionRecorder`; provider via `MockProvider` per ADR-0032 | **D4.2 (remediated 2026-04-24 via CH-02)** | direct-reuse |
| Session list query at API surface (page-11 recent_sessions panel) | The page-11 `recent_sessions` panel queries persisted Session rows for the project via a dedicated `Repository::list_recent_sessions_for_project` method, ordered newest-first by `started_at`, bounded to LIMIT 10 at the query layer; returns the view-shape `RecentSessionEntry` directly | **honored** | CH-24 (ADR-0059 §D59.1 / §D59.2 / §D59.3) ships: `Repository::list_recent_sessions_for_project(project_id: ProjectId, limit: u32) -> RepositoryResult<Vec<RecentSessionEntry>>` trait method declared at `domain/src/repository.rs` + SurrealStore impl at `store/src/repo_impl.rs` (SurrealQL `SELECT * FROM session WHERE project_id = $project ORDER BY started_at DESC LIMIT $limit` — cap pushed into query for scale-resilience) + InMemoryRepository impl at `domain/src/in_memory.rs` (filter + sort_by `started_at` DESC + take); `RecentSessionEntry` 6-field baby-phi-defined view-shape struct at `domain/src/model/composites_m5.rs:225` (id: SessionId, project_id: ProjectId, agent_id: AgentId, started_at: DateTime<Utc>, ended_at: Option<DateTime<Utc>>, status: String — `started_by_display_name` deferred per ADR-0059 §D59.3-FOLLOWUP); production call-site at `server/src/platform/projects/detail.rs:225` calls `repo.list_recent_sessions_for_project(project_id, RECENT_SESSIONS_LIMIT)` with `RECENT_SESSIONS_LIMIT: u32 = 10` constant; old `RecentSessionStub` placeholder struct DELETED (0 source hits repo-wide); tripwire assertion at `acceptance_m5_sessions.rs:224-230` FLIPPED from `assert!(recent.is_empty())` to `assert_eq!(recent.len(), 1)`. Mid-cycle scope expansion approved at gate-2 user-lock; gate-2.5 forks F-D59.2.b + F-D59.3.b user-locked DIVERGENT from planner-recommendations (richer / more-defensive options chosen). | **D-CH24-recent-sessions-api-flip ✓** | direct-reuse (reads `phi_core::session::model::Session` via existing wrap at `nodes.rs`; new struct is baby-phi-defined, not a phi-core type — 0 net new `use phi_core` lines) |

### `concepts/project.md`

| § | Claim | Status | Code evidence | Covering drift | phi-core leverage |
|---|---|---|---|---|---|
| Project fields | name/description/goal/status/shape/token_budget/tokens_spent/objectives/key_results | honored | `nodes.rs:407-435` | — | N/A |
| OKRs | Objective + KeyResult as embedded value objects | honored | `Vec<Objective>`, `Vec<KeyResult>` | — | N/A |
| Status | 4 variants: Planned/InProgress/OnHold/Finished | honored | `nodes.rs:450-456` | — | N/A |
| Task | Task with full field set + 7-state flow | silent-in-code | id-only scaffold | D-new-26 | N/A |
| Shapes A/B/C/D, E forbidden | Multi-scope enforcement | honored | `in_memory.rs:1350` enforces Shape B 2-owner | — | N/A |

### `concepts/system-agents.md`

| § | Claim | Status | Code evidence | Covering drift | phi-core leverage |
|---|---|---|---|---|---|
| Two v0 system agents per org | memory-extraction + agent-catalog at org creation | **honored** | `orgs/create.rs:366-415` seeds both standard system agents at org creation; CH-19 (ADR-0057 §D57.3) additionally ratifies the **3-way union bucketing convention** at `system_agents/list.rs:55-70` (canonical slug OR membership in `Organization.system_agents` registry OR `AgentRole::System`) for distinguishing standard vs org-specific system agents — robust against M3-era fixture rows pre-dating M5 canonical slugs; concept-doc `system-agents.md` §"Properties Shared by All System Agents" tail carries refresh paragraph documenting the 3-way union | **D6.3 ✓** | wrap |
| Memory-extraction listener fires on session_end | Body reads transcript, writes memories | honored at v0 (heuristic body) | CH-21 shipped: `MemoryExtractionListener::on_event` body at [`listeners.rs`](../../../../../../modules/crates/domain/src/events/listeners.rs) mints 1 Memory per non-aborted SessionEnded, derives tags from session, decides `{private,public}` scope, upserts Identity (`witnessed.memories_extracted += 1`), emits `platform.memory.extracted` + `platform.identity.updated` audits, calls `record_system_agent_fire`. LLM-driven supervisor agent body deferred to **M6-DEFERRED-04** per ADR-0040 § Out-of-Scope (4-pool routing + grant enforcement land with the LLM body). | D4.2 (existing) + **D6.1 remediated 2026-04-28 via CH-21** | direct-reuse (deferred to M6) |
| Agent-catalog listener fires on 8 events | Body upserts catalog rows | honored | CH-22 shipped: `AgentCatalogListener::on_event` body at [`listeners.rs`](../../../../../../modules/crates/domain/src/events/listeners.rs) upserts the catalog row on every trigger variant; `SessionAborted` is a documented no-op. Six production emit sites wired (ADR-0035 D35.5): agents/create + agents/update + system_agents/add + disable + archive + orgs/create. Acceptance test `agent_create_populates_catalog_and_advances_runtime_status_tile` verifies the HTTP path lights up the row. | D4.2 (existing) | N/A |
| Runtime-status telemetry | queue_depth, last_fired_at populated | honored | CH-22 + CH-21 shipped both call sites: `AgentCatalogListener` calls `record_system_agent_fire` on every fire (catalog tile advances), and `MemoryExtractionListener` calls it on every non-aborted fire (memory-extractor tile advances). Both system-agent runtime-status tiles are populated. Tests `agent_catalog_listener_runtime_status_tile_advances_on_fire` (CH-22) + `memory_extraction_listener_advances_runtime_status_tile_for_extractor` (CH-21) + acceptance `scenario_3_disabled_extractor_skips_both_extraction_and_telemetry` (CH-21) pin the call sites. | **D6.1 remediated 2026-04-28 via CH-21** | N/A |
| Disable/archive durable | active:false, archived_at | honored | migration 0007 added `agent.active: bool DEFAULT true` + `agent.archived_at: option<string>`; repo methods `set_agent_active` + `set_agent_archived_at` flip them; system-agent `disable.rs` + `archive.rs` handlers wired (durable write BEFORE audit emit per ADR-0034 D34.4); acceptance tests verify both round-trip via `repo.get_agent` | **D6.5 (remediated 2026-04-27 via CH-01)** | N/A |

### `concepts/token-economy.md`

| § | Claim | Status | Code evidence | Covering drift | phi-core leverage |
|---|---|---|---|---|---|
| Worth formula | avg_rating × earned − consumed | **accepted-as-is** | Agent struct has no `total_tokens_earned`, `total_tokens_consumed`, or Worth-formula fields; CH-19 (ADR-0057 §D57.9) ratifies the deferral to M6-or-M7-DEFERRED (token-economy / contracts-bidding milestone); concept-doc `token-economy.md` carries 1-line deferred-state footnote at §"Worth" preamble referencing CH-19 + M6-or-M7-DEFERRED | **D-new-27 ✓** | N/A |
| Rating window size=20 | Rolling window fields | **accepted-as-is** | Agent struct has no `rating_window: Vec<f32>`, `rating_history_avg`, `rating_history_count` fields; CH-19 (ADR-0057 §D57.9) ratifies the deferral to M6-or-M7-DEFERRED | **D-new-27 ✓** | N/A |
| Intern → Contract carry-forward | Cumulative token tracking | **accepted-as-is** | Agent struct has no cumulative token-tracking fields; CH-19 (ADR-0057 §D57.9) ratifies the deferral to M6-or-M7-DEFERRED (depends on Worth-formula fields landing first) | **D-new-27 ✓** | N/A |

### `concepts/permissions/README.md`

| § | Claim | Status | Code evidence | Covering drift | phi-core leverage |
|---|---|---|---|---|---|
| 5-component grant | (subject, action, resource, constraints, provenance) | honored | `Grant` struct | — | N/A |
| Provenance traversal | Bootstrap axiom chain | **honored** | CH-14 (ADR-0053 §D53.1/§D53.2/§D53.3) ships typed `domain::permissions::axioms` module + `Repository::walk_provenance_chain` on both backends; bootstrap-claim AR satisfies `is_bootstrap_ar` two-witness predicate at chain root | **D-new-14 ✓** | N/A |

### `concepts/permissions/01-resource-ontology.md`

| § | Claim | Status | Code evidence | Covering drift | phi-core leverage |
|---|---|---|---|---|---|
| 9 fundamental classes | `Fundamental::ALL[9]` | honored | `fundamentals.rs:44-54` | — | N/A |
| 8 composite classes | `Composite::ALL[8]` | honored | `composites.rs:50-59` | — | N/A |
| Composite `#kind:` auto-tag | `kind_tag()` canonical form | honored | `composites.rs:81-92` | — | N/A |
| Instance identity tag `{kind}:{id}` | Auto-added at creation | honored | CH-06 wired emission at 10+ creation handlers via `auto_tags_for(kind, id)` (ADR-0037) | D-new-11 (remediated) | N/A |
| 3 ownership edges | OWNED_BY / CREATED / ALLOCATED_TO | honored | `edges.rs` | — | N/A |
| Catalogue as Step 0 precondition | catalogue.contains() gates Step 0 | honored | `engine.rs:130-144` | — | N/A |

### `concepts/permissions/02-auth-request.md`

| § | Claim | Status | Code evidence | Covering drift | phi-core leverage |
|---|---|---|---|---|---|
| 9 AuthRequest states | Draft..Cancelled | honored | `nodes.rs` AuthRequestState | — | N/A |
| Per-state ACL matrix | Requestor/approvers/owner/admins have distinct access per state | **honored** — typed function `check_auth_request_access` captures the matrix verbatim; **17 production callsites** consult it (4 mutation + 8 submit + 2 list + 1 slot-fill read + 2 NO-OP-system-internal skips); admin/auditor read-at-every-state column partially-honoured per `D-CH18-FOLLOWUP-01` deferral note | CH-18 (ADR-0056 §D56.1/§D56.2/§D56.3/§D56.5/§D56.6/§D56.7/§D56.8/§D56.9/§D56.10) ships typed pure-function `domain::auth_requests::access::check_auth_request_access(&AuthRequest, &PrincipalRef, IntendedOp) -> Result<(), AuthRequestAccessError>` at `domain/src/auth_requests/access.rs` with `IntendedOp` 11-variant closed enum (NOT in `Action::CANONICAL`; `Action::CANONICAL.len() == 34` invariant preserved) + `AuthRequestAccessError` 5-variant typed-error enum (`NotAuthorisedForRead`, `NotAuthorisedForModify`, `OperationForbiddenInState`, `RequestorOnlyOperation`, `UnfilledApproverSlotOnly`) + `classify_principal` four-classifier partition (`Requestor`, `SlotApprover`, `Bootstrap`, `Other`); 17 production wiring callsites at `server/src/platform/templates/{approve,deny,revoke}.rs` (4 mutation — emit `auth_request.access_denied` Alerted on Err) + `server/src/platform/projects/create.rs` (slot-fill mutation + slot-fill read) + `server/src/platform/orgs/{dashboard.rs:273,293, show.rs:63}` (list-side silent post-filter per F3.B.list-filter.a) + 8 submit-side defence-in-depth synthetic-Draft probe sites (`projects/create.rs:470, defaults/put.rs, secrets/add.rs, mcp_servers/{register,patch_tenants,archive}.rs, model_providers/{register,archive}.rs`); 7 kernel-internal fast-path skip sites documented at ADR-0056 §D56.8 (`events/listeners.rs:170` + `templates/revoke.rs:115,131` cascade + `bootstrap/claim.rs:307` + `templates/mod.rs:178` find_adoption_ar helper + `projects/resolvers.rs:55,122,161` Template A/C/D resolvers); new `auth_request.access_denied` Alerted-class audit-event builder at `domain/src/audit/events/m5_2/auth_request_access.rs`; `PrincipalRef` `PartialEq, Eq` derive (F1.B); Repository trait docstring contract on `get_auth_request` + `update_auth_request` per §D56.7 | **D-new-12 ✓** | N/A |
| Slot independence | Partial approvals → scoped partial grants | honored | `auth_requests/state.rs:32-50` | — | N/A |
| Per-resource slots | `resource_slots: Vec<ResourceSlot>` | honored | `nodes.rs` | — | N/A |
| allocate vs transfer cardinality | allocate additive, transfer exclusive | **honored** | CH-08 (ADR-0052 §D52.1 + §D52.5 + §D52.6) ships `Repository::apply_transfer_grant` compound-tx primitive at `repository.rs:1397` (atomic three-write tx: rewrite OWNED_BY edge + revoke sender's grant + mint recipient's grant per concept-doc 02 line 206) with adapter impls at `in_memory.rs:1868` (Mutex-guarded) and `store/src/repo_impl.rs:2691` (BEGIN/COMMIT block); Allocate path remains additive via existing `create_grant` boundary (sender's grant untouched); 6 atomicity tests + 3 SurrealStore integration tests verify the compound-tx semantics. Forward-defensive primitive — no production caller wired today (`Action::Transfer` has zero runtime mint sites; first M6+ transfer-flow chunk consumes). | **D-new-13 ✓** | N/A |
| System Bootstrap Template + system:genesis | Hardcoded axioms | **honored** | CH-14 (ADR-0053 §D53.1/§D53.2/§D53.6) ships typed `SYSTEM_GENESIS_PRINCIPAL` const + `system_genesis_principal()` helper-fn + `is_bootstrap_ar(ar)` two-witness predicate at `domain::permissions::axioms`; bootstrap claim at `bootstrap/claim.rs:192,204` flipped to use the helper-fn; claim-time-as-system-init divergence rationale documented in ADR-0053 §D53.6 | **D-new-14 ✓** | N/A |
| 2-tier retention (active 90d + archive) | `active_until` + `is_archive_eligible` + retrieval gating | partially-honored | math in `retention.rs`; no archival action / retrieval gate | D-new-15 | N/A |

### `concepts/permissions/03-action-vocabulary.md`

| § | Claim | Status | Code evidence | Covering drift | phi-core leverage |
|---|---|---|---|---|---|
| Standard action vocabulary | 34 named actions (concept-doc recount; was "33" in original audit row) | **honored** | `domain::permissions::action::Action` enum with 35 variants (34 canonical + Wildcard); `ALL[35]` / `CANONICAL[34]` iteration constants; `as_str` / `TryFrom<&str>` / `Display` / `FromStr`; carriers `Grant.action` + `Manifest.actions` + `ToolAuthorityManifest.actions` are `Vec<Action>` | **D-new-09 (remediated 2026-04-29 via CH-04)** | N/A |
| Action × Fundamental matrix | Only compatible actions per fundamental | **honored** | `Action::applies_to(Fundamental) -> bool` encodes the 9×10 matrix from `concepts/permissions/03-action-vocabulary.md` lines 27–37; `Action::applies_to_composite(Composite)` derives via constituents() per concept-doc line 39; exhaustive 306-cell test transcribes the doc verbatim | **D-new-10 (remediated 2026-04-29 via CH-04)** | N/A |
| Observability action verbs (observe / log / attest) universal across fundamentals — first runtime exercise on `session_object` | Concept-doc 03 line 22 (Observability row: `observe, log, attest`) + line 44 (universal applicability: *"Discovery, Authority, and Observability apply universally"*) | **honored** | `Action::Observe` is canonical pre-CH-17 (introduced at CH-04 / ADR-0043 when the 34-verb enum landed): variant declaration at `domain/src/permissions/action.rs:73`; `Action::CANONICAL` array entry at `:282`; `as_str() => "observe"` mapping at `:322`. CH-17 (ADR-0055 §D55.5/§D55.9) lands the **first runtime exercise** of Observe on `session_object`: sibling permission-builder `domain::permissions::builders::build_session_observe_manifest` at `domain/src/permissions/builders/session_observe.rs:70` returning `Manifest { actions: vec![Action::Observe], resource: vec!["session_object".to_string()], … }`. SSE handler `server::handlers::sessions::events` calls `open_live_stream` which builds the manifest + runs Steps 0–5 of the engine (`Action::Observe.applies_to_composite(Composite::SessionObject) == true` via DataObject + Tag constituents). `Action::CANONICAL.len() == 34` invariant preserved (Observe was already canonical; CH-17 does not add a variant). Template A's `fire_grant_on_lead_assignment` extended to mint `[Read, Inspect, List, Observe]` so legitimate launchers cover the SSE manifest at Step 3 (migration `0016_template_a_session_object_grant_add_observe.surql` backfills legacy grants). | **D7.1 ✓** | direct-reuse |
| `allocate` umbrella | covers delegate/approve/escalate/revoke etc. | **honored** | CH-08 (ADR-0052 §D52.2 + §D52.3) ships typed `AllocateRefinement { no_further_delegation: bool, max_depth: Option<u8> }` at `domain::permissions::AllocateRefinement` (`permissions/allocate_refinement.rs`) per concept-doc 02 line 197 + concept-doc 03 line 54 (refinement-as-constraint framing); `Grant.allocate_refinement: Option<AllocateRefinement>` field at `model/nodes.rs:701` with `#[serde(default)]` shielding (mirrors CH-11 D48.1 + CH-13 D50.5 field-add precedents); 3 unit tests cover round-trip + legacy-grant decode-as-None. Engine-side enforcement is forward-defensive — typed-field structural-boundary closes the typing-half of D-new-29; first M6+ chunk wiring engine-enforcement consumes the typed field. | **D-new-29 ✓** | N/A |
| Over-declaration principle | Manifest = max reach; intersection with grants | honored | `manifest.rs` + `engine.rs` Step 1 | — | N/A |

### `concepts/permissions/04-manifest-and-resolution.md`

| § | Claim | Status | Code evidence | Covering drift | phi-core leverage |
|---|---|---|---|---|---|
| 8-step algorithm (0+1+2+2a+3+4+5+6) | Matches pseudocode | honored | `engine.rs` step_N functions | — | N/A |
| Step 0 hard-gates | Catalogue miss → Denied | honored | `engine.rs:130-144` | — | N/A |
| Step 3 hard-match | Missing reach match → Denied | honored | `engine.rs:88-92` | — | N/A |
| Step 6 consent gating | Missing consent → Pending | honored | `engine.rs:106` | — | N/A |
| Decision outcomes | Allowed/Denied/Pending | honored | `decision.rs:99-130` | — | N/A |
| **All steps hard-gate** (not advisory) | Any Denied → refuse | **honored** | CH-15 (ADR-0054 §D54.1+§D54.5+§D54.6) ships: typed builder `domain::permissions::build_session_launch_manifest` at `permissions/builders/session_launch.rs` + hard-deny `match preview.decision` at `server/src/platform/sessions/launch.rs:226` returning `SessionError::PermissionCheckFailed { step, reason }` for every Decision::Denied (steps 0..6); Step 6 Consent path remains routed through `gate_session_launch_consent` per CH-11 + ADR-0048 (already hard-deny). New `platform.session.launch_denied` audit event (Alerted) at `domain/src/audit/events/m5_2/session_launch.rs` emitted on every step-1-to-6 deny BEFORE returning Err. Acceptance tests `acceptance_sessions_m5p4::launch_denies_with_403_when_agent_holds_no_session_grants` (Step 2) + `launch_succeeds_with_template_a_session_grant_after_p1_extension` (happy path) + `launch_emits_launch_denied_audit_event_on_403` + `launch_does_not_register_session_on_403` + `preview_decision_matches_launch_decision_on_grants_stable` cover the cardinality + invariants. | **D4.1 ✓** | N/A |
| Publish-time manifest validator | Rejects missing fundamentals, reserved-namespace writes | **honored** | `validate_published_manifest` at `domain::permissions::manifest::validator` shipped with 4 hard rules + 3 warnings; Repository-level guard wired at both `SurrealStore::create_tool_authority_manifest` + `InMemoryRepository::create_tool_authority_manifest` via `RepositoryError::ManifestValidation { source }` variant; 38 tests cover all rejection classes including 81-cell Constraint × Fundamental matrix transcribed verbatim | **D-new-07 (remediated 2026-04-29 via CH-05)** | N/A |
| 5-tier scope cascade | Project → Org → base_project → base_org → intersection | **honored** | CH-07 (ADR-0051) ships full 2-tier cascade + intersection fallback at `engine.rs::step_5_scope_resolution` (`cascade_multi_scope` + `cascade_single_scope` + `cascade_intersection_fallback`); base_project / base_org tie-breakers use lexicographic-min placeholder per ADR-0051 §D51.2/§D51.3 (M6+ deferral via `D-CH07-FOLLOWUP-01`); 10 unit tests + 6 acceptance tests cover the 5 distinct outcomes (`tests/multi_scope_cascade_acceptance.rs`) | **D-new-06 ✓** | N/A |
| Provenance chain to bootstrap | Traversal via `descends_from` | **honored** | CH-14 (ADR-0053 §D53.3/§D53.5) ships `Repository::walk_provenance_chain(grant) -> Vec<AuthRequest>` on both backends + `AuthRequest.descends_from_grant: Option<GrantId>` field-add with `#[serde(default)]` + migration 0014 (`descends_from_grant option<string>` ON auth_request); walker terminates via `is_bootstrap_ar` two-witness predicate or depth cap 32 (`RepositoryError::ProvenanceCycleDepthExceeded`); 8 walker unit tests + 3 acceptance tests for chain-walk paths | **D-new-14 ✓** | N/A |
| Composite expansion invariant | Memory vs Session disambiguated by `#kind:` | honored | `expansion.rs:140-146` | — | N/A |

### `concepts/permissions/05-memory-sessions.md`

| § | Claim | Status | Code evidence | Covering drift | phi-core leverage |
|---|---|---|---|---|---|
| Memory selector predicates (tags) | Full tag-predicate DSL | honored | CH-06 ships 6 predicates + 3 combinators via pest-PEG (ADR-0036) | **D-new-03** (remediated) | N/A |
| Memory tag vocab agent/project/org/#public | Tags field on Memory | honored | `nodes.rs:791 tags: Vec<String>` | — | N/A |
| store/recall/delete actions | Memory operations | silent-in-code | no recall tool / store action | D-new-16 | N/A |
| Default memory-recall grant | System-provenance grant on each agent | concept-aspirational | Memory contract deferred to M6 (C-M6-1) | — | N/A |
| Supervisor extraction 2 grants | Reads subordinate sessions + stores extractions | silent-in-code | listener stub | D4.2 + D6.1 (existing) | N/A |
| Session tags frozen at creation (except lifecycle) | No grant mints [modify] on structural tags | **honored** | CH-12 (ADR-0049) ships: publish-time Rule E in `validate_published_manifest` rejects `[Modify]` on composites whose `target_kinds` overlaps `reserved_namespace_prefixes()`; runtime `validate_tag_write_on_session` + `SESSION_FROZEN_TAG_PREFIXES` + `FrozenTagViolation` at `domain::permissions::manifest::validator`; `tool.frozen_tag_write_rejected` (Alerted) audit-event builder at `domain::audit::events::m5_2::tool_authority` per F5.B; Repository trait docstring documents the validator + audit-event pairing as a precondition for any future tag-write method | **D-new-08 ✓** | N/A |
| Session tag vocabulary | agent/project/org/task/delegated_from/role_at_creation/agent_kind/#archived/#active | **partially-honored** | governance fields plus the full session-tag prefix list at `domain::permissions::manifest::validator::SESSION_FROZEN_TAG_PREFIXES` (10 prefixes: `#kind:`, `session:`, `agent:`, `project:`, `org:`, `task:`, `delegated_from:`, `role_at_creation:`, `agent_kind:`, `derived_from:`); CH-12 closes the **immutability-axis** by enforcing rejection of `[Modify]` on every prefix; the **emission-axis** remains aspirational — only `#kind:session` + `session:<id>` are auto-emitted today via `Composite::auto_tags_for("session", id)`; the 6 M6+ categories ship as forward-defensive entries in the const, with full emission deferred to a future M6+ "Session structural-tag emission" chunk (tracked as recommended LOW drift `D-CH12-FOLLOWUP-01` per plan §10) | **D-new-08 ✓** (immutability axis) | wrap |
| Templates A/B/C/D auto-fire grants | Shipped as pure fns + listeners | honored | `templates/a,b,c,d.rs` + listeners; CH-17 (ADR-0055 §D55.9) extends Template A's `fire_grant_on_lead_assignment` mint from `[Read, Inspect, List]` to `[Read, Inspect, List, Observe]` for both `project_grant` (templates/a.rs:139–143) and `session_grant` (templates/a.rs:175–179); migration `0016_template_a_session_object_grant_add_observe.surql` walks legacy Template A grants (provenance: `descends_from(AR with kinds CONTAINS '#template:a')`; live: `revoked_at = NONE`) and appends `"observe"` to the action array idempotently. | **D7.1 ✓** | N/A |
| Session lifecycle — live events (operator observes via SSE) | `GET /sessions/:id/events` streams `phi_core::AgentEvent`s gated by `Action::Observe` on `session_object` | **honored** | CH-17 (ADR-0055 §D55.1/§D55.3/§D55.4/§D55.5/§D55.6/§D55.8) ships: trait-shaped `SessionLiveStreamRegistry` at `server/src/state.rs:134–208` with default `InProcessSessionLiveStreamRegistry` (`DashMap<SessionId, broadcast::Sender<AgentEvent>>`); `BabyPhiSessionRecorder.broadcast_tx: Option<broadcast::Sender<AgentEvent>>` tap at `domain/src/session_recorder.rs:148`; SSE handler `server::handlers::sessions::events` at `handlers/sessions.rs:215` returning `Sse::new(stream).keep_alive(KeepAlive::new().interval(Duration::from_secs(30)).text(": keep-alive"))`; `BroadcastStreamRecvError::Lagged` → typed `event: lagged` SSE error event then close; sibling permission-builder `domain::permissions::builders::build_session_observe_manifest` at `domain/src/permissions/builders/session_observe.rs:70` returning `[Observe]` on `session_object`; platform-layer `open_live_stream` at `server/src/platform/sessions/events.rs:109` runs Steps 0–5 of the engine + emits `platform.session.live_stream_denied` (Alerted-class) at `domain/src/audit/events/m5_2/session_live_stream.rs:47` BEFORE returning `SessionError::PermissionCheckFailed { step, reason }` (no consent gate; consent is launch-time per ADR-0048 §D48.5). `+1` direct-reuse `phi_core::types::event::AgentEvent` import in `events.rs` (per Rule 1 of `baby-phi/CLAUDE.md`'s phi-core leverage discipline). | **D7.1 ✓** | direct-reuse |
| Worked examples (4 scenarios) | Engine computes Allowed/Denied | honored | `engine.rs` full pipeline | — | N/A |
| Shape E forbidden | Enforced at project creation | honored | `in_memory.rs:1350` | — | N/A |

### `concepts/permissions/06-multi-scope-consent.md`

| § | Claim | Status | Code evidence | Covering drift | phi-core leverage |
|---|---|---|---|---|---|
| Shapes A/B/C/D valid; E forbidden | Hard schema constraint | honored | ProjectShape enum | — | N/A |
| Unified resolve_scope cascade | project → org with tie-breaker | **honored** | CH-07 (ADR-0051) ships the `resolve_scope` 2-tier branching at `engine.rs::cascade_multi_scope` (project-tier match-count branch → org-tier match-count branch → intersection fallback) reading session scopes from `ctx.session_org_tags` / `ctx.session_project_tags` per ADR-0051 §D51.4; intersection-fallback ceiling re-clamp + `DeniedReason::IntersectionEmpty` per ADR-0051 §D51.5 + §D51.7 | **D-new-06 ✓** | N/A |
| 3 consent policies | Implicit / One-Time / Per-Session | honored | `ConsentPolicy` enum | — | N/A |
| Implicit consent: auto-issue on edge | Template listeners fire | honored | listeners wired | — | N/A |
| One-Time consent: Consent node lifecycle | Requested → Acknowledged | honored | 6-state machine + per-transition repo + sweeper shipped at CH-10 (ADR-0047) | **D-new-05** ✓ | N/A |
| Per-Session consent: subordinate_required | Grant flag + real-time approval flow | honored | CH-11 shipped: `Grant.approval_mode: ApprovalMode` + engine `step_6_consent_gating` real body branches on `approval_mode × ConsentPolicy × ConsentState` per ADR-0048 D48.5/D48.6; per-policy minters + `Repository::request_consent` ship at `domain::consents::minters` + `repository.rs`; `consent.requested` audit event + `(subordinate, org, Option<session_id>)` ConsentIndex; Channel notification remains M6+ per concept-doc line 416 | **D-new-17** ✓ | N/A |
| Consent node full field list | consent_id/agent_id/scope/state/requested_at/responded_at/revocable/provenance | honored | 11-field shape shipped at CH-09 (ADR-0045) | **D-new-04** ✓ | N/A |
| Contractor model | base_org ceiling does not reach cross-scope | **honored** | CH-07 (ADR-0051 §D51.6) ships the membership-bounded ceiling clamp at `engine.rs::step_2a_ceiling` — non-empty `session_org_tags` filters Organization-tier ceilings to those whose `holder == PrincipalRef::Organization(o) ∧ session_org_tags.contains(&o)`; concept-doc 06 line 162 verbatim invariant enforced; Project / Agent ceilings pass through unchanged; 4 unit tests + Scenario 7 acceptance test (`tests/multi_scope_cascade_acceptance.rs`) | **D-new-20 ✓** | N/A |

### `concepts/permissions/07-templates-and-tools.md`

| § | Claim | Status | Code evidence | Covering drift | phi-core leverage |
|---|---|---|---|---|---|
| Templates A–E via adoption AR | Build adoption + listener fires | **honored** | templates + listeners; CH-19 (ADR-0057 §D57.2) additionally ratifies the **multi-AR-per-(org, kind) history semantics**: an org may adopt → revoke → re-adopt over time, and `find_adoption_ar` at `templates/mod.rs:170-186` returns the most-recent AR (sorted by `submitted_at` desc) — matching concept-doc 02 §"Interaction with Authority Templates" "current adoption state is the most-recent AR's state" + concept-doc 07 §"Templates Are Pre-Authorized Allocations" framing; concept-doc 07 tail carries cross-ref note clarifying adoption-AR pattern is the v0 implementation of the YAML-config-object framing | **D5.3 ✓** | N/A |
| Template A [read, inspect, list] on project | Issued on HAS_LEAD | **honored** | CH-15 (ADR-0054 §D54.3+§D54.4) extends `templates/a.rs::fire_grant_on_lead_assignment` return type from `Grant` to `Vec<Grant>` (CascadeResult-style typed-multi-value precedent); each `HasLeadEdgeCreated` event mints two grants (project-resource grant `[Read, Inspect, List]` on `project:<uuid>` (preserved verbatim) + paired session-object grant `[Read, Inspect, List]` on `tags contains "project:<uuid>" AND tags contains #kind:session` selector form). Both grants `descends_from` the same Template A adoption AR. Migration `0015_template_a_session_object_grant.surql` walks every active legacy Template A grant and inserts the paired session-object grant under shared provenance (idempotent on re-run via `descends_from NOT IN (...)` belt-and-braces guard + migration-runner ledger). The TemplateAFireListener body iterates the Vec, persists each grant + emits one `template.a.grant_fired` audit event per persisted grant. Concept doc 07 §"Template A — Project Lead Authority" verbatim: "the lead has [read, inspect, list] on every session tagged `project:P`" — materialised on-the-wire post-CH-15. | **D4.1 ✓** | N/A |
| audit_class composition: strictest wins | Org / template AR / override composition | honored | composer at `permissions/audit_composition.rs` (`compose_audit_class` + `compose_audit_class_with_source` + `AuditClassSource` enum); `Grant.audit_class` denormalisation at `model/nodes.rs:693`; 3 fire listeners wire `resolve_composed_audit_class` at `events/listeners.rs:303,433,552`; audit-event builders at `audit/events/m4/templates.rs:30` (template_a) + `m5/templates.rs:29,77` (template_c/d) accept composed class + emit `audit_class_source` snake-case string in diff per ADR-0050 §D50.6 | **D-new-19 ✓** | N/A |
| Standard Org Template config | tools_allowlist/resource_catalogue/etc. as embedded config | **honored** | CH-19 (ADR-0057 §D57.10) ratifies the shipped pattern: org-template adoption is expressed via adoption ARs (`#template:org-standard`) + listener-fired Grants (M3/M5 pattern) — functionally equivalent to YAML config-object framing; concept-doc `permissions/07-templates-and-tools.md` §"Standard Organization Template" preamble carries refresh paragraph documenting "v0.1 ships the YAML-config semantics through adoption-AR + listener-fired-Grant pattern; the YAML config above is the **conceptual contract**; the AR is the v0.1 **implementation surface**" | **D-new-30 ✓** | N/A |
| Standard Project Template config | filesystem/session/memory grants | **honored** | CH-19 (ADR-0057 §D57.10) ratifies the shipped pattern: project-template adoption uses Templates A/B/C/D adoption ARs + listener-fired Grants per concept-doc 07; concept-doc `permissions/07-templates-and-tools.md` §"Standard Project Template" preamble carries the same YAML-as-conceptual-contract / AR-as-implementation-surface refresh paragraph | **D-new-30 ✓** | N/A |
| 14 Tool Authority Manifest examples | Declared shape per tool | partially-honored | ToolAuthorityManifest fields partial | D4.3 + D-new-07 | N/A |
| Manifest validation at publish-time | Reject invalid declarations | **honored** | Same as the §04 row above — validator + repo guard land at CH-05 | **D-new-07 (remediated 2026-04-29 via CH-05)** | N/A |

### `concepts/permissions/08-worked-example.md`

| § | Claim | Status | Code evidence | Covering drift | phi-core leverage |
|---|---|---|---|---|---|
| Shape A/B/C resolution per worked examples | Engine computes expected Decisions | **honored** | CH-07 (ADR-0051) — engine cascade body covers Shape A (`cascade_single_scope` back-compat) + Shape B/C (`cascade_multi_scope` 2-tier + intersection fallback); worked-example Scenarios 4 / 5 / 6 covered by `tests/multi_scope_cascade_acceptance.rs::scenario_{4,5,6}_*`; Shape D system-session covered by `shape_d_system_session_org_tier_wins`; Shape A regression covered by `shape_a_baseline_empty_session_tags_project_tier_wins` | **D-new-06 ✓** | N/A |
| Contractor scenario | base_org bounded | **honored** | CH-07 (ADR-0051 §D51.6) — concept-08 §"Step 7" Scenario 7 (`contractor-x-9` with base_org=Gamma reading `acme-website-redesign`, session tagged `[org:acme]` only) covered by `tests/multi_scope_cascade_acceptance.rs::scenario_7_contractor_reads_acme_session_project_tier_wins`; Gamma ceiling structurally excluded by the membership filter in `step_2a_ceiling`; project-tier resolution wins as concept-doc specifies | **D-new-20 ✓** | N/A |
| Shape E forbidden recovery | Rejected at creation | honored | `in_memory.rs:1350` | — | N/A |
| Ad-hoc AR + revocation cascade | Revoke walks grants by provenance + per-cascaded-AR emission | **honored** | CH-14 (ADR-0053 §D53.4 + §D53.7) ships `Repository::revoke_grants_by_descends_from_recursive(ar, at) -> domain::repository::CascadeResult { revoked_grants: Vec<GrantId>, cascaded_ars: Vec<AuthRequestId> }` BFS algorithm with depth cap 32 on both backends; Template-revoke handler at `server/src/platform/templates/revoke.rs:99` flipped to recursive variant + iterates `cascaded_ars` to transition each level-≥1 AR (Approved → Revoked) and emit one `auth_request.revoked` audit event per cascaded AR via the injected `AuditEmitter`; existing single-hop `revoke_grants_by_descends_from` preserved verbatim for M2 `narrow_mcp_tenants` per ADR-0033 contract; 7 acceptance tests cover 3-hop cascade + idempotency + depth-cap + recursive-vs-single-hop diff + per-AR audit-event emission count + cascaded-AR state-transition (`tests/acceptance_authority_chain.rs`) | **D-new-18 ✓** + **D-CH14-FOLLOWUP-02 ✓** | N/A |

### `concepts/permissions/09-selector-grammar.md`

| § | Claim | Status | Code evidence | Covering drift | phi-core leverage |
|---|---|---|---|---|---|
| PEG grammar (atoms + predicates + composition) | Full recursive-descent parser | honored | CH-06 ships pest-PEG with 13 productions matching concept-09 lines 58-102 | **D-new-03** (remediated) | N/A |
| `tags contains/intersects/any_match/subset_of` | Tag-predicate primitives | contradicted | only Exact/Prefix/KindTag | **D-new-03** | N/A |
| AND/OR/NOT logical composition | Combinators | honored | CH-06 ships `BoolExpr::Or/And/Not` with NOT > AND > OR precedence + parens; 6 unit tests pin precedence | **D-new-03** (remediated) | N/A |
| Reserved namespace write rejection | Publish-time validator denies | **honored** | Rule C of `validate_published_manifest` rejects `[Modify]` on bare `tag` resource (CH-05 — cleanest publish-time discriminator); full reserved-namespace prefix list (`#kind:`, `delegated_from:`, `derived_from:`, plus auto-generated `{kind}:` per `Composite::ALL`) ships via `reserved_namespace_prefixes()` for downstream consumers; **CH-12 (ADR-0049) extends the rule set to a fifth Rule E** that closes the composite-case gap by rejecting `[Modify]` on a composite whose `target_kinds` overlaps the reserved-namespace prefix list — Rule C still fires for `[Modify] × bare tag`; Rule E covers `[Modify] × composite × target_kinds`. CH-05 + CH-12 together close both publish-time discriminators. | **D-new-31 (remediated 2026-04-29 via CH-05)**, **D-new-08 ✓ (composite case via CH-12)** | N/A |

---

## Coverage statement

All 20 concept docs walked. All major § headings produced at least
1 matrix row (most produced 3–6). Denominator for
documentation-vs-concept-docs confidence: **20/20 docs audited = 100%**.

Every row with `contradicted` / `partially-honored` / `silent-in-code`
status is either:
- Already covered by an existing drift (D1.1–D7.6), OR
- Generating a new drift (D-new-01 through D-new-31), OR
- Flagged `concept-aspirational` with explicit deferral note (not a drift).

No unclassified rows. Discovery phase satisfied the close invariant that
every concept claim has an explicit classification + covering pointer.
