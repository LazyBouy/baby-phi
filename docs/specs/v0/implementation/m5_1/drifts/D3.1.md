<!-- Last verified: 2026-05-10 by Claude Code (CH-20 P2: Status flipped `discovered` → `accepted-as-is`; ratified via CH-20 / ADR-0058 §D58.4 + convention-doc `v0/conventions/event-bus-wiring.md` §"Serde-tag-collision rename"; cycle hex `240616a4`.) -->
<!-- Last verified: 2026-04-24 by Claude Code -->

# D3.1 — `DomainEvent::AgentCreated` field renamed `kind` → `agent_kind` (serde discriminator collision)

## Identification
- **ID**: D3.1
- **Phase of origin**: M5/P3
- **Discovery source**: `plan-archive-ledger`
- **Date discovered**: 2026-04-23 (M5/P3 close)
- **Status**: `accepted-as-is`
- **Bucket**: C — convention/pattern decision
- **Severity**: LOW
- **Tags**: `serde`, `event-bus`
- **Blocks**: none (any emit site constructing the variant)
- **Blocked-by**: none

## Concept alignment
- **Concept doc(s)**: [`concepts/agent.md`](../../../concepts/agent.md) §"agent kind" (Human vs LLM distinction) + [`concepts/coordination.md`](../../../concepts/coordination.md) §"event-driven reactivity" (event bus concept)
- **Concept claim (verbatim or close paraphrase)**: Domain events carry agent-lifecycle facts. The event's field naming is below concept granularity (concept describes the kind-distinction, not the JSON key name).
- **Contradiction**: None. `agent_kind` JSON key still carries the concept-level kind distinction.
- **Classification**: `concept-silent-plan-filled-gap`
- **phi-core leverage status**: `N/A — no phi-core overlap` (`DomainEvent` is baby-phi governance-tier, not phi-core `AgentEvent`)

## Plan vs. reality
- **Plan said** (plan-archive line 1201, §Part 4 P3 Deliverables): `AgentCreated { agent_id, owning_org, kind, role, at, audit_event_id }` — field name `kind: AgentKind`.
- **Reality (shipped state at current HEAD)**: `DomainEvent` carries `#[serde(tag = "kind", rename_all = "snake_case")]` at the enum level. Serde's `tag = "kind"` reserves the JSON key `kind` for the variant discriminator; a variant field *also* named `kind` produces the compile-time error "variant field name 'kind' conflicts with internal tag". Shipped as: `AgentCreated { agent_id, owning_org, agent_kind: AgentKind, role: Option<AgentRole>, at, event_id }` ([`events/mod.rs:162-165`](../../../../../../modules/crates/domain/src/events/mod.rs#L162-L165)).
- **Root cause**: `reality-feedback-contradicted-estimate` (serde tag-discriminator collision not anticipated at plan-time).

## Where visible in code
- **File(s)**: [`modules/crates/domain/src/events/mod.rs`](../../../../../../modules/crates/domain/src/events/mod.rs) lines 162–165 (variant definition), 419 (pattern-match site), 159 (variant doc-comment explaining the rename)
- **Test evidence**: `events::mod::tests::sample_agent_created`, `events::mod::tests::event_id_accessor_matches_emitted_value_for_every_variant` — all pass with `agent_kind:` field name.
- **Grep for regression**: `grep -n "AgentCreated {.*\bkind:" modules/crates/` — expect 0 hits (nobody constructs with plain `kind:`); `grep -n "agent_kind:" modules/crates/domain/src/events/mod.rs` — expect ≥1.

## Remediation scope (estimate only)
- **Approach (sketch)**: Confirm-in-place. Wire key `agent_kind` is stable; external consumers deserialising `DomainEvent` JSON must expect that field.
- **Implementation chunk this belongs to**: CH-20
- **Dependencies on other drifts**: none
- **Estimated effort**: 0 engineer-days.
- **Risk to concept alignment if deferred further**: none.

## Prior documentation locations (pre-M5.1)
- Plan archive lines: 1199–1206
- Code comments: [`events/mod.rs`](../../../../../../modules/crates/domain/src/events/mod.rs) variant doc-comment (lines 159+)
- ADR references: none
- Other doc pointers: [`m5/architecture/event-bus-m5-extensions.md`](../../m5/architecture/event-bus-m5-extensions.md) §"Field-naming note"

## Lifecycle history
- 2026-04-23 — `discovered` — logged in M5/P3 close drift addendum
- 2026-04-24 — `classified` — migrated to M5.1 drift catalogue
- 2026-05-10 — `accepted-as-is` — ratified via CH-20 (cycle hex `240616a4`) / ADR-0058 §D58.4 + convention-doc `v0/conventions/event-bus-wiring.md` §"Serde-tag-collision rename"; review trigger: no near-term.
