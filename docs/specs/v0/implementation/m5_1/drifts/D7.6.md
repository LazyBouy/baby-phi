<!-- Last verified: 2026-04-24 by Claude Code -->

# D7.6 — Web pages use inline `async function run()` server actions (not separate `"use server"` module declarations)

## Identification
- **ID**: D7.6
- **Phase of origin**: M5/P7
- **Discovery source**: `plan-archive-ledger`
- **Date discovered**: 2026-04-24 (M5/P7 close)
- **Status**: `discovered`
- **Bucket**: C — convention/pattern decision
- **Severity**: LOW
- **Tags**: `nextjs-pattern`, `server-actions`, `convention-establishment`
- **Blocks**: none (future per-row-action pages follow this pattern)
- **Blocked-by**: none

## Concept alignment
- **Concept doc(s)**: Concept docs are silent on frontend framework idioms (below concept granularity)
- **Concept claim (verbatim or close paraphrase)**: No direct concept claim on Next.js server-action shape.
- **Contradiction**: None.
- **Classification**: `concept-silent-plan-filled-gap`
- **phi-core leverage status**: `N/A — no phi-core overlap` (web UI layer)

## Plan vs. reality
- **Plan said** (plan-archive line 1427, §Part 6 Documentation plan): implicit convention from M2/M3/M4 web pages — Server Actions live in sibling `actions.ts` file with `"use server"` at top.
- **Reality (shipped state at current HEAD)**: Hybrid pattern. Top-level `actions.ts` still exports callable actions (`listTemplatesAction`, etc.) and handles revalidation + error mapping. Inline per-row `<form action={run}>` closures marked `"use server"` at function body (3+ `"use server"` hits in `templates/page.tsx` at lines 142, 159, 182). Closure captures are strings only (orgId, kind) — serialization-safe. Official Next.js 14 pattern for dynamic-id action dispatch.
- **Root cause**: `implementer-unilateral-choice` (dynamic-id-per-row closures cleanest with inline `"use server"`).

## Where visible in code
- **File(s)**: `modules/web/app/(admin)/organizations/[id]/templates/page.tsx` (ApproveForm / DenyForm / AdoptForm / RevokeForm); `modules/web/app/(admin)/organizations/[id]/system-agents/page.tsx` (TuneForm / DisableForm); `modules/web/app/(admin)/organizations/[id]/projects/[project_id]/sessions/new/page.tsx` (previewSubmit / launchSubmit) — all 3 pages use the hybrid.
- **Test evidence**: `npm run build` + `npm run typecheck` + `npm run lint` green.
- **Grep for regression**: `grep -rnE '"use server"' modules/web/app/\(admin\)/organizations/\[id\]/templates/page.tsx` — expect ≥3 inline hits.

## Remediation scope (estimate only)
- **Approach (sketch)**: Ratify convention via ADR or a new `conventions/nextjs-server-actions.md` doc. Document the hybrid pattern for future per-row-action pages.
- **Implementation chunk this belongs to**: CH-20
- **Dependencies on other drifts**: none
- **Estimated effort**: 0 days (shipped); ~0.5 day for conventions doc.
- **Risk to concept alignment if deferred further**: none.

## Prior documentation locations (pre-M5.1)
- Plan archive lines: 1425–1431
- Code comments: inline in each page.tsx
- ADR references: none

## Lifecycle history
- 2026-04-24 — `discovered` — logged in M5/P7 close drift addendum
- 2026-04-24 — `classified` — migrated to M5.1 drift catalogue
