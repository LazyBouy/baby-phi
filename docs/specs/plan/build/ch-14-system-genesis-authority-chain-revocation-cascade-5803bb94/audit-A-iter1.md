<!-- Auto-generated by chunk-auditor agent. Do not edit. -->

# Audit A, iteration 1 — system-genesis authority-chain revocation-cascade

**Cycle hex:** `5803bb94`
**Auditor model:** opus
**Auditor agent version:** 1
**Date:** 2026-05-08
**Plan reference:** [./plan.md](./plan.md)
**Final verdict:** PASS

## Summary

| Claim | Verdict | Evidence-anchor |
|---|---|---|
| 1. `SYSTEM_GENESIS_PRINCIPAL` const + `system_genesis_principal()` helper at axioms.rs | PASS | `axioms.rs:30,38-40` |
| 2. `is_bootstrap_ar` two-witness predicate via `matches!` | PASS | `axioms.rs:68-75` |
| 3. `AuthRequest.descends_from_grant: Option<GrantId>` + `#[serde(default)]` | PASS | `nodes.rs:835,847-848` |
| 4. `walk_provenance_chain` on trait + both impls | PASS | `repository.rs:1063` + `in_memory.rs:1214` + `repo_impl_m5.rs:31` |
| 5. `revoke_grants_by_descends_from_recursive` BFS + depth-cap 32 | PASS | `repository.rs:1094-1098` + `in_memory.rs:1271-1331` + `repo_impl_m5.rs:128-212` |
| 6. Template-revoke handler uses recursive variant + iterates cascaded_ars | PASS | `revoke.rs:98-100,114-136` |
| 7. Single-hop variant unchanged + M2 narrow_mcp_tenants still calls it | PASS | `repo_impl_m2.rs:740,753-781` + `in_memory.rs:1196-1210` + `repository.rs:1042-1046` |
| 8. `CascadeResult { revoked_grants, cascaded_ars }` typed return | PASS | `repository.rs:172-182` |
| 9. Per-cascaded-AR loop: get_auth_request → guard → revoke_ar → update_auth_request → emit | PASS | `revoke.rs:114-136` |
| 10. cargo test --workspace -j 4 → 1431/0/2 | PASS-with-caveat | observed PASS in audit shell; orchestrator MUST-RUN gate remains authoritative |
| 11. CI guards green + phi-core import delta = 0 | PASS-with-caveat | observed PASS in audit shell; phi-core imports = 48 (baseline); orchestrator gate-4 authoritative |
| 12. Migration 0014 idempotent single-column-add nullable | PASS | `0014_authority_chain.surql:34` (`DEFINE FIELD OVERWRITE`) |
| 13. K8s axes A1–A7 per plan §3.B verified | PASS | trait methods take concrete types only (no generics) → `&dyn Repository`-dispatchable; one nullable-additive migration; BFS insertion-stable |
| 14. Prior-chunk invariants intact (CH-08/13/11/M2) | PASS | acceptance_authority_templates 7/7 green; acceptance_mcp_servers narrow 1/1 green; full workspace 1431/0/2 |

## Per-claim detail

### Claim 1 — `pub const SYSTEM_GENESIS_PRINCIPAL: &str = "system:genesis"` ships at axioms.rs; `system_genesis_principal()` returns `PrincipalRef::System(SYSTEM_GENESIS_PRINCIPAL.into())`

**Verdict:** PASS

**Evidence:**
- `modules/crates/domain/src/permissions/axioms.rs:30` — `pub const SYSTEM_GENESIS_PRINCIPAL: &str = "system:genesis";`
- `modules/crates/domain/src/permissions/axioms.rs:38-40` — `pub fn system_genesis_principal() -> PrincipalRef { PrincipalRef::System(SYSTEM_GENESIS_PRINCIPAL.into()) }`

The module is also re-exported from `permissions/mod.rs` (test at `axioms.rs:88` uses `system_genesis_principal()` via `super::*`).

### Claim 2 — `is_bootstrap_ar(ar) -> bool` two-witness via `matches!` (PrincipalRef has no PartialEq) AND `provenance_template == Some(system_bootstrap_template_id())`

**Verdict:** PASS

**Evidence:** `modules/crates/domain/src/permissions/axioms.rs:68-75`:
```
pub fn is_bootstrap_ar(ar: &AuthRequest) -> bool {
    let requestor_is_genesis = matches!(
        &ar.requestor,
        PrincipalRef::System(s) if s == SYSTEM_GENESIS_PRINCIPAL,
    );
    let provenance_is_bootstrap = ar.provenance_template == Some(system_bootstrap_template_id());
    requestor_is_genesis && provenance_is_bootstrap
}
```
Both witnesses AND-combined exactly per F5.A locked fork. `system_bootstrap_template_id()` returns `TemplateId::from_uuid(uuid::Uuid::nil())` at `axioms.rs:50-52`.

### Claim 3 — `AuthRequest.descends_from_grant: Option<GrantId>` with `#[serde(default)]`

**Verdict:** PASS

**Evidence:** `modules/crates/domain/src/model/nodes.rs:835,847-848`:
```
    #[serde(default)]
    pub descends_from_grant: Option<GrantId>,
```
Doc comment at lines 837-846 cross-refs ADR-0053 §D53.5 + names the deferred adoption-AR-side wiring. The field is `#[serde(default)]`-shielded (line 847), so legacy / pre-CH-14 rows decode as `None`.

### Claim 4 — `walk_provenance_chain(grant) -> Vec<AuthRequest>` on trait + both impls

**Verdict:** PASS

**Evidence:**
- Trait def: `modules/crates/domain/src/repository.rs:1063` — `async fn walk_provenance_chain(&self, grant: GrantId) -> RepositoryResult<Vec<AuthRequest>>;`
- InMemoryRepository: `modules/crates/domain/src/in_memory.rs:1214-1269` — leaf-to-root walk, reverse for root-to-leaf, terminates on `is_bootstrap_ar` OR `None` parent OR depth-cap.
- SurrealStore: `modules/crates/store/src/repo_impl_m5.rs:31-73` (inherent) + delegating trait impl at `repo_impl.rs:1702-1707`.

The plan said "repo_impl.rs"; actual SurrealDB impl lives in a sibling file `repo_impl_m5.rs` with a thin trait-delegation in `repo_impl.rs`. Plan-vs-code: this is a code-organization choice, not a missing impl — the trait method IS implemented on the SurrealStore type and is dispatched through `repo_impl.rs:1702`. Verified consistent with the gate-2 fix description.

### Claim 5 — `revoke_grants_by_descends_from_recursive(ar, at) -> CascadeResult` on trait; BFS verified; depth cap 32 via `RepositoryError::ProvenanceCycleDepthExceeded`

**Verdict:** PASS

**Evidence:**
- Trait def: `modules/crates/domain/src/repository.rs:1094-1098` — returns `CascadeResult` (gate-2 fix; was previously `Vec<GrantId>`).
- `MAX_PROVENANCE_DEPTH: u8 = 32` at `repository.rs:148`.
- `RepositoryError::ProvenanceCycleDepthExceeded { depth_cap: u8 }` at `repository.rs:139`.
- InMemory BFS: `in_memory.rs:1271-1331` — `for _ in 0..MAX_PROVENANCE_DEPTH` loop; Step A revokes live grants in frontier; Step B finds child ARs of just-revoked grants (records them in cascaded_ars); rolls frontier; depth-cap exit at line 1329-1331.
- SurrealDB BFS: `repo_impl_m5.rs:128-212` — same algorithm via SQL `UPDATE grant ... WHERE descends_from INSIDE $frontier` (line 149-151) + `SELECT ... FROM auth_request WHERE descends_from_grant INSIDE $revoked` (line 180-181); depth-cap exit at line 209-211.

Both impls preserve insertion order (BFS-stable) per ADR-0053 §D53.7 cross-pod determinism guarantee.

### Claim 6 — Template-revoke handler at templates/revoke.rs calls recursive (NOT single-hop) AND iterates cascade.cascaded_ars

**Verdict:** PASS

**Evidence:** `modules/crates/server/src/platform/templates/revoke.rs`:
- Line 98-100: `let cascade = repo.revoke_grants_by_descends_from_recursive(next.id, input.now).await?;`
- Line 102: `let grant_count_revoked = grants_revoked.len() as u32;`
- Line 114: `for cascaded_ar_id in cascade.cascaded_ars.iter().copied() { … }` — per-AR loop entry.

Plan said "expect ~89/~90/~114"; actual line numbers are 98-100 (recursive call) and 114 (loop entry). Within drift tolerance — gate-2 fix expanded the handler.

### Claim 7 — Single-hop `revoke_grants_by_descends_from` UNCHANGED; M2 narrow_mcp_tenants still uses single-hop

**Verdict:** PASS

**Evidence:**
- Trait def: `modules/crates/domain/src/repository.rs:1042-1046` — body unchanged: `async fn revoke_grants_by_descends_from(&self, ar: AuthRequestId, at: DateTime<Utc>) -> RepositoryResult<Vec<GrantId>>;` (returns `Vec<GrantId>`, not `CascadeResult` — sibling preserved per F3.A).
- InMemory: `modules/crates/domain/src/in_memory.rs:1196-1210` — same single-loop body (`g.descends_from == Some(ar) && g.revoked_at.is_none()`).
- SurrealDB: `modules/crates/store/src/repo_impl_m2.rs:753-781` — same single SQL `UPDATE grant SET revoked_at = $at WHERE descends_from = $ar AND revoked_at IS NONE`.
- M2 narrow_mcp_tenants caller: `modules/crates/store/src/repo_impl_m2.rs:740` — `let revoked = self.m2_revoke_grants_by_descends_from(ar, at).await?;` — explicitly uses single-hop, NOT recursive variant. ADR-0033 contract preserved.

### Claim 8 — `CascadeResult` struct exists carrying `revoked_grants` + `cascaded_ars`

**Verdict:** PASS

**Evidence:** `modules/crates/domain/src/repository.rs:172-182`:
```
#[derive(Debug, Clone, PartialEq, Eq, Default, Serialize, Deserialize)]
pub struct CascadeResult {
    pub revoked_grants: Vec<GrantId>,
    pub cascaded_ars: Vec<AuthRequestId>,
}
```
Plan said "line 170 or wherever"; actual location is 172 — within tolerance.

### Claim 9 — Per-cascaded-AR emission loop: (a) get_auth_request, (b) state-guard, (c) revoke_ar, (d) update_auth_request, (e) audit.emit

**Verdict:** PASS

**Evidence:** `modules/crates/server/src/platform/templates/revoke.rs:114-136`:
- Line 114: `for cascaded_ar_id in cascade.cascaded_ars.iter().copied() {`
- (a) Line 115-118: `let cascaded_ar = match repo.get_auth_request(cascaded_ar_id).await? { Some(a) => a, None => continue, };`
- (b) Line 119-127: idempotency-guard — `if !matches!(cascaded_ar.state, AuthRequestState::Approved | AuthRequestState::Partial) { continue; }`
- (c) Line 128-130: `let (next_cascaded, cascaded_event) = revoke_ar(&cascaded_ar, Some(input.actor), &input.reason, input.now).map_err(...)?;`
- (d) Line 131: `repo.update_auth_request(&next_cascaded).await?;`
- (e) Line 132-135: `audit.emit(cascaded_event).await.map_err(...)?;`

All five sub-steps land in the correct order.

### Claim 10 — cargo test --workspace -j 4 → 1431 / 0 / 2

**Verdict:** PASS-with-caveat (observed PASS in audit shell; orchestrator MUST-RUN gate remains authoritative)

**Evidence:** Sandbox-permission environment is non-deterministic. In this audit shell the workspace test ran clean. Aggregating `test result:` lines with awk yielded `passed=1431 failed=0 ignored=2` — exact match to the gate-2-fix target band (baseline 1408 + 23 new = 1431).

Spot-checked `acceptance_authority_chain` test file: `running 8 tests` all green, including the two gate-2 acceptance tests:
- `revoke_cascade_emits_one_event_per_cascaded_ar ... ok` (verified at `acceptance_authority_chain.rs:383`)
- `revoke_cascade_transitions_cascaded_ars_to_revoked_state ... ok` (verified at `acceptance_authority_chain.rs:528`)

### Claim 11 — CI guards green; phi-core import delta = 0

**Verdict:** PASS-with-caveat (observed PASS in audit shell; orchestrator MUST-RUN gate authoritative)

**Evidence:**
- `bash scripts/check-doc-links.sh` — `OK. all markdown under docs/specs/v0/implementation has valid relative links + verification headers`
- `bash scripts/check-ops-doc-headers.sh` — `OK. all 32 ops doc(s) carry the 'Last verified' header`
- `bash scripts/check-phi-core-reuse.sh` — `OK. no forbidden phi-core redeclarations under modules/crates`
- `bash scripts/check-spec-drift.sh` — `OK. 29 referenced ids all present`
- `grep -rn "use phi_core::" modules/crates/ | wc -l` → **48** (matches baseline; delta = 0).

### Claim 12 — Migration 0014 idempotent: `DEFINE FIELD descends_from_grant ON auth_request TYPE option<string>`

**Verdict:** PASS

**Evidence:** `modules/crates/store/migrations/0014_authority_chain.surql:34` — `DEFINE FIELD OVERWRITE descends_from_grant ON auth_request TYPE option<string>;`

Migration number is **0014** (plan said 0011 was the next-free number; next-free at draft time was actually 0014 because 0011-0013 were pre-existing manages-supervisor / consent-deadline / per-session-consent migrations — verified by `ls migrations/`). Migration is idempotent (`DEFINE FIELD OVERWRITE`), single-column-add, nullable (`option<string>`). K8s-axis A4 compatible per plan §3.B + CHK8S-D-05 precedent.

### Claim 13 — K8s axes A1–A7 per plan §3.B

**Verdict:** PASS

**Evidence (per axis):**
- A1 (in-process state): walker is pure read; cascade is sequence of repo writes; no `OnceCell` / `RwLock` / `Mutex` introduced — verified by grep on the new files.
- A2 (IPC): no `mpsc` / `broadcast` / `watch` / `Notify` introduced — repo_impl_m5.rs + axioms.rs use no async channels.
- A3 (pod-local resource): no new files / sockets / sub-processes.
- A4 (migration): one nullable additive column on `auth_request` (migration 0014); idempotent via `DEFINE FIELD OVERWRITE`.
- A5 (trait-shape): both new methods take concrete types (`GrantId`, `AuthRequestId`, `DateTime<Utc>`) and return concrete types — no generics, no `impl Trait`. `&dyn Repository`-dispatchable. Confirmed by handler at `revoke.rs:36-40` taking `repo: Arc<dyn Repository>` and calling the new methods.
- A6 (cross-pod state sharing): all reads/writes flow through `Repository`; durable per ADR-0033 §D33.2.
- A7 (audit hash-chain): per-cascaded-AR `auth_request.revoked` emission is deterministic in BFS insertion order on both backends per repo_impl_m5.rs + in_memory.rs algorithms; single-writer pattern preserved (only the handler emits).

### Claim 14 — Prior-chunk invariants intact

**Verdict:** PASS

**Evidence:**
- CH-08 round-trip: workspace tests 1431/0/2 include `transfer_grant_atomic_round_trip_surreal` + the AR-construction-site cascade tests; all green.
- CH-13 audit-class composition: `acceptance_authority_templates` test suite 7/7 green including `revoke_a_cascades_grants_and_blocks_re_revoke`.
- CH-11 approval_mode legacy decode: workspace-wide test suite green at 1431/0/2 (the unit test `grant_approval_mode_default_is_implicit_via_legacy_decode` at `nodes.rs:2100` runs as part of `cargo test -p domain` which is part of `--workspace`).
- M2 `narrow_mcp_tenants`: `acceptance_mcp_servers::narrow_tenants_cascades_and_emits_summary_plus_auth_request_revoked` ... ok (1/1 green); single-hop `revoke_grants_by_descends_from` body unchanged at `repo_impl_m2.rs:753-781`.

## Random spot-checks

**Spot-check 1: Claim 6 — handler line locations.** Plan said handler calls recursive at "~90"; actual line is 98. Read `revoke.rs:98-100` directly: `let cascade = repo.revoke_grants_by_descends_from_recursive(next.id, input.now).await?;` — exact match to recursive variant.

**Spot-check 2: Claim 9(b) — idempotency guard semantics.** Plan said "skip non-Approved/Partial". Read `revoke.rs:119-127`: `if !matches!(cascaded_ar.state, AuthRequestState::Approved | AuthRequestState::Partial) { continue; }`. Matches plan exactly. The guard correctly prevents double-revoke when an AR has already been closed-terminal through a prior cascade run.

## Workspace test result
- Command: `/root/rust-env/cargo/bin/cargo test --manifest-path /root/projects/phi/baby-phi/Cargo.toml --workspace -j 4`
- Output: 1431 passed / 0 failed / 2 ignored
- Plan §8 expected delta: gate-2-fix-revised target 1408 + 23 = 1431 (was originally 1426–1430 before gate-2 fix added +2 acceptance tests + +1 supporting unit test)
- Match: ✅ (exact)

## CI guards
- check-doc-links.sh: ✅
- check-ops-doc-headers.sh: ✅
- check-phi-core-reuse.sh: ✅
- check-spec-drift.sh: ✅

## Final verdict

**PASS (clean).** All 14 claims verified with concrete file:line evidence. The gate-2 inline correction is fully shipped: cascade method returns `CascadeResult { revoked_grants, cascaded_ars }`, the handler iterates cascaded_ars with the full 5-step per-AR emission loop (get → state-guard → revoke_ar → update_auth_request → emit), and the two new acceptance tests (`revoke_cascade_emits_one_event_per_cascaded_ar` + `revoke_cascade_transitions_cascaded_ars_to_revoked_state`) are present and green. Workspace tests land at 1431/0/2 — exact match. All 4 CI guards green and phi-core import baseline holds at 48 (delta = 0). Migration 0014 is idempotent + nullable + additive — K8s-axis A4 compatible. Both new trait methods are `&dyn Repository`-dispatchable (no generics). Prior-chunk invariants intact across CH-08/CH-13/CH-11/M2. No NEW drift detected.

The orchestrator's MUST-RUN gate-4 (clippy + 4 CI guards) remains the authoritative cross-check; in this audit shell those calls succeeded so claims 10 + 11 are recorded as PASS-with-caveat per chunk-auditor v5 convention.

Recommendation: proceed to retrospective. No re-spawn of Implementer or Planner indicated.
