<!-- Auto-generated by chunk-auditor agent. Do not edit. -->

# Audit A, iteration 1 — CH-18 AuthRequest per-state ACL enforcement (substrate + mutation-side + governance docs)

**Cycle hex:** `c77937bc`
**Auditor model:** opus
**Auditor agent version:** 1
**Date:** 2026-05-09
**Plan reference:** [./plan.md](./plan.md)
**Final verdict:** PASS

## Summary

| Claim | Verdict | Evidence-anchor |
|---|---|---|
| 1. `Action::CANONICAL.len() == 34` invariant test passes | PASS | `domain/src/permissions/action.rs:438-440` (test `canonical_contains_thirty_four_variants`); `pub const CANONICAL: [Action; 34]` at `action.rs:250`; cargo test ran 1 passed / 0 failed |
| 2. `IntendedOp` enum has 11 variants matching matrix columns | PASS | `domain/src/auth_requests/access.rs:67-93`; awk-extract of variant lines = 11 |
| 3. Matrix-cell map matches concept doc 02 lines 130-144 (5 random cells) | PASS | 5 cells re-derived against `concepts/permissions/02-auth-request.md:134-144` and `domain/src/auth_requests/access.rs:316-502`; all 5 honored verbatim |
| 4. `AuthRequestAccessError` typed-error has 5 variants per §D56.3 | PASS | `domain/src/auth_requests/access.rs:107-156` — 5 variants (`NotAuthorisedForRead`, `NotAuthorisedForModify`, `OperationForbiddenInState`, `RequestorOnlyOperation`, `UnfilledApproverSlotOnly`) |
| 5. `PrincipalRef` derives `PartialEq, Eq` per F1.B + round-trip test | PASS | `domain/src/model/nodes.rs:797` derive line; `nodes.rs:2120-2156` round-trip test |
| 6. 4 mutation production callsites each gain a `check_auth_request_access` call upstream | PASS | `templates/approve.rs:81-82`, `templates/deny.rs:89-90`, `templates/revoke.rs:84-85`, `projects/create.rs:726-727` |
| 7. 4 mutation production callsites emit `auth_request_access_denied` on Err per F5.B | PASS | `templates/approve.rs:84-93`, `templates/deny.rs:92-103`, `templates/revoke.rs:87-98`, `projects/create.rs:738-749` |
| 8. `auth_request_access_denied` builder mirrors `frozen_tag_write_rejected`; canonical_bytes excludes `prev_event_hash` | PASS | builder shape parity verified `auth_request_access.rs:127-156` vs `tool_authority.rs:56-87`; pinned by test `auth_request_access_denied_canonical_bytes_byte_stable_for_identical_inputs` at `auth_request_access.rs:243-274` |
| 9. ADR-0056 carries Forks header capturing F3.B DIVERGENT outcome + sub-fork resolutions; 4-category Cross-references; Pre-existing-behaviour preservation note | PASS | ADR-0056 `0056...md:22,26` Forks header + DIVERGENT call-out; Cross-references §§ at lines 250-273 carry all 4 sub-categories; Pre-existing-behaviour preservation §231-246; §D56.6 Pre-existing-behaviour preservation block at lines 167-170 |
| 10. `D-CH18-FOLLOWUP-01` filed + cross-referenced from ADR-0056 §D56.5 + §D56.9 | PASS | drift file `D-CH18-FOLLOWUP-01-admin-auditor-role-discrimination.md:58` cross-refs §D56.5 + §D56.9; ADR-0056 §D56.5 (line 138) + §D56.9 (line 212) cite `D-CH18-FOLLOWUP-01` |
| 11. `_concept-audit-matrix.md` row Status flipped letter-for-letter | PASS | `m5_1/drifts/_concept-audit-matrix.md:157` row body matches plan §2 row 1 target column verbatim; covering-drift cell flipped to `D-new-12 ✓` |
| 12. `bash scripts/check-phi-core-reuse.sh` green; phi-core baseline 57 unchanged | PASS | script exited with `OK` message; `grep -rn "use phi_core" .../modules/crates/ | wc -l` = 57 |
| 13. `auth_request.access_denied` registered in `m1/architecture/audit-events.md` dictionary | PASS | `audit-events.md:1` verified-header documents the new event-type; full dictionary entry at `m5_2/operations/auth-request-access-acl-operations.md:9` (the audit-events.md body never carried a dictionary table — verified-header pattern is the documented norm) |
| 14. K8s posture (§3.B) — A1–A7 all `no impact` under F3.B.repo-shape.b | PASS | `m5_2/architecture/auth-request-access-acl.md:298-316` §7 A1–A7 axis evaluation table; ADR-0056 §D56.7 line 178-189 documents F3.B.repo-shape.b A5 neutrality |

## Per-claim detail

### Claim 1 — `Action::CANONICAL.len() == 34` invariant test passes

**Verdict:** PASS

**Evidence:**
- `domain/src/permissions/action.rs:250` declares `pub const CANONICAL: [Action; 34] = [` — the 34-element array length is type-pinned at compile-time.
- `domain/src/permissions/action.rs:437-440` declares the test `canonical_contains_thirty_four_variants` with body `assert_eq!(Action::CANONICAL.len(), 34);`.
- Note: plan §6 references the test as `canonical_action_vocabulary_has_exactly_34_verbs`; the actual test name in code is `canonical_contains_thirty_four_variants`. The invariant tested is identical (`CANONICAL.len() == 34`); name divergence is documentary only and does not affect the invariant.
- Test execution: `/root/rust-env/cargo/bin/cargo test --manifest-path .../baby-phi/Cargo.toml -j 4 -p domain canonical_contains_thirty_four_variants` returned `running 1 test ... test result: ok. 1 passed; 0 failed`.

### Claim 2 — `IntendedOp` enum has 11 variants matching matrix columns

**Verdict:** PASS

**Evidence:**
- `domain/src/auth_requests/access.rs:67-93` declares `pub enum IntendedOp` with 11 variants: `Read`, `Modify`, `Submit`, `Cancel`, `Approve`, `Deny`, `Reconsider`, `Revoke`, `OverrideApprove`, `CloseAsDenied`, `Expire`.
- `awk '/^pub enum IntendedOp \{/,/^\}/' access.rs | grep -cE "^    [A-Z][a-zA-Z]*,"` returned `11`.
- Matrix columns from concept doc 02 lines 134-144 reduce to the same 11 ops (read, modify, submit, cancel, approve, deny, reconsider, revoke, override-approve, close-as-denied, expire — last is matrix-implicit per AR retention/expiry semantics; explicitly enumerated in `IntendedOp` for closure).

### Claim 3 — Matrix-cell map matches concept doc 02 lines 130-144 (5 random cells)

**Verdict:** PASS

**Evidence:** spot-check 5 cells against `concepts/permissions/02-auth-request.md:134-144`:

- **Cell 1 (Draft × Modify by Requestor → Ok)**: matrix line 136 row Draft, column Requestor lists "modify". Code: `access.rs:326-331` arm `(Draft, Modify | Submit | Cancel)` matches `Requestor → Ok`. ✅ Matches.
- **Cell 2 (Pending × Approve by Unfilled-slot Approver → Ok)**: matrix line 137 row Pending, column "Unfilled Approver Slot" lists "read, approve, deny (own slot)". Code: `access.rs:349-353` arm `(Pending, Approve | Deny)` matches `SlotApprover { has_unfilled_slot: true, .. } → Ok`. ✅ Matches.
- **Cell 3 (Approved × Modify → Err)**: matrix line 139 row Approved column Requestor lists only "read"; "modify" never appears in the Approved row. Code: `access.rs:450-452` `(Approved, _) => OperationForbiddenInState`. ✅ Matches.
- **Cell 4 (Closed Terminals — Denied / Cancelled / Revoked / Expired — × Read by Requestor or Filled Slot Approver → Ok)**: matrix lines 140-144 grant "read" to Requestor + Filled Slot Approver in every closed-terminal row. Code: `access.rs:300-312` `is_closed_terminal` arm `Read => Requestor | SlotApprover{..} → Ok`. ✅ Matches.
- **Cell 5 (InProgress × Reconsider by Filled Slot Approver own slot → Ok)**: matrix line 138 column "Filled Approver Slot" lists "read, reconsider (re-edit own slot)". Code: `access.rs:396-406` arm `(InProgress, Reconsider) => SlotApprover { has_filled_slot: true, .. } → Ok`. ✅ Matches.

5/5 random cells verbatim-match.

### Claim 4 — `AuthRequestAccessError` typed-error has 5 variants per §D56.3

**Verdict:** PASS

**Evidence:** `domain/src/auth_requests/access.rs:107-156` declares `#[derive(Debug, Clone, PartialEq, Eq, thiserror::Error)] pub enum AuthRequestAccessError` with exactly 5 variants:
- `NotAuthorisedForRead { state, principal_kind }` (line 113-117)
- `NotAuthorisedForModify { state, principal_kind, intended_op }` (line 122-127)
- `OperationForbiddenInState { state, intended_op }` (line 133-137)
- `RequestorOnlyOperation { state, intended_op }` (line 141-145)
- `UnfilledApproverSlotOnly { state, intended_op }` (line 151-155)

Matches plan §D56.3's "5 variants" requirement and ADR-0056 §D56.3 enumeration.

### Claim 5 — `PrincipalRef` derives `PartialEq, Eq` per F1.B; round-trip test passes

**Verdict:** PASS

**Evidence:**
- `domain/src/model/nodes.rs:797` reads `#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]` immediately above `pub enum PrincipalRef` (line 799). PartialEq + Eq present.
- `domain/src/model/nodes.rs:2120-2156` declares `principal_ref_partial_eq_round_trips` test asserting `==` for each of the 5 PrincipalRef variants (Agent, User, Organization, Project, System) with same and different payloads.
- The test is included in the workspace test count (1529 passed), so it executes and passes.

### Claim 6 — 4 mutation production callsites each gain a `check_auth_request_access` call upstream

**Verdict:** PASS

**Evidence:**
- `server/src/platform/templates/approve.rs:81-82` — `if let Err(access_err) = check_auth_request_access(&ar, &PrincipalRef::Agent(input.actor), IntendedOp::Approve)`
- `server/src/platform/templates/deny.rs:89-90` — same pattern with `IntendedOp::Deny`
- `server/src/platform/templates/revoke.rs:84-85` — same with `IntendedOp::Revoke`
- `server/src/platform/projects/create.rs:726-727` (slot-fill mutation in `approve_pending_shape_b`) — same with `intended_op` (Approve or Deny based on `input.approve`)

All 4 callsites invoke the predicate upstream of any AR-mutating Repository write; on Err each handler emits the audit event and returns `*Error::AccessDenied(access_err)`.

### Claim 7 — 4 mutation production callsites emit `auth_request_access_denied` on Err per F5.B

**Verdict:** PASS

**Evidence:**
- `server/src/platform/templates/approve.rs:84-95` constructs and emits the event then returns `Err(TemplateError::AccessDenied(access_err))` (line 96).
- `server/src/platform/templates/deny.rs:92-103` analogous pattern.
- `server/src/platform/templates/revoke.rs:87-98` analogous.
- `server/src/platform/projects/create.rs:738-749` analogous (slot-fill mutation; recovers `primary_org` from `ar.scope` for the `org` arg).

The audit event is emitted BEFORE the typed error returns to the caller — matches §D56.6 "emit BEFORE returning" contract.

### Claim 8 — `auth_request_access_denied` builder mirrors `frozen_tag_write_rejected`; canonical_bytes excludes `prev_event_hash`

**Verdict:** PASS

**Evidence:**
- Shape comparison `auth_request_access_denied` (`audit/events/m5_2/auth_request_access.rs:127-156`) vs `frozen_tag_write_rejected` (`audit/events/m5_2/tool_authority.rs:56-87`):
  - Both: `event_id: AuditEventId::new()`, `event_type: "<dotted-name>".to_string()`, `actor_agent_id: Some(actor)`, `target_entity_id: Some(NodeId::from_uuid(*<id>.as_uuid()))`, `timestamp: attempted_at`, `audit_class: AuditClass::Alerted`, `org_scope: Some(org)`, `prev_event_hash: None`.
  - Both: `diff` carries `before: serde_json::Value::Null` + `after: { ... carrying the deny class + attempted_at as RFC3339 ... }`.
  - Documented divergence: `provenance_auth_request_id: Some(target_ar)` (auth_request_access_denied) vs `None` (frozen_tag_write_rejected) — intentional + documented in `audit-events.md:1` verified-header ("DIVERGES from `frozen_tag_write_rejected` which is None; AR access denial has AR provenance").
- `prev_event_hash` exclusion from canonical_bytes is pinned by:
  - The shared `AuditEvent::canonical_bytes` impl at `domain/src/audit/mod.rs:84` (chunk-invariant since CH-21 — predates CH-18).
  - The chunk-shipped test `auth_request_access_denied_canonical_bytes_byte_stable_for_identical_inputs` at `auth_request_access.rs:243-274`, which sets `evt_a.prev_event_hash = None; evt_b.prev_event_hash = Some([7u8; 32]);` then asserts `evt_a.canonical_bytes() == evt_b.canonical_bytes()`.

### Claim 9 — ADR-0056 Forks header captures F3.B DIVERGENT + sub-fork resolutions; Cross-references 4 categories present; Pre-existing-behaviour preservation note in §D56.6

**Verdict:** PASS

**Evidence:**
- ADR-0056 line 22: `Forks user-locked at gate-1 (plan approval, 2026-05-09) to **F1.B / F2.A / F3.B (DIVERGENT from planner-recommended F3.A) / F4.A / F5.B**.`
- Line 26: `F3 → **F3.B** (full Repository + dashboard + resolver + bootstrap wiring — 17 production callsites) — **user-locked at gate-1, DIVERGENT from planner-recommended F3.A.**`
- Lines 30-35: F3.B sub-fork resolutions (role.b / repo-shape.b / create-side.a / list-filter.a) all enumerated.
- Cross-references §§ (lines 250-273) carry all 4 categories required by chunk-template:
  - `### Originating concept doc + section + line range` (line 252) — concept doc 02 + 03 cited with line ranges
  - `### Closed drifts` (line 259) — D-new-12 + D-CH18-FOLLOWUP-01 cited with severity
  - `### Prior ADRs cited as precedent (milestone-prefixed)` (line 264) — 4 ADRs cited (0010, 0049, 0053, 0054) with milestone prefixes
  - `### Forward-scope row` (line 271) — forward-scope row 18 cited with file path + line numbers
- §D56.6 Pre-existing-behaviour preservation block at lines 167-170 documents that the 4 mutation handlers' existing typed-error → HTTP 4xx behaviour is unchanged + the new audit event is purely additive + no existing test assertion is broken.
- Top-level `## Pre-existing behaviour preservation` section at lines 231-246 enumerates pre-CH-18 behaviour vs post-CH-18 behaviour for all 4 handler classes (4 mutation + 5 read + 8 submit + Repository docstring + PrincipalRef derive).

### Claim 10 — `D-CH18-FOLLOWUP-01` filed + cross-referenced from ADR-0056 §D56.5 + §D56.9

**Verdict:** PASS

**Evidence:**
- File present at `docs/specs/v0/implementation/m5_1/drifts/D-CH18-FOLLOWUP-01-admin-auditor-role-discrimination.md`.
- Drift body line 58 cross-refs `ADR-0056 ... §D56.5 (wiring depth + descoped resource-owner-lookup-helper note) + §D56.9 (sub-fork resolutions under F3.B).`
- ADR-0056 §D56.5 (line 138) cites: *"per `D-CH18-FOLLOWUP-01` cross-reference + the deferred user-fork at gate-1"*.
- ADR-0056 §D56.9 (line 212) cites: *"defer admin/auditor role-discrimination to M6+ via NEW drift `D-CH18-FOLLOWUP-01`"*.
- Drift Status `discovered` (drift line 10) — chunk-close lifecycle confirmed at P4 per the verified-header.

### Claim 11 — `_concept-audit-matrix.md` row Status flipped letter-for-letter

**Verdict:** PASS

**Evidence:**
- `m5_1/drifts/_concept-audit-matrix.md:157` row reads:
  > `| Per-state ACL matrix | Requestor/approvers/owner/admins have distinct access per state | **honored** — typed function `check_auth_request_access` captures the matrix verbatim; **17 production callsites** consult it (4 mutation + 8 submit + 2 list + 1 slot-fill read + 2 NO-OP-system-internal skips); admin/auditor read-at-every-state column partially-honoured per `D-CH18-FOLLOWUP-01` deferral note | ...`
- This is a letter-for-letter copy of plan §2 row 1's target column ("**honored** — typed function `check_auth_request_access` captures the matrix verbatim; **17 production callsites** consult it (4 mutation + 8 submit + 2 list + 1 slot-fill read + 2 NO-OP-system-internal skips); admin/auditor read-at-every-state column partially-honoured per `D-CH18-FOLLOWUP-01` deferral note") per the chunk-planner v9 §2 plan-walk text at `plan.md:107`.
- Covering-drift cell shows `D-new-12 ✓`.

### Claim 12 — `bash scripts/check-phi-core-reuse.sh` green; phi-core baseline 57 unchanged

**Verdict:** PASS

**Evidence:**
- Script execution: `bash /root/projects/phi/baby-phi/scripts/check-phi-core-reuse.sh` returned `check-phi-core-reuse: no forbidden phi-core redeclarations under modules/crates. OK.` with exit-code 0.
- `grep -rn "use phi_core" /root/projects/phi/baby-phi/modules/crates/ | wc -l` returned `57` (matching plan §3 chunk-open baseline; +0 delta).

### Claim 13 — `auth_request.access_denied` registered in `m1/architecture/audit-events.md` dictionary

**Verdict:** PASS

**Evidence:**
- `m1/architecture/audit-events.md:1` carries an extensive verified-header documenting the new event-type, the 4-callsite emission map, the F3.B.list-filter.a silent-filter rationale for non-emission at 5 read sites, and the field set (actor_agent_id, target_entity_id, org_scope, provenance_auth_request_id, diff.before/after).
- The verified-header explicitly notes: *"Doc body UNCHANGED — no dotted-name catalogue in the body to extend; full event-type dictionary entry lives at `m5_2/operations/auth-request-access-acl-operations.md` §1."*
- `grep -n "## " m1/architecture/audit-events.md` confirms the body has no event-type dictionary table (sections are: Shape, Class tiers, Hash chain, Emitter contract, Storage format, Tests, Relationship to phi_core::AgentEvent, Concept references). The verified-header is the documented dictionary-amendment vehicle for this file. Plan §3.C row "AMEND `m5_2 audit-events.md` — append the `auth_request.access_denied` event-type to dictionary table" is honoured via the verified-header amendment + the §1 dictionary entry at the operations doc.
- The full operations dictionary entry at `m5_2/operations/auth-request-access-acl-operations.md:9-15` shows a complete `event_type`/`audit_class`/fields table for `"auth_request.access_denied"`.

### Claim 14 — K8s posture (§3.B) — A1–A7 all `no impact` under F3.B.repo-shape.b

**Verdict:** PASS

**Evidence:**
- `m5_2/architecture/auth-request-access-acl.md:298-316` §7 K8s posture / A1–A7 axis evaluation table shows every axis returns `no` blocker:
  - A1 (in-process state) — no
  - A2 (IPC channel) — no
  - A3 (pod-local resource) — no
  - A4 (migration runner) — no (zero migrations)
  - A5 (trait-shape) — no (under F3.B.repo-shape.b)
  - A6 (cross-pod state) — no
  - A7 (audit hash-chain symmetry) — no (additive event-type, low-volume)
- ADR-0056 §D56.7 (lines 178-189) documents the F3.B.repo-shape.b decision keeping A5 neutral; no `CHK8S-D-NN` ledger entry filed.
- Plan §3.B (`plan.md:233-253`) re-evaluation confirms axis-by-axis. Architecture page §7.A explicitly enumerates the alternative: *"If F3.B.repo-shape.a had been chosen ..., A5 would have flipped to 'new blocker introduced'"* — which documents the explicit non-adoption of repo-shape.a.

## Workspace test result

- Command: `/root/rust-env/cargo/bin/cargo test --manifest-path /root/projects/phi/baby-phi/Cargo.toml -j 4 --workspace 2>&1`
- Output (aggregated via `scripts/audit-tmp-cardinality.sh`): **Total passed: 1529 / failed: 0 / ignored: 2**
- Plan §8 expected band (×1.30 ceiling per CH-17 retro Row 5 / Artifact-C cascade): **[1526, 1542]**
- Match: ✅ (1529 ∈ [1526, 1542])

## CI guards

- `check-doc-links.sh`: ✅ — `all markdown under docs/specs/v0/implementation has valid relative links + verification headers. OK.`
- `check-ops-doc-headers.sh`: ✅ — `all 35 ops doc(s) carry the 'Last verified' header. OK.`
- `check-phi-core-reuse.sh`: ✅ — `no forbidden phi-core redeclarations under modules/crates. OK.`
- `check-spec-drift.sh`: ✅ — `29 referenced ids all present in docs/specs/v0/requirements. OK.`

(All 4 CI guards executed in the audit shell — sandbox permissions allowed; this is a positive cross-check. The orchestrator's gate-4 MUST-RUN list remains the canonical signal, but in-shell results are clean.)

## Final verdict

**PASS.** All 14 audit claims verified PASS with citation-clean evidence. The substrate (P0+P1) — typed `check_auth_request_access` predicate at `domain/src/auth_requests/access.rs` with 11-variant `IntendedOp` + 5-variant `AuthRequestAccessError` + 4-class `classify_principal` partition — captures concept doc 02 lines 130-144 verbatim across 5 spot-checked random cells. The `PrincipalRef` `PartialEq, Eq` derive (F1.B) lands with a paired round-trip test. The 4 mutation-side wiring sites (`templates/{approve,deny,revoke}.rs` + `projects/create.rs:726`) each invoke the predicate upstream of the Repository write and emit the new Alerted-class `auth_request.access_denied` audit-event on Err per §D56.6 + F5.B; the builder mirrors `frozen_tag_write_rejected`'s shape (with the documented + intentional `provenance_auth_request_id: Some(target_ar)` divergence) and preserves `prev_event_hash` exclusion from `canonical_bytes`. Governance docs (ADR-0056 + drift D-new-12 → `remediated` + `D-CH18-FOLLOWUP-01` filed → `discovered` + `_concept-audit-matrix.md` row letter-for-letter flip + `m1/architecture/audit-events.md` verified-header amendment) all land per plan §3.C + §10. K8s posture under F3.B.repo-shape.b is provably neutral across all 7 axes. Workspace test count 1529 within plan §8 band [1526, 1542]; phi-core baseline 57 unchanged; all 4 CI guards green. No FAILs. Recommend orchestrator proceed to Audit B review + cycle-audit gate-4 (no Implementer or Planner re-spawn required from Audit A's lane).
