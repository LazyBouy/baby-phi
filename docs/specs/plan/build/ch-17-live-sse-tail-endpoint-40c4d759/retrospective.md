<!-- Auto-generated by chunk-retrospector agent. Reviewed by orchestrator + user before standards updates apply. -->

# Retrospective — CH-17 — Live SSE tail endpoint

**Cycle hex:** `40c4d759`
**Date:** 2026-05-09
**Plan reference:** [./plan.md](./plan.md)
**Cycle-audit reference:** [./cycle-audit.md](./cycle-audit.md)

## §1 — Cycle metadata

- **Total audit iterations:** 1 (Audit A iter 1 PASS + Audit B iter 1 PARTIAL → 2 Trivial-1L orchestrator inline patches; no auditor re-spawn).
- **Final orchestrator audit:** GREEN — see [`cycle-audit.md`](./cycle-audit.md).
- **Plan estimated effort:** ~1.0–1.3 days (iter-2 with F5.B-locked + F5.B.subfork.a migration 0016) | **Actual effort:** ~1.5 days (≈08:00 UTC plan-mode → ≈14:14 UTC cycle-audit close + ~4h additional integration-test scope expansion + mid-cycle disk-full recovery).
- **Tests delta planned:** plan §8 draft band [1484, 1488]; **Audit A widened** to [1488, 1495] post-implementation; actual: **+29 (1462 → 1491)**.
- **Files changed:** 25 modified + 8 new (12 code + 11 doc + 5 test files; per cycle-audit §7).
- **Drifts closed:** D7.1 (HIGH) → `remediated`. **0 new follow-up drifts** (3rd cycle in 6 to close cleanly without `CH-NN-FOLLOWUP-NN`).
- **ADR(s) accepted:** ADR-0055 (`m5_2/decisions/0055-sse-broadcast-fanout-and-keepalive.md`) flipped Proposed → Accepted with sub-decisions D55.1–D55.9.
- **K8s ledger:** new entry **CHK8S-D-10** (HIGH; M7b target — Redis pub/sub-backed `SessionLiveStreamRegistry` for cross-pod live-event fan-out). Deferred-ledger totals 9 → 10.
- **Forks user-locked:** 6 + 1 sub-fork (F1.A / F2=64 / F3.B / F4=30s / F5.B / F6.B / F5.B.subfork.a). F5 re-spawned at iter-2 to correct fact-base (Observe is canonical at action.rs:73,282).
- **Disk reclaimed at chunk-seal:** **107 GB** via `cargo clean` (from 110G used → 154G free).

## §2 — Outcomes

What CH-17 delivered against the plan:

- ✅ **Drift D7.1 (HIGH) remediated cleanly.** SSE handler at `GET /api/v0/sessions/:id/events` (`server/src/handlers/sessions.rs:215`), platform body `open_live_stream` (`server/src/platform/sessions/events.rs:109`), trait-shaped `SessionLiveStreamRegistry` (`server/src/state.rs:134-208`), broadcast tap on `BabyPhiSessionRecorder` (`domain/src/session_recorder.rs:148`), and CLI default-tail consumer (`cli/src/commands/session.rs:74,246-317`) all shipped per ADR-0055 §D55.1–§D55.4.
- ✅ **F5.B fact-base correction applied + ADR-documented.** Iter-2 re-spawn surfaced that `Action::Observe` is already canonical (`action.rs:73,282,322`) per concept-doc 03 line 22 + 44; `[Observe]` on `session_object` is semantically more precise than the iter-1 `[Read, Inspect, List]` fallback. Sibling builder `build_session_observe_manifest` shipped at `domain/src/permissions/builders/session_observe.rs:70`. Closed 34-verb invariant preserved (`Action::CANONICAL.len() == 34` at `action.rs:439`).
- ✅ **Migration 0016 + Template A 4-action mint extension shipped cleanly.** `0016_template_a_session_object_grant_add_observe.surql` walks live Template A grants and idempotently appends `"observe"`; `fire_grant_on_lead_assignment` at `domain/src/templates/a.rs:151-156,191-196` mints `[Read, Inspect, List, Observe]` going forward. Authority Chain preservation (CH-14 invariant) intact: migration leaves `descends_from` untouched.
- ✅ **Doc-sync sweep widened (CH-15 retro Row 1) closed cleanly first-pass.** Audit B claim 16 — ZERO unaddressed stale-narrative hits across all `m*/architecture+operations+user-guide`. **First cycle to close on first audit pass with the widened sweep in effect** — empirical validation that the widened sweep is correctly preventing the cross-doc stale-reference class of finding.
- ✅ **Permissions-audit cluster validations both passed.** bash-check cluster: 0 PermissionRequests (validates CH-15's `resolved-via-workaround` state holds). cargo-test 4-stage cluster: 0 PermissionRequests (validates CH-14's `audit-tmp-cargo-counts.sh` script-refactor + settings.json line 45 rule).
- ⚠️ **Mid-cycle Bash sandbox failure (disk-full).** Compiled test binaries consumed >160 GB causing every cargo/clippy/test/grep/script-spawn invocation to return Exit 1 (no stdout/stderr). The first implementer-spawn shipped 25 modified + 5 new code files BEFORE the failure but couldn't run gate verifications. User manually freed disk; orchestrator re-dispatched the implementer twice (once for paperwork, once for integration tests). **Not a process-quality regression** — clean recovery; standards-update proposed (§5 row 1) to prevent recurrence.
- ⚠️ **Plan §8 test-count band drifted.** Plan §8 predicted [1484, 1488]; Audit A widened to [1488, 1495] post-implementation; final 1491. The realised count exceeded plan §8 upper bound by +3. Indicates the planner under-estimated the test-fixture-assertion cascade (Artifact C predicted ≤ 12 assertion edits; actuals were larger).
- ⚠️ **§3.C user-guide tier filename divergence.** Plan §3.C row 4 named `m5_2/user-guide/session-live-events-walkthrough.md` (NEW); shipped reality folded the content into `m5/user-guide/first-session-walkthrough.md` CH-17 amendment subsection (deliberate avoid-fragmentation choice). Audit B iter 1 flagged as PARTIAL claim 11; orchestrator-applied Trivial-1L plan §3.C row 4 amendment closed inline. Process insight: planner should evaluate amendment-target-vs-new-file at plan time.
- ⚠️ **User-driven scope expansion at gate 3 (~4h).** Audit B iter 1 surfaced that plan §8 named integration tests (sse_live_stream_test.rs + 2 CLI + acceptance round-trip) that the first implementer-spawn did NOT ship (band-floor met via 8 surrogate platform-layer + builder-unit tests). User chose ship-integration-tests-in-cycle over defer-via-follow-up-drift per quality-over-speed principle. Final test count 1491/0/2.
- ⚠️ **phi-core import baseline planned +1 / actual +2 production sites.** Plan §3 predicted +1 production import (`events.rs`); actual is +2 (`events.rs:48` + `state.rs:11`) — both canonical re-uses of `phi_core::types::event::AgentEvent`, neither duplicates a phi-core surface (`check-phi-core-reuse.sh` green). Total Δ +8 (1462 baseline 49 → 57: +1 prod-1 + +1 prod-2 + +6 test-only direct-reuse imports).

## §3 — Audit-cycle gaps

Every FAIL across iterations + every cycle-audit §9 finding traced to a root cause + a proposed change.

| # | Iteration | Audit | Claim | Verdict | Root cause | Why earlier phases missed it | Proposed gap-closing change |
|---|---|---|---|---|---|---|---|
| 1 | iter1 | B | Cycle-index row added for CH-17 | FAIL (Trivial-1L) | `_cycle-index.md` row missing for CH-17 at gate-3 close | Plan §7 P-seal listed cycle-index update as part of seal paperwork, but the implementer-spawn that ran the seal phase didn't reach §"Active cycles" table edit before the integration-test scope expansion forked attention | **Already remediated in-cycle** — orchestrator-applied Trivial-1L patch at gate-3. Persistent gap-closing: chunk-implementer prompt should explicitly enumerate `_cycle-index.md` row insertion as a chunk-seal MANDATORY paperwork item with verification command (`grep -n <cycle-hex> _cycle-index.md`), not as an implicit follow-on. |
| 2 | iter1 | B | Plan §3.C row 4 user-guide tier (new m5_2 stub) | PARTIAL (Trivial-1L) | Plan §3.C predicted `m5_2/user-guide/session-live-events-walkthrough.md` NEW; shipped `m5/user-guide/first-session-walkthrough.md` "CH-17 amendment" subsection (deliberate avoid-fragmentation choice during paperwork phase) | Planner did not evaluate whether the existing `m5/user-guide/*` walkthrough was a more discoverable amendment target than a NEW fragmentary `m5_2/user-guide/` stub | **§5 row 3** — chunk-planner template §3.C: when planner predicts a `m5_2/user-guide/` (or any `mN/user-guide/`) NEW file, plan §3.C row MUST evaluate whether existing `m<prev>/user-guide/*` file would be a more discoverable amendment target. Document the evaluation rationale inline; if amendment chosen, explicitly cite the target file. |
| 3 | gate-2 (sandbox) | self | Implementer ran `cargo build/test/clippy/grep` invocations — all returned Exit 1 with no stdout/stderr | OPS-FAILURE (mid-cycle) | Compiled test binaries consumed >160 GB on root disk; every shell command failed silently | No prior cycle had hit this scale of disk consumption; orchestrator nor implementer had a pre-flight disk-space check; user discovered via diagnostics + manually freed disk | **§5 row 1** (USER REQUESTED) — codify "cargo clean at chunk-seal" as a permanent habit: chunk-implementer prompt + per-chunk-planning-template §"Chunk-close paperwork" + CLAUDE.md gate-2 sub-step. Trigger: at chunk-seal AFTER all verifications green, run `cargo clean --manifest-path /root/projects/phi/baby-phi/Cargo.toml` + report disk reclaimed in implementation report. CH-17 reclaimed 107 GB at close. |
| 4 | gate-3 → gate-4 (orchestrator) | self | Plan §8 expected band [1484, 1488] but realised count 1491 (+3 over upper bound) | DRIFT (resolved by Audit A's prompt-side band-widening to [1488, 1495]) | Planner's Artifact C cascade analysis predicted ≤ 12 assertion-edit sites; actuals were larger because the prop-test extension + builder unit tests + new sse_live_stream_test.rs + acceptance round-trip pushed the count beyond Artifact C's bound | Plan §8 used a tight asymmetric ×1.0–×1.20 buffer band; the buffer didn't account for cycle-time scope-additions (4 builder unit tests + 2 migration tests + integration tests added at user-driven gate-3 scope expansion) | **§5 row 5** — when plan §8 lower-bound test count is contingent on Artifact C cascade (assertion-edit fan-out), buffer ceiling should widen to ×1.30 (was ×1.20). When user-driven mid-cycle scope expansion lands, planner should re-stamp plan §8 band immediately rather than rely on auditor prompt-side widening. |
| 5 | iter-1 | B | Plan §8 band-discrepancy not updated post-implementation | DRIFT (informational) | Plan §8 band [1484, 1488] never updated after gate-3 scope expansion; auditor prompt carried widened band [1488, 1495] which Audit A used | Implementer-spawn responsible for paperwork didn't loop back to plan §8 update after the gate-3 scope addition; plan §8 became a stale draft-time prediction | (covered by §5 row 5 above — same root cause; plan §8 must update on mid-cycle scope changes) |
| 6 | gate-1 (planner) | self | F5 framing iter-1 incorrectly predicted closed-set break | RESOLVED (re-spawn iter-2 corrected fact-base) | Planner's iter-1 grep of `Action::CANONICAL` array missed the line containing `Observe` at `:282`; planner cited concept-doc 03 line 22 but did not visualize line 44's universal-applicability claim | Planner's pre-flight check did not include a literal grep of `^\s*Observe,$` against `action.rs` AND a verbatim read of concept-doc 03 lines 22+44 | **§5 row 2** — chunk-planner v8 already has §3.D rule (`forward-scope-vs-concept-doc precedence`); CH-15 + CH-17 are 2 cycles where iter-2 re-spawn was needed to correct fact-base. Codify a pre-§3.D-claim MANDATORY GREP step: when planner is about to claim "X breaks the closed action set" or "Y extends the closed vocabulary", planner MUST first run `grep -nE '^\s*<verb>,$' action.rs` AND verbatim-read concept-doc 03's full applicability matrix BEFORE writing the claim. (Chunk-planner v8's prompt may already imply this; CH-17 retro affirms + flags if pattern continues into CH-18+.) |
| 7 | gate-3 → gate-4 (orchestrator) | self | First implementer-spawn shipped only platform/builder unit tests, omitting integration tests named in plan §8 | RESOLVED (orchestrator + user re-dispatched implementer; +4h scope) | First implementer-spawn (after disk-full recovery) interpreted "band-floor met" as scope-complete, skipping the named integration tests; the plan's §"Test infrastructure" guidance about `reqwest stream` + `std::process::Command` SSE-client fixture pattern was on plan but no fixture file pre-existed | Plan §8 named integration tests but didn't separate them from unit tests as a MUST-SHIP-BEFORE-CHUNK-SEAL list; auditor (Audit B) caught it but only at iter 1 (gate-3 close) | **§5 row 6** — per-chunk-planning-template §8 should split test enumeration into MUST-SHIP (named test files) vs MAY-COVER (band-floor surrogates). Implementer prompt: when MUST-SHIP files are absent at chunk-seal, the implementer MUST flag this as a chunk-seal blocker, not silently substitute surrogate band-floor coverage. |
| 8 | gate-4 (orchestrator) | self | Production phi-core import delta predicted +1 / actual +2 (state.rs + events.rs) | DRIFT (informational; check-phi-core-reuse green) | Plan §3 predicted +1 production import (events.rs only); actuals show state.rs imports `AgentEvent` because the broadcast Sender's generic type bound is `AgentEvent` — both canonical re-uses, neither duplicates a phi-core type | Plan §3 phi-core leverage map only listed events.rs as the +1 site; the inferential need for state.rs to also import AgentEvent (because the trait registry's value type IS broadcast::Sender<AgentEvent>) was not anticipated | **§5 row 7** — chunk-planner template §3 phi-core leverage map: when a phi-core type is shared across multiple sites (e.g., `AgentEvent` in `state.rs` registry value type + `events.rs` SSE handler wire-serialisation), plan §3 should anticipate Δ ≥ N rather than +1, with a per-site enumeration column. |

## §3.5 — Permissions audit findings

> Auto-extracted from the `permissions-audit` skill (full report appended at end of doc).
> Window: 2026-05-09T07:47:56Z → 2026-05-09T14:16:09Z (UTC; ~6h28m). Tool calls: **281**. Unique signatures: **92**.
> PermissionRequest count: **7** (4 AskUserQuestion + 2 Bash:cargo:build + 1 Bash:cat). Hook denials (PostToolUseFailure): **8**.

### Hot allow-rule candidates

**(none qualified as ≥ 3-prompt hot signature this cycle.)** All PermissionRequest fires were one-offs or two-of-a-kind:
- `Bash:cargo:build` — 2 prompts. Both invocations during disk-full recovery used `2>` redirect form OR composite stdout/stderr redirect-and-pipe (`1>/tmp/out.log 2>&1; wc -l /tmp/out.log`) which the existing `Bash(cargo build *)` allow rule does not match (compound shape with `;` chain). Below the ≥ 3-prompt hot threshold; does not warrant a new rule.
- `Bash:grep` (1 prompt) — a `grep -nE` invocation matched against an existing `Bash(grep *)` allow rule but the invocation contained an embedded `\|` pipe alternation in the regex, parsed as a composite Bash shape. One-off.
- `Bash:cat` (1 prompt) — `cat /tmp/cargo-build.log | tail -50`. Two-stage pipe; allow rule `Bash(cat *)` matches `cat ...` but the pipe-stage to `tail` does not get covered. One-off.
- `AskUserQuestion` (4 prompts — F5 lock + F5 confirmation + F5.B.subfork lock + plan-approval ExitPlanMode equivalent) — **expected per gate-1 escalation protocol**; not a hot-allow-rule candidate.

### Dead allow rules (removal candidates)

**(none qualified — need more cycle data with consistent zero-hit observations.)** Cycle window is too narrow (281 tool calls in ~6.5h captured + sub-agent calls likely under separate process IDs not captured in this window).

### Hook false-positive flags

**(none flagged as likely-test/verification false positive.)** All 8 PostToolUseFailure entries are legitimate failures:
- 3× `Bash:cargo:build | reason: Exit code 1` — disk-full failures during the mid-cycle Bash sandbox issue.
- 2× `Read:... | reason: File content (>25000 tokens) exceeds maximum` — large plan/audit doc reads (legitimate; agent retried with offset/limit).
- 2× `Bash:bash:audit-tmp-carg... | reason: Exit code 101 passed=N failed=N` — cargo-test failures inside the audit-tmp-cargo-counts.sh script-shaped invocation; legitimate compile-error / fmt-fix iteration during scope-expansion phase.
- 1× `Bash:cat:/tmp/cargo-build.log | reason: Exit code 1` — disk-full failure (file empty / unwritten because parent cargo-build also failed).

### Cross-cycle trend signal

**bash-check cluster: 0 prompts in CH-17 (validates CH-15's `resolved-via-workaround` state — second cycle of zero-prompt observation).** Per `permissions-audit` skill v3 step 5b: the `resolved-via-workaround` state holds; **drop the cluster from cross-cycle trend tracking permanently** (it is no longer noise per the lifecycle-state policy). The mid-cycle Bash sandbox failure was a disk-full failure, NOT a matcher issue — does not regress the workaround validation.

**cargo-test 4-stage cardinality cluster: 0 prompts in CH-17 (validates CH-14's `audit-tmp-cargo-counts.sh` script-refactor — second cycle of zero-prompt observation).** Settings.json line 45 `Bash(bash /root/projects/phi/baby-phi/scripts/audit-tmp-*.sh*)` continues to cover cycle-time audit-extraction script invocations cleanly.

**Tool call volume:** 281 events in ~6.5h captured (vs CH-15: 514 in ~2h49m, CH-14: 730 in ~4h26m, CH-08: 493 in ~14h22m, CH-07: 237). CH-17's lower count reflects the dispatched-implementer + dispatched-auditor model — those sub-agent calls happen under separate logger process IDs and don't all show up in the orchestrator's window. No anomaly.

**settings.json mid-cycle edit capture (per CH-08 retro Row 3 / chunk-retrospector v3 step 5b):** settings.json mtime is `2026-05-08T14:49:22Z` — **OUTSIDE the CH-17 cycle window**. No mid-cycle out-of-band edit. (Last user-led edit was at CH-15 close; settings.json is stable.)

### Audit-driven standards updates proposed

**No standards updates from §B/§D/§E originate this cycle.** The permissions-audit landscape is healthy: zero hot allow-rule candidates, zero dead-rule removal candidates, zero hook false-positives, zero cross-cycle regressions. All standards updates in §5 below originate from §3 audit-cycle gaps OR §3.5 cross-cycle affirmations OR USER REQUESTED.

## §4 — Process changes proposed

Changes to how the workflow runs (lighter-weight than §5 standards updates; many call out-or-evolve agent-prompt invariants).

1. **Implementer + orchestrator pre-flight disk-space check** (closes §3 row 3) — at chunk-open and after a chunk-seal verification round, run `df -h /` (or equivalent); flag if < 10 GB free; block dispatch if < 2 GB free. Pairs with the `cargo clean at chunk-seal` standards update (§5 row 1).
2. **Planner pre-§3.D-claim mandatory grep** (closes §3 row 6) — when chunk-planner is about to write a §3.D contradiction claim of the form "X breaks the closed action set" or "Y extends the canonical N-verb vocabulary", planner MUST run `grep -nE '^\s*<verb>,$' action.rs` + verbatim-read concept-doc 03's full applicability matrix BEFORE writing the claim. Echoes CH-15 retro Row 5; CH-17 affirms the pattern; if CH-18+ also exhibits, escalate to a hard-gate skill check.
3. **Implementer chunk-seal MUST-SHIP file enumeration** (closes §3 row 7) — chunk-implementer prompt: when MUST-SHIP files (per plan §8 + §7 P-seal) are absent at chunk-seal, the implementer MUST flag this as a chunk-seal blocker (NOT silently substitute surrogate band-floor coverage). Pairs with the per-chunk-planning-template §8 split (§5 row 6).
4. **Audit-cycle attribution policy when scope expands mid-cycle** (informational) — when user-driven scope expansion lands at gate-3 (as in CH-17's integration-test addition), the next auditor-spawn auditor-prompt expected-band statement MUST be re-stated; the planner-spawn does NOT need to re-write plan §8 (which records draft-time predictions, not gate-time bands). This was Audit A's behaviour and is correct; codify in chunk-auditor v6 prompt.

## §5 — Standards updates proposed

Changes to durable artifacts. Each entry: target file + concrete diff intent + gap citation. **Total: 7 standards updates proposed.**

| # | Target | Anchor | Change | Closes gap |
|---|---|---|---|---|
| 1 | `/root/projects/phi/.claude/agents/chunk-implementer.md` + `baby-phi/docs/specs/v0/implementation/m5_1/process/per-chunk-planning-template.md` (§"Chunk-close paperwork") + `/root/projects/phi/CLAUDE.md` (§"Orchestrator's gates" gate-2) | new mandatory step | **(USER REQUESTED 2026-05-09)** Codify "cargo clean at chunk-seal" as a permanent habit. **Trigger condition**: at chunk-seal AFTER all gate-2 verifications green AND gate-3 audit clean (NOT BEFORE — would bust the verification cache). Run `cargo clean --manifest-path /root/projects/phi/baby-phi/Cargo.toml` and report disk reclaimed in the cycle-audit §7 metrics row "Disk reclaimed at chunk-seal". CH-17 reclaimed 107 GB. | §3 row 3 (mid-cycle disk-full Bash sandbox failure) |
| 2 | `/root/projects/phi/.claude/agents/chunk-planner.md` (v8 → v9) | §3.D `forward-scope-vs-concept-doc precedence` rule | Strengthen the §3.D rule: BEFORE writing any §3.D contradiction claim about a closed action vocabulary, planner MUST run `grep -nE '^\s*<verb>,$' /root/projects/phi/baby-phi/modules/crates/domain/src/permissions/action.rs` AND verbatim-cite concept-doc 03 line 22 (Action × Category table) + line 44 (universal-applicability claim) BEFORE issuing the contradiction. Cite CH-15 + CH-17 as the 2 precedent cycles where iter-2 re-spawn was needed. **Pattern-watch**: if CH-18+ continues to exhibit, escalate to `phi-core-leverage-check` skill-style hard-gate. | §3 row 6 (F5 iter-1 fact-base error; 2nd cycle in 2 to apply rule) |
| 3 | `baby-phi/docs/specs/v0/implementation/m5_1/process/per-chunk-planning-template.md` | §3.C user-facing-doc impact map | Add new evaluation rule: **"§3.C amend-don't-add precedence"** — when planner predicts a `mN/user-guide/` NEW file for a chunk-narrative walkthrough, the §3.C row MUST evaluate whether an existing `m<prev>/user-guide/*` file (the discoverable single-walkthrough surface for users) would be a more appropriate amendment target. Document the evaluation rationale inline in §3.C; if amendment chosen, cite the target file + planned subsection title. CH-17 retro affirms via the `m5_2/user-guide/session-live-events-walkthrough.md` → `m5/user-guide/first-session-walkthrough.md` "CH-17 amendment" Trivial-1L correction. | §3 row 2 (Audit B PARTIAL claim 11 user-guide tier filename divergence) |
| 4 | `/root/projects/phi/.claude/agents/chunk-implementer.md` | §"Chunk-seal paperwork" | Explicitly enumerate `_cycle-index.md` row insertion as a MANDATORY chunk-seal item with verification command (`grep -n <cycle-hex> /root/projects/phi/baby-phi/docs/specs/plan/build/_cycle-index.md`); NOT as an implicit follow-on of plan §7 P-seal. Failure-mode: implementer-spawn that runs the seal phase forks attention (e.g., to integration-test scope expansion) and the cycle-index row gets dropped. CH-17 had this fail; orchestrator-applied Trivial-1L closed inline. | §3 row 1 (cycle-index row missing for CH-17) |
| 5 | `baby-phi/docs/specs/v0/implementation/m5_1/process/per-chunk-planning-template.md` | §8 test-count band rule | Widen the asymmetric buffer ceiling from ×1.20 → ×1.30 when plan §8 lower-bound test count is contingent on Artifact C (assertion-edit fan-out) cascade. **Mid-cycle scope-expansion rule**: when user-driven scope expansion lands at gate-3 (e.g., integration tests added), the implementer MUST re-stamp plan §8 band; auditor-prompt-side band-widening is a band-aid, not a primary fix. CH-17 ran [1484, 1488] plan band → [1488, 1495] audit-prompt band → 1491 actual; widening on Artifact C cycles avoids future gate-discrepancy noise. | §3 rows 4 + 5 (plan §8 band-discrepancy) |
| 6 | `baby-phi/docs/specs/v0/implementation/m5_1/process/per-chunk-planning-template.md` | §8 test enumeration | **Split §8 test enumeration into 2 sub-sections**: (a) MUST-SHIP — named test files that MUST exist as files-on-disk by chunk-seal (e.g., `server/tests/sse_live_stream_test.rs`); (b) MAY-COVER — band-floor surrogates that count toward the test-count target but are not MUST-SHIP. Pair with chunk-implementer prompt change: when MUST-SHIP files are absent at chunk-seal, implementer MUST flag as chunk-seal blocker, NOT silently substitute MAY-COVER coverage. CH-17's first implementer-spawn dropped the integration-tests file (`sse_live_stream_test.rs`) per band-floor surrogate substitution; user-driven re-dispatch closed the gap with +4h scope. | §3 row 7 (first implementer-spawn dropped integration tests) |
| 7 | `/root/projects/phi/.claude/agents/chunk-planner.md` | §3 phi-core leverage map | When a phi-core type is shared across multiple sites (the chunk's surface change touches ≥ 2 distinct files importing the same `phi_core::*` type), plan §3 MUST anticipate Δ ≥ N rather than +1, with a per-site enumeration column. CH-17 had `AgentEvent` shared across `state.rs` (registry value type) + `events.rs` (SSE handler wire-serialisation) — predicted +1, actual +2 (both canonical re-uses; check-phi-core-reuse green). Not a violation, but plan-§3-prediction precision should anticipate it. | §3 row 8 (phi-core import delta predicted +1 / actual +2) |

**Standards updates from §3.5 permissions audit:** **0 (zero originated this cycle).** The permissions-audit landscape is fully healthy.

## §6 — Cross-cycle patterns + open questions

- **Echoes prior cycle?**
  - **§3 row 6 (F5 iter-1 fact-base error → re-spawn iter-2)** — **YES** — echoes CH-15 (cycle hex `c3f46f17`)'s F-CRITICAL re-spawn. Both CH-15 + CH-17 needed iter-2 re-spawn to correct planner's §3.D contradiction claim. **2-cycle pattern — escalate to standards update §5 row 2** (chunk-planner v8 → v9 strengthen §3.D rule).
  - **§3 row 1 (cycle-index row missing)** — **NO direct echo, but related pattern**: CH-14 (cycle hex `5803bb94`) had similar paperwork-skipped finding (`_changelog.md` row missing) caught at gate-3 close, also closed via Trivial-1L. **Pattern: when implementer-spawn forks attention (gate-3 scope expansion, audit-fix iteration), small paperwork items drop. Standards update §5 row 4 enumerates them explicitly.**
  - **§3 row 2 (§3.C user-guide tier filename divergence)** — **NO echo** — first occurrence; flag for next-cycle pattern check.
  - **Permissions audit cluster validations** — both bash-check + cargo-test 4-stage clusters at 0 prompts for 2 consecutive cycles (CH-15 + CH-17). **`resolved-via-workaround` lifecycle states hold; drop both clusters from cross-cycle tracking permanently per skill v3 step 5b.**

- **Open questions for next cycle (CH-18+):**
  - Does CH-18 also need iter-2 re-spawn for a §3.D contradiction claim? If YES → standards update §5 row 2 is insufficient; promote to skill-style hard-gate (`phi-core-leverage-check`-equivalent for §3.D rule).
  - Does CH-18's chunk-implementer prompt (after §5 row 4 codification) ship cycle-index row at chunk-seal first time? Validation cycle for §5 row 4.
  - Does CH-18+ continue the "0 follow-up drifts" streak (now 3 cycles in 6: CH-08 + CH-15 + CH-17)? If YES, this validates the multi-agent pipeline's quality-over-speed principle.

- **System health signals:**
  - Audit re-spawn count: **1** (Audit A iter 1 PASS + Audit B iter 1 PARTIAL → 2 Trivial-1L orchestrator inline patches, no auditor re-spawn) — **green** (well under threshold of ≥ 2 per audit letter).
  - Final-cycle-audit findings count: **0** (cycle-audit gate-4 closed GREEN with no orchestrator findings beyond §9 retrospector-flagged items) — **green**.
  - Standards updates proposed: **7** (1 USER REQUESTED + 6 retro-derived) — **yellow** (threshold is ≥ 4 = system instability; CH-17 is at 7 which is high but reflects (a) the user-requested cargo-clean codification, (b) 2 cycles of pattern accumulation now ready for codification, (c) the mid-cycle disk-full incident generating multiple targeted fixes).
  - Tool calls captured this cycle: **281** (vs CH-15: 514, CH-14: 730, CH-08: 493) — **green** (lower count reflects sub-agent calls under separate logger process IDs, not orchestrator-side anomaly).
  - Hot allow-rule candidates: **0** (vs CH-15: 0, CH-14: 5, CH-08: 4) — **green** (2nd consecutive zero-prompt cycle for hot signatures).
  - Hook false-positive count: **0** in real cycle work (8 PostToolUseFailure entries are all legitimate disk-full or large-doc-read failures, not test/verification false positives) — **green**.
  - Doc-sync sweep stale-narrative hits: **0** unaddressed — **green** (validates CH-15 retro Row 1 widened sweep; CH-17 is the **first cycle** to close on first audit pass with the widened sweep in effect).
  - **Newly-discovered drifts surfaced by retrospective:** **0** (no new drift files needed; all CH-17 findings either closed in-cycle, traced to existing CHK8S-D-10, or codified as standards updates above).

## Appendix — Permissions audit (full)

> Verbatim copy of the `permissions-audit` skill's stdout for this cycle. Reviewers can follow the §A–§H trail to confirm §3.5 extraction.

```markdown
# Permissions Audit — Cycle 40c4d759

**Window:** 2026-05-09T07:47:56Z → 2026-05-09T14:16:09Z (UTC; ~6h28m)
**Total tool calls observed:** 281
**Total unique input_signatures:** 92
**Log file size:** 2,372,406 bytes (single file `.claude/tool-use.log`; no rotations)

## §A — Tool distribution
> Share of total tool calls captured in the cycle window. Helps spot anomalies.

| Tool | Calls | % of total |
|---|---|---|
| Bash | 144 | 51.2% |
| Read | 73 | 26.0% |
| Edit | 49 | 17.4% |
| Write | 7 | 2.5% |
| AskUserQuestion | 6 | 2.1% |
| Agent | 2 | 0.7% |

Event distribution: 266 PostToolUse / 8 PostToolUseFailure / 7 PermissionRequest.

## §B — Hot allow-rule candidates (≥ 3 prompts in cycle, no rule match)
> Patterns the user was prompted for ≥ 3 times — likely missing allow rules.

| Pattern | Prompts | First seen | Sample input | Proposed rule |
|---|---|---|---|---|
| (none qualified — all PermissionRequest fires were 1× or 2×; none reached the ≥ 3-prompt hot-threshold) | — | — | — | — |

**Sub-threshold fires (visibility only — informational):**
- `Bash:cargo:build` — 2 prompts. Both during disk-full recovery; invocations used `; wc -l /tmp/out.log` chain shape that the existing `Bash(cargo build *)` allow rule does not match.
- `Bash:grep` — 1 prompt. `grep -nE` with embedded `\|` regex alternation; one-off.
- `Bash:cat:/tmp/cargo-build.log` — 1 prompt. Two-stage pipe; allow rule covers `cat` but not the pipe-stage.
- `AskUserQuestion` — 4 prompts (F5 lock + F5 fact-base correction confirmation + F5.B.subfork lock + plan-approval). Expected per gate-1 escalation protocol.

## §C — Auto-approved by mode (visibility only)

| Tool | Pattern | Count | Notes |
|---|---|---|---|
| Read | `/root/projects/phi/**` | 73 | `defaultMode: dontAsk` + `Read(/**)` allow rule cover. |
| Edit | `/root/projects/phi/**` (cycle paperwork + paperwork docs) | 49 | `Edit(/**)` allow rule covers (no test/verification false-positive flagged). |
| Write | new doc + script files | 7 | `Write(/**)` allow rule covers. |

## §D — Allow-rule utilization

| Rule | Hits | Status |
|---|---|---|
| `Bash(/root/rust-env/cargo/bin/cargo test *)` | ~9 | active |
| `Bash(/root/rust-env/cargo/bin/cargo build *)` | ~6 (2 prompted under chain-shape) | active; 2 sub-threshold prompts on chain-shape (informational, not hot) |
| `Bash(/root/rust-env/cargo/bin/cargo fmt *)` | 2 | active |
| `Bash(RUSTFLAGS="-Dwarnings" /root/rust-env/cargo/bin/cargo clippy *)` | 4 | active |
| `Bash(git -C /root/projects/phi/baby-phi *)` | 15 | active |
| `Bash(grep *)` | many (2 for `'pub`, 2 for `"tokio`, 2 for `"session_registry:`, etc.) | active |
| `Bash(ls *)` | many (top-7 path patterns observed) | active |
| `Bash(find *)` | 3+ | active |
| `Bash(wc *)` | 3+ | active |
| `Bash(bash /root/projects/phi/baby-phi/scripts/audit-tmp-*.sh*)` | 4 | **active — validates CH-14 close-time literal-name + script-refactor workaround for 2nd consecutive cycle. RESOLVED-VIA-WORKAROUND state holds.** |
| `Bash(bash /root/projects/phi/baby-phi/scripts/check-doc-links.sh*)` + `check-ops-doc-headers.sh*` + `check-phi-core-reuse.sh*` + `check-spec-drift.sh*` (4 literal-name rules) | 4× each in audit shells (sub-agent context — log not captured in this orchestrator window) | active in audit-shells (orchestrator gate-4 also runs these at gate-4 close — ≥ 4 hits per rule across cycle) — RESOLVED-VIA-WORKAROUND validation 2nd cycle |
| `Bash(stat *)` | 1+ | active |
| `Bash(cat *)` | 2 (1 of which prompted on pipe-stage shape — sub-threshold) | active |
| `Agent(chunk-planner)` / `chunk-implementer` / `chunk-auditor` | 2+ each | active |
| (other allow rules — not exercised this cycle window) | 0 | unused this cycle (informational; need ≥ 3-cycle zero-hit before "removal candidate") |

## §E — Hook denials (review for false-positives)

| Hook | Pattern | Count | Sample reason | False-positive? |
|---|---|---|---|---|
| (none flagged) | — | — | — | — |

**8 PostToolUseFailure entries are all legitimate failures, not hook-denials:**
- 3× `Bash:cargo:build → Exit 1` (disk-full recovery); no — legitimate ops failure.
- 2× `Read:... → File content >25000 tokens` (large plan/audit doc reads); no — legitimate; agent retried with offset/limit.
- 2× `Bash:bash:audit-tmp-cargo-... → Exit 101 passed=N failed=M` (cargo-test fails inside audit script); no — legitimate compile/fmt iteration during scope-expansion.
- 1× `Bash:cat:/tmp/cargo-build.log → Exit 1` (file empty/unwritten because parent build also failed); no.

No hooks fired denials this cycle (`scope-edits.sh` + `block-destructive-bash.sh` PreToolUse hooks did not block any operation in the cycle window).

## §F — High-frequency rejected patterns (workflow issue, not rule issue)

| Pattern | Reject count | Likely cause |
|---|---|---|
| (none — no signature was rejected ≥ 5× this cycle) | — | — |

## §G — Cross-cycle trends

| Metric | CH-08 (`7cbe74a4`) | CH-14 (`5803bb94`) | CH-15 (`c3f46f17`) | CH-17 (`40c4d759`, this cycle) | Δ vs prior |
|---|---|---|---|---|---|
| Tool calls captured | 493 | 730 | 514 | **281** | -45% (sub-agent calls in separate logger windows) |
| **PermissionRequest** | 18 | 70 | **0** | **7** (4 AskUserQuestion + 3 Bash sub-threshold) | +7 (vs CH-15's 0); but excluding AskUserQuestion-as-expected → 3 Bash one-offs |
| Hot allow-rule candidates (≥ 3 prompts) | 4 | 5 | 0 | **0** | unchanged — green |
| bash-check cluster prompts | 15 | 43 | 0 | **0** | RESOLVED-VIA-WORKAROUND state holds; drop from cross-cycle tracking |
| cargo-test 4-stage cluster prompts | n/a | 14 | 0 | **0** | RESOLVED-VIA-WORKAROUND state holds; drop from cross-cycle tracking |
| Hook false-positive flags | 0 | 0 | 0 | **0** | green |
| settings.json mid-cycle edit | YES (07:36 UTC out-of-band) | NO | NO | NO | green — settings.json stable across CH-15 → CH-17 |

**Trend headline:** **Permissions landscape fully stable.** Both previously-troublesome clusters (bash-check + cargo-test 4-stage) at 0 prompts for the 2nd consecutive cycle. The 7 PermissionRequest fires this cycle decompose to 4 expected `AskUserQuestion` (gate-1 fork escalation protocol) + 3 sub-threshold one-off Bash invocations during disk-full recovery (chain-shape `;` and 2-stage pipe forms). Per skill v3 step 5b: bash-check + cargo-test 4-stage clusters now permanently dropped from cross-cycle trend tracking. **No standards updates originate from §B/§D/§E/§F.**

## §H — Skill findings + proposed standards updates

> Roll these into the retrospective's §5 Standards updates table.

1. **(none originate from §B/§D/§E/§F.)** Permissions landscape healthy this cycle.

**Cross-cycle affirmation findings (informational, not standards-update-generating):**

a. **bash-check cluster `resolved-via-workaround` state holds at 2 cycles** (CH-15 + CH-17, 0 prompts each). Skill v3 step 5b dictates: drop from cross-cycle tracking permanently. **Action**: update `baby-phi/docs/specs/permissions/granular-bash-discipline-ab19399b.md` data series to append `CH-17 (0/281) — validation passed (2nd consecutive cycle)`. **Not a §5 standards update** — informational lifecycle-state record only.

b. **cargo-test 4-stage cluster `resolved-via-workaround` state holds at 2 cycles** (CH-15 + CH-17, 0 prompts each). Same treatment.

c. **settings.json stable across CH-15 → CH-17.** No mid-cycle out-of-band edit; no rule churn needed.

---
*Generated by `.claude/skills/permissions-audit.md` v3 from `.claude/tool-use.log`.*
```
