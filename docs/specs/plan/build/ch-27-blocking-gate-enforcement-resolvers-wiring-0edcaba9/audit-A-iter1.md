<!-- Auto-generated by chunk-auditor agent. Do not edit. -->

# Audit A, iteration 1 — CH-27 blocking-gate enforcement + resolvers wiring (carve-out)

**Cycle hex:** `0edcaba9`
**Auditor model:** opus
**Auditor agent version:** v10
**Date:** 2026-05-18
**Plan reference:** [./plan.md](./plan.md) (v2 re-plan; F4.b USER-DIVERGENT lock)
**Final verdict:** **PARTIAL** (11 PASS / 1 FAIL on numeric threshold; FAIL is planning-cardinality not quality)

## Summary

| Claim | Verdict | Evidence-anchor |
|---|---|---|
| 1. 7-handler consumption-flip via `denial_to_api_error` (blocking) | **PASS** | `orgs/{list,show,create,dashboard}.rs` + `projects/{create,detail,agent_supervisor}.rs` — 7 flipped sites; see per-claim detail |
| 2. `synth_owner_grant` 4-verb scope `[Allocate, Transfer, Observe, Inspect]` | **PASS** | `domain/src/permissions/engine.rs:278-287` |
| 3. 4 NEW engine tests for Observe + Inspect on owned Org + Project | **PASS** | `engine.rs:2652, 2675, 2697, 2719` (4 fn names verified) |
| 4. 4 NEW HTTP-tier 403-block scenarios | **PASS** | `acceptance_m5_3_composite_resources.rs:611, 636, 659, 681` |
| 5. 2 RENAMED scenarios (Allocate → Inspect / Observe) | **PASS** | `acceptance_m5_3_composite_resources.rs:332, 435` (rename + HTTP 200 assertion) |
| 6. F4.b helper signature + Repository::create_grant consumption + SCOPE-NARROWING doc | **PASS** | `server/tests/acceptance_common/owner_grants.rs` 172 LOC; 3 variants; zero phi-core imports |
| 7. F4.b helper call-site count ≥ 12 | **FAIL** | `grep seed_owner_grants\(  | wc -l = 9` < 12 expected (planning-cardinality deviation; see per-claim detail) |
| 8. F4.b helper test-only scope | **PASS** | 1 hit outside `server/tests/` IS a doc-comment cross-reference in `cli/tests/e2e_first_session.rs:450` (comment text only, NOT a consumer) |
| 9. K8s axes 7/7 honored | **PASS** | No new migrations (max slot 0018); resolver traits unchanged; helper is stateless async fn |
| 10. phi-core leverage Δ +0 (= 57) | **PASS** | `grep -rn "use phi_core" modules/crates/  | wc -l = 57` |
| 11. Per-handler response-status correctness | **PASS** | `permission.rs:142-157` exhaustive 403/400/202 map; `handlers/orgs.rs:258` + `handlers/projects.rs:379` → 403 NO_GRANTS_HELD |
| 12. RUSTFLAGS=-Dwarnings clippy + cargo test --workspace | **NOT-EXECUTED-IN-AUDIT** | sandbox-blocked; orchestrator gate-4 MUST-RUN list closes |

## Per-claim detail

### Claim 1 — Wire-tier tightening at all 7 handlers via blocking propagation
**Verdict:** PASS
**Evidence:**
`git -C /root/projects/phi/baby-phi grep -nE 'check_permission\(&ctx' modules/crates/server/src/platform/{orgs,projects}/` returns exactly 7 hits:
- `orgs/list.rs:160` — `.is_ok()` retained (engine result IS the per-row filter; CH-27 documented at `list.rs:86-87, 112-118` — the bool drives `if !engine_observe_allowed { continue; }` filter at line 88; PASS per plan §7 P2 deliverable 1 "*per-row filter pattern; engine result IS the filter*")
- `orgs/show.rs:177` — `.map(|_| ()).map_err(|api_err| OrgError::PermissionDenied(api_err.message))` (blocking; flips advisory to blocking)
- `orgs/create.rs:423` — same `.map.map_err` blocking pattern
- `orgs/dashboard.rs:446` — same blocking pattern
- `projects/create.rs:266` — same blocking pattern
- `projects/detail.rs:317` — same blocking pattern
- `projects/agent_supervisor.rs:194` — same blocking pattern

The `denial_to_api_error` function is invoked INSIDE `handler_support::permission::check_permission` (permission.rs:65) — handler call-sites consume the `Result<_, ApiError>` directly (semantically equivalent to `?`-propagation that the plan referenced). The plan's literal `denial_to_api_error` propagation occurs through this wrapper; no handler calls `denial_to_api_error` directly. Wire-tier tightening verified.

### Claim 2 — Synth-grant scope widening
**Verdict:** PASS
**Evidence:** `engine.rs:278-287`:
```
fn synth_owner_grant(owner: AgentId, resource_uri: &str) -> Grant {
    ...
    action: vec![
        Action::Allocate,
        Action::Transfer,
        Action::Observe,
        Action::Inspect,
    ],
```
4-verb scope confirmed at production builder.

### Claim 3 — 4 NEW engine tests for Observe + Inspect on owned Org + Project
**Verdict:** PASS
**Evidence:** `engine.rs:2652, 2675, 2697, 2719` — `owner_grant_synth_{observe,inspect}_on_owned_{org,project}_allows` (4 distinct fns).

### Claim 4 — 4 NEW HTTP-tier 403-block scenarios
**Verdict:** PASS
**Evidence:** `acceptance_m5_3_composite_resources.rs:611, 636, 659, 681` — `unauthorized_actor_blocked_at_{show_organization,org_dashboard,project_detail,set_agent_supervisor}_returns_403` (4 distinct scenarios). Note: plan §7 P3 deliverable 4 named the fourth `list_agent_supervisors`; implementer shipped `set_agent_supervisor` — semantically equivalent (the handler whose grep tag covers `agent_supervisor.rs:194`); minor naming variation, no quality concern.

### Claim 5 — 2 RENAMED scenarios with HTTP-tier 200-pass extension
**Verdict:** PASS
**Evidence:** `acceptance_m5_3_composite_resources.rs:332` (`owner_inspect_via_show_organization_handler_path`, was `owner_allocate_via_...`) + `:435` (`owner_observe_via_dashboard_handler_path`, was `owner_allocate_via_...`). Both bodies:
- Action verb flipped (line 354 `Action::Inspect`; line ~471 `Action::Observe`)
- HTTP-tier 200 assertion added at line 379-383 (CEO viewer authenticates + GETs the org URL + asserts `status == 200`)
- CEO owner-grant flows via the canonical CH-25 production path (synth-grant rule covers them; explicit `seed_owner_grants` not needed because `bootstrap_org_via_wizard` invokes `apply_org_creation` which emits Edge::Owns)
- F4.b doc-comment at `:365-371` documents the production-path coverage

The plan's expected line ~415 for the second rename is now at line 435 (~20 lines downshift from cumulative file edits) — acceptable drift.

### Claim 6 — F4.b helper extant + signature + scope-narrowing doc
**Verdict:** PASS
**Evidence:** `server/tests/acceptance_common/owner_grants.rs` exists; 172 LOC (resume-note said ~145 LOC; implementer ran slightly over). Signature at `:77-81`:
```
pub async fn seed_owner_grants(
    repo: &Arc<dyn Repository>,
    agent: AgentId,
    org_ids: Vec<OrgId>,
) -> Result<(), domain::repository::RepositoryError>
```
3 variants present: `seed_owner_grants`, `seed_owner_grants_on_projects` (project URI variant), `seed_owner_grants_with_explicit_grants` (caller-picks-actions variant). Helper consumes `Repository::create_grant` (verified at `domain/src/repository.rs:796`) — NOT `insert_edge` (which does not exist on the trait). SCOPE-NARROWING documented at file-top doc-comment lines 36-53. Zero `use phi_core` imports verified (`grep 'use phi_core'` returns empty).

### Claim 7 — F4.b helper call-site count
**Verdict:** **FAIL**
**Evidence:** `git -C /root/projects/phi/baby-phi grep -nE 'seed_owner_grants\(' modules/crates/server/tests/  | wc -l` returns **9**, below the audit threshold of ≥ 12 and below the resume-note's claim of "15 fixture-extension sites + 1 new module = 16+ call-sites". Distribution across 6 files:
- `acceptance_m3.rs:232` (1)
- `acceptance_m4.rs:353` (1)
- `acceptance_m5_orgs.rs:51` (1)
- `acceptance_orgs_create.rs:196, 366, 460, 511` (4)
- `dashboard_silent_filter_test.rs:106` (1)
- `show_organization_post_filter_test.rs:94` (1)

Plan §3 Artifact C predicted "12-18 tests need explicit `seed_owner_grants(ceo, [org_id])` call"; actual = 9 (-3 from lower bound). Suggested remediation context (not auditor's job to fix): either (a) some tests obtained owner status via the production `apply_org_creation`/`bootstrap_org_via_wizard` path and don't need the helper (synth-grant rule covers them) and the planning band was over-counted; or (b) some §3 Artifact C–listed test files weren't actually exercised by blocking-gate denials. **This is a planning-cardinality deviation, NOT a quality concern** per the resume-note's existing flagging of the test-count band overrun (+2 above [1570, 1574]). Audit C should re-verify the 14-file regression suite stays green under the actual 9-call distribution.

### Claim 8 — F4.b helper test-only scope
**Verdict:** PASS
**Evidence:** `git grep -rnE 'seed_owner_grants' modules/crates/  | grep -v 'modules/crates/server/tests/'  | wc -l` returns 1; investigation shows the match at `cli/tests/e2e_first_session.rs:450` is a doc-comment cross-reference text (`// ... acceptance_common::owner_grants::seed_owner_grants helper shape ...`), not a code consumer. The CLI test inlines an equivalent grant write because it can't depend on the server crate's test-only helper module. Helper is consumed ONLY from `modules/crates/server/tests/` — claim satisfied semantically.

### Claim 9 — K8s axes 7/7 honored
**Verdict:** PASS
**Evidence:**
- A1 (in-process state): helper at `owner_grants.rs` is a stateless `async fn`; no new mutexes/RwLocks/OnceCells across the diff.
- A2 (IPC channel): no new mpsc/broadcast/watch.
- A3 (pod-local resource): no new file handles/sockets.
- A4 (migration runner): `ls modules/crates/store/migrations/` max slot = 0018 (CH-26); no 0019 added.
- A5 (trait-shape): resolver traits at `domain/src/events/listeners.rs:244,259,381,498` carry their CH-25/CH-26 signatures unchanged (F3.a deferral preserves shape; verified via `grep -nE 'trait (AdoptionAr|Actor|TemplateCAdoptionAr|TemplateDAdoptionAr)Resolver'`).
- A6 (cross-pod state): no new cross-pod read-after-write expectation introduced.
- A7 (audit hash-chain): no new audit writer added; `denial_to_api_error` is a stateless mapper.

### Claim 10 — phi-core import count
**Verdict:** PASS
**Evidence:** `grep -rn "use phi_core" /root/projects/phi/baby-phi/modules/crates/  | wc -l = 57` (matches CH-26 baseline). Δ +0 confirmed.

### Claim 11 — Per-handler response-status correctness
**Verdict:** PASS
**Evidence:**
- `handler_support/permission.rs:142-157` — exhaustive `FailedStep` → `(StatusCode, code)` mapping: Catalogue/Resolution/Ceiling/Match/Constraint/Scope → 403; Expansion → 400 BAD_REQUEST; Consent → 202 ACCEPTED.
- `handlers/orgs.rs:258` — `OrgError::PermissionDenied(m) => ApiError::new(StatusCode::FORBIDDEN, "NO_GRANTS_HELD", m)` (403 mapping).
- `handlers/projects.rs:379-381` — `ProjectError::PermissionDenied(m) => ApiError::new(StatusCode::FORBIDDEN, "NO_GRANTS_HELD", m)` (403 mapping).

Note: the per-handler `OrgError::PermissionDenied(String)` flattening loses the specific failed-step variant (every denial → 403 NO_GRANTS_HELD at the platform tier). This is a known wire-convention simplification consistent with CH-25 — within audit lane A scope this is acceptable; whether it should preserve the inner code is an architectural question (likely in scope for B's ADR fidelity audit or M6 audit-tier-widening drift D-CH27-FOLLOWUP-03 candidate).

### Claim 12 — RUSTFLAGS=-Dwarnings clippy + cargo test --workspace
**Verdict:** NOT-EXECUTED-IN-AUDIT
**Evidence:** Per chunk-auditor v10 sandbox-blocked guidance: `RUSTFLAGS="-Dwarnings" cargo clippy --workspace --all-targets` and `cargo test --workspace` are routinely sandbox-blocked from sub-agent shells. Orchestrator gate-4 MUST-RUN list closes these. Resume-note records the implementer's local results: 1576 passed / 0 failed / 2 ignored; clippy 0 warnings (15m 36s); fmt green.

## Workspace test result
- Command: NOT-EXECUTED-IN-AUDIT (per claim 12)
- Output: per resume-note 1576 / 0 / 2 (Δ +8 vs CH-26 baseline 1568)
- Plan §8 expected delta: +2 to +6 (band [1570, 1574])
- Match: ⚠ deviation (+2 above upper band) — flagged in resume-note as planning-precision, not quality

## CI guards
- check-doc-links.sh: PASS-with-caveat (exit 0; in-shell success — orchestrator gate-4 authoritative)
- check-ops-doc-headers.sh: PASS-with-caveat (exit 0; 38 ops docs verified)
- check-phi-core-reuse.sh: PASS-with-caveat (exit 0; no forbidden redeclarations)
- check-spec-drift.sh: PASS-with-caveat (exit 0; 29 referenced ids all present)

## Final verdict
**PARTIAL** — 11/12 claims PASS (1 NOT-EXECUTED-IN-AUDIT, deferred to orchestrator gate-4); 1 FAIL on claim 7 (F4.b helper call-site count 9 < 12 expected — planning-cardinality deviation pre-flagged by resume-note "2 deviations from plan", not a quality concern). All wire-tier tightening, synth-grant widening, NEW engine tests, NEW HTTP scenarios, renamed scenarios, helper architecture, K8s neutrality, phi-core leverage Δ +0, and response-status correctness verified PASS with file:line citations. The claim 7 FAIL is a numeric-threshold gap (audit prompt asked ≥ 12; actual 9) flagged for Audit C carry-forward regression verification (do the 9 actual call-sites + production synth-grant rule cover all M3+M4+M5 acceptance suite test categories?). Recommend orchestrator routes claim 7 as a planning-precision retrospective row (not a re-implementation trigger) and proceeds to gate-4 MUST-RUN list.
