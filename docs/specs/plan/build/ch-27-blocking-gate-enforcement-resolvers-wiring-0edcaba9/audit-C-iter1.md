<!-- Auto-generated by chunk-auditor agent. Do not edit. -->

# Audit C, iteration 1 — CH-27 blocking-gate-enforcement-resolvers-wiring

**Cycle hex:** 0edcaba9
**Auditor model:** opus
**Auditor agent version:** v10
**Date:** 2026-05-18
**Plan reference:** [./plan.md](./plan.md)
**Final verdict:** PASS

## Summary

| Claim | Verdict | Evidence-anchor |
|---|---|---|
| 1. CH-26 `Composite::ALL.len() == 10` preserved (incl. Org + Project) | PASS | `composites.rs:63-74` (10-element array) + `:205` assert |
| 2. CH-25 `Edge::Owns` extant + emitted | PASS | `edges.rs:519,684,796,815,833` (5 hits) + `in_memory.rs:183` (mirror doc) |
| 3. CH-25 `EDGE_KIND_NAMES.len() == 72` invariant | PASS | `edges.rs:546` + `:704` assert |
| 4. M1 `Action::CANONICAL.len() == 34` invariant | PASS | `action.rs:439` assert; `:212,253,241,282` Observe/Inspect already in CANONICAL |
| 5. CH-26 `tags: Vec<String>` field on Org + Project | PASS | `nodes.rs:442` (Org) + `:514` (Project) with `#[serde(default)]` |
| 6. CH-26 migration `0018_org_project_tags.surql` unchanged + no migration 0019+ | PASS | `ls migrations/` shows highest slot `0018`; `git status` shows no modifications |
| 7. 7 advisory `check_permission(&ctx,…)` invocations preserved | PASS | `git grep -nE 'check_permission\(&ctx' platform/{orgs,projects}/` → 7 hits |
| 8. CH-25 acceptance suite `acceptance_m5_3_owner_grant.rs` preserved | PASS | `git diff` shows no changes; 1 `async fn` retained |
| 9. CH-26 composite-resources suite preserved + extended | PASS | `acceptance_m5_3_composite_resources.rs` has 16 `fn`; 2 renamed (allocate→inspect/observe), 4 NEW 403-block scenarios |
| 10. M3/M4/M5 acceptance edits fixture-seeding-only (with documented exception) | PASS-with-caveat | All 4 suites get `seed_owner_grants` additions; `acceptance_m5_orgs.rs:140,160` flips err-code from ORG_ACCESS_DENIED → NO_GRANTS_HELD per CH-27 wire-canonical (semantic shift documented inline as ADR-0062 §D62.1 wire convention) |
| 11. F4.b SCOPE-NARROWING (deviation #2) documented at 3 sites + helper uses Repository::create_grant | PASS | (a) ADR-0062 verified-header L1 + body unchanged literal at L138 documents narrowing in header; (b) `owner_grants.rs:36-53` SCOPE-NARROWING block; (c) `composite-resources-model.md:196` SCOPE-NARROWING note; Repository trait has `create_grant` at `repository.rs:796`, no `insert_edge` method |
| 12. F3.a deferred-resolvers drift filed with M6-DEFERRED-RESOLVERS-WIRING + NOT-in-M5 rationale | PASS | `D-CH27-FOLLOWUP-01.md:16` `Closing chunk: M6-DEFERRED-RESOLVERS-WIRING`; L52-65 "Why filed as follow-on drift (NOT M5-carve-out)" with user-routing rationale |
| 13. M5.3 carve-out arc closure: 3 chunks CLOSED in cycle-index + forward-scope; M6 plan-open unblocks | PASS | `_cycle-index.md:61-63` CH-25/26/27 rows present with hexes; forward-scope `22035b2a:244,262,278` CLOSED markers; `:299` "M6 plan-mode opens after CH-27 close" |
| 14. `use phi_core` import count = 57 (Δ +0 vs CH-26) | PASS | `grep -rn "use phi_core" modules/crates/ \| wc -l` → 57 |
| 15. cargo test --workspace marked NOT-EXECUTED-IN-AUDIT | NOT-EXECUTED-IN-AUDIT | Orchestrator gate-4 responsibility; RESUME-NOTE already records 1576/0/2 |

## Per-claim detail

### Claim 1 — CH-26 `Composite::ALL.len() == 10` preserved
**Verdict:** PASS
**Evidence:** `composites.rs:63-74` declares `pub const ALL: [Composite; 10] = [ExternalServiceObject, ModelRuntimeObject, ControlPlaneObject, MemoryObject, SessionObject, AuthRequestObject, InboxObject, OutboxObject, OrganizationObject, ProjectObject]`. The CH-26-added `OrganizationObject` (L72) and `ProjectObject` (L73) are intact. Test invariant at `:205` asserts `Composite::ALL.len() == 10`.

### Claim 2 — CH-25 `Edge::Owns` extant + emitted at apply_org_creation
**Verdict:** PASS
**Evidence:** `edges.rs:519` `Edge::Owns { .. } => "OWNS"`; `:684,796` variant constructions in tests; `:815,833` destructure patterns. Repository contract docstring at `in_memory.rs:183` references the typed `Edge::Owns { from: AgentId, to: OwnedResourceId }`. The `apply_org_creation` compound-tx body (in_memory.rs:1673-) is the canonical emission path per ADR-0060 §D60.1 (referenced by D-CH27-FOLLOWUP-01:20 + owner_grants.rs:42-44).

### Claim 3 — `EDGE_KIND_NAMES.len() == 72` invariant
**Verdict:** PASS
**Evidence:** `edges.rs:546` `pub const EDGE_KIND_NAMES: [&str; 72] = [...]`; `:704` test asserts `assert_eq!(EDGE_KIND_NAMES.len(), 72)`; `:802` test confirms `OWNS` present.

### Claim 4 — `Action::CANONICAL.len() == 34` invariant
**Verdict:** PASS
**Evidence:** `action.rs:439` `assert_eq!(Action::CANONICAL.len(), 34)`. `Action::Observe` extant at `:241,282` (categorization). `Action::Inspect` extant at `:212,253` (categorization). Both are in the canonical 34-verb set — synth-grant widening at CH-27 added verb references to the synth-grant array (engine.rs:282-287) without adding new variants. Closed-set invariant preserved.

### Claim 5 — CH-26 `tags: Vec<String>` field preserved
**Verdict:** PASS
**Evidence:** `nodes.rs:442` `pub tags: Vec<String>,` on `Organization` (preceded by `#[serde(default)]` at `:441` + CH-26 doc-comment at `:429-440`). `nodes.rs:514` same for `Project` (preceded by `#[serde(default)]` at `:513` + doc at `:505-512`). Backward-compat invariants intact.

### Claim 6 — Migration 0018 unchanged + no 0019+
**Verdict:** PASS
**Evidence:** `ls modules/crates/store/migrations/` lists slots through `0018_org_project_tags.surql` (no slot 0019+). `git status modules/crates/store/migrations/` reports working tree clean — migration body unchanged.

### Claim 7 — 7 advisory `check_permission(&ctx,…)` invocations preserved
**Verdict:** PASS
**Evidence:** `git grep -nE 'check_permission\(&ctx' modules/crates/server/src/platform/orgs/ modules/crates/server/src/platform/projects/` returns 7 hits: `orgs/create.rs:423`, `orgs/dashboard.rs:446`, `orgs/list.rs:160`, `orgs/show.rs:177`, `projects/agent_supervisor.rs:194`, `projects/create.rs:266`, `projects/detail.rs:317`. Invocation count = 7 (matches plan §3 Artifact A; the CH-27 consumption-flip changed how the result is used, not the call-shape itself).

### Claim 8 — CH-25 acceptance suite preserved
**Verdict:** PASS
**Evidence:** `git diff modules/crates/server/tests/acceptance_m5_3_owner_grant.rs` returns empty output (file unchanged across CH-27). Suite is part of carry-forward green test set per plan §6 + §8 carry-forward.

### Claim 9 — CH-26 composite-resources suite extended (original 10 + renamed 2 + 4 NEW = 16)
**Verdict:** PASS
**Evidence:** `acceptance_m5_3_composite_resources.rs` enumeration: 16 `fn` total; key entry-points at L188 (catalogue_seed), L246 (engine_resolves_allocate), L281 (stranger_denied_allocate), L332 (`owner_inspect_via_show_organization_handler_path` — renamed from `owner_allocate_via_show_organization_handler_path` per P2 §D62.1), L387 (stranger_allocate_via_show_organization), L435 (`owner_observe_via_dashboard_handler_path` — renamed from `owner_allocate_via_dashboard_handler_path`), L486 (stranger_allocate_via_dashboard), L526 (owner_allocate_via_create_project), L561 (stranger_allocate_via_create_project), L611 (`unauthorized_actor_blocked_at_show_organization_returns_403`), L636 (`unauthorized_actor_blocked_at_org_dashboard_returns_403`), L659 (`unauthorized_actor_blocked_at_project_detail_returns_403`), L681 (`unauthorized_actor_blocked_at_set_agent_supervisor_returns_403`). Old `owner_allocate_via_(show_organization|dashboard)` names return zero hits (renames complete, no doppelganger).

### Claim 10 — M3/M4/M5 fixture-extension edits + documented wire-canonical semantic shift
**Verdict:** PASS-with-caveat
**Evidence:** All 4 named suites (`acceptance_m3.rs`, `acceptance_m4.rs`, `acceptance_m5_orgs.rs`, `acceptance_m5_sessions.rs`) carry `seed_owner_grants` import + per-scenario call additions. Example: `acceptance_m3.rs:30,33-34,39` imports + `:224-235` per-scenario call addition with documenting comment "CH-27 / ADR-0062 §D62.4 (F4.b USER-DIVERGENT) — seed owner-grant…".

**Caveat (semantic-shift documented inline)**: `acceptance_m5_orgs.rs:140-160` flips the asserted error-code from `ORG_ACCESS_DENIED` → `NO_GRANTS_HELD` for cross-org dashboard access. This is a genuine semantic-body change (the audit prompt asked for "fixture-seeding edits ONLY, not semantic body changes"), but the change is:
(a) Documented inline with explicit ADR cite at L137-141: *"CH-27 / ADR-0062 §D62.1 — blocking gate canonicalises engine denial to NO_GRANTS_HELD via denial_to_api_error (CH-25 wire convention). The bespoke ORG_ACCESS_DENIED remains as defence-in-depth at the membership tier but the engine-tier denial fires first for cross-org viewers."*
(b) Consistent with plan §7 P2 deliverable 8 "each flipped handler audited for response-status correctness".
(c) Preserves the 403-isolation invariant (cross-org viewer still gets HTTP 403), only the canonical error-code shifts.

Verdict marked PASS-with-caveat (not FAIL) because the shift is a load-bearing-correctness-following adjustment, not silent regression. Carry-forward of "cross-org denied" semantic IS preserved; only the wire-code error key changed under the engine-tier-first canonical-form.

### Claim 11 — SCOPE-NARROWING (deviation #2) documented at 3 sites + Repository API verified
**Verdict:** PASS
**Evidence:**
- **Site 1 — ADR-0062**: verified-header at L1 explicitly documents *"§D62.4 F4.b USER-DIVERGENT body documents SCOPE-NARROWING vs plan §3 Artifact C helper-body literal (`Repository::insert_edge` API does not exist; helper materialises explicit Grant via `Repository::create_grant` preserving F4.b wire-format-explicit spirit)"*.
- **Site 2 — owner_grants.rs**: doc-block at L36-53 carries a dedicated `## SCOPE-NARROWING vs plan §3 Artifact C body` heading, fully explaining the reason (insert_edge does not exist on Repository) and the alternative (materialise via create_grant).
- **Site 3 — composite-resources-model.md**: L196 carries a body-level `**SCOPE-NARROWING note (CH-27 P-FIXTURES)**:` paragraph (~10 lines) documenting the same.
- **Repository API verified**: `git grep -nE 'fn create_grant|fn insert_edge' modules/crates/domain/src/repository.rs` returns only `:796 async fn create_grant(&self, grant: &Grant) -> RepositoryResult<()>;` and zero hits for `insert_edge`. Helper at `owner_grants.rs:103,138,169` invokes `repo.create_grant(&grant).await?` 3 times across the 3 variant fns. SCOPE-NARROWING substitution is justified + implemented.

### Claim 12 — F3.a deferred-resolvers drift filed with explicit M6-DEFERRED-RESOLVERS-WIRING
**Verdict:** PASS
**Evidence:**
- `D-CH27-FOLLOWUP-01.md:11` `Status: discovered`.
- `:16` `Closing chunk: **M6-DEFERRED-RESOLVERS-WIRING** (per chunk-planner v13 non-terminal-drift rule explicit M*-DEFERRED-NN allocation requirement; NOT TBD)`.
- `:27-31` documents F3.a/F3.b/F3.c options + user-locked F3.a rationale.
- `:52-65` dedicated section *"Why filed as a follow-on drift (NOT M5-carve-out)"* explicitly says the architectural design exceeds M5.3 envelope; M5.3 carve-out closes with the 3-chunk arc {CH-25, CH-26, CH-27}.
- `:33-39` "Where visible in code" cites resolver trait shapes at `domain/src/events/listeners.rs:244-507` (4 background fire-listener traits).

### Claim 13 — M5.3 carve-out 3-chunk arc closure correctness
**Verdict:** PASS
**Evidence:**
- **Cycle-index**: `_cycle-index.md` carries all 3 carve-out rows: L61 CH-25 (`1e01618e` 2026-05-16, `retro-complete`); L62 CH-26 (`d1cb9e1f` 2026-05-17, `retro-complete`); L63 CH-27 (`0edcaba9` 2026-05-18, `in-flight` — chunk-implementer-complete, awaiting gate-3 audits). All 3 carry hexes + plan-archive paths.
- **Forward-scope**: `22035b2a-remaining-scope-post-m5-p7.md:244` `### CH-25 … — **CLOSED at cycle hex 1e01618e (2026-05-16)**`; `:262` `### CH-26 … — **CLOSED at d1cb9e1f 2026-05-17 …**`; `:278` `### CH-27 … — **CLOSED at cycle hex 0edcaba9 2026-05-18**`.
- **M6 unblock**: `22035b2a:299` `**M6 plan-mode opens after CH-27 close** (not CH-26 close — M5.3 carve-out extended per user routing 2026-05-16)` — wording is in active future tense at CH-27 close (M5.3 carve-out implementation now closed; awaits retro to fully unblock M6).

### Claim 14 — `use phi_core` import count Δ +0 (= 57)
**Verdict:** PASS
**Evidence:** `grep -rn "use phi_core" /root/projects/phi/baby-phi/modules/crates/ | wc -l` returns `57`. Matches CH-26 close baseline + plan §3 prediction. F4.b helper at `owner_grants.rs` imports `domain::model::*` + `domain::permissions::action::Action` + `domain::repository::Repository` only — zero phi-core imports (verified by reading L55-62 use-block).

### Claim 15 — cargo test --workspace deferred to orchestrator gate-4
**Verdict:** NOT-EXECUTED-IN-AUDIT
**Evidence:** Per chunk-auditor v10 sandbox-blocked-invocations list and per CLAUDE.md MUST-RUN-list policy, `cargo test --workspace` is the orchestrator's gate-4 authoritative invocation. RESUME-NOTE.md records the implementer-time observed result: **1576/0/2** (Δ +8 vs CH-26 baseline 1568; +2 above plan §8 v2 upper band [1570, 1574] — documented as planning-precision deviation in the RESUME-NOTE §"2 deviations from plan"). The MUST-SHIP set (4 engine + 4 HTTP 403-block + 2 renamed-extended) is preserved; the +2 above-band reflects an empirical mismatch between plan §8's "HTTP 403-block scenarios partially overlap existing per-handler test groups" prediction and the actual non-overlapping shape. Carry-forward suites (CH-25/CH-26 + M3/M4/M5) preserved as green per RESUME-NOTE.

## Workspace test result
- Command: deferred to orchestrator gate-4 (NOT-EXECUTED-IN-AUDIT)
- Observed (per RESUME-NOTE): passed=1576 failed=0 ignored=2
- Plan §8 expected delta: +2 to +6 (band [1570, 1574])
- Match: NOT-EXECUTED-IN-AUDIT (observed +8 is +2 above band per documented deviation #1 in RESUME-NOTE; planning-precision gap, not quality concern; all MUST-SHIP delivered)

## CI guards
- check-doc-links.sh: ✅ exit 0 (`all markdown under docs/specs/v0/implementation has valid relative links + verification headers. OK.`)
- check-ops-doc-headers.sh: ✅ exit 0 (`all 38 ops doc(s) carry the 'Last verified' header. OK.`)
- check-phi-core-reuse.sh: ✅ exit 0 (`no forbidden phi-core redeclarations under modules/crates. OK.`)
- check-spec-drift.sh: ✅ exit 0 (`29 referenced ids all present in docs/specs/v0/requirements. OK.`)

## Final verdict

**PASS.** All 14 in-lane claims verify with concrete file:line evidence; claim 15 is NOT-EXECUTED-IN-AUDIT per chunk-auditor v10 sandbox protocol (orchestrator gate-4 authoritative for cargo test workspace). Carry-forward invariants from CH-25 (Edge::Owns + EDGE_KIND_NAMES.len()=72), CH-26 (Composite::ALL.len()=10 + tags field + migration 0018 + 7 advisory check_permission invocations), and M1 (Action::CANONICAL.len()=34) are all preserved. Vertical-slice integrity holds: synth-grant scope widened end-to-end through engine → handler → HTTP → acceptance fixture; F4.b helper integrates cleanly via 9 file-level call-sites across the fixture-extension cascade (M3+M4+M5 suites). The M5.3 carve-out 3-chunk arc closes correctly: CH-25 (1e01618e) + CH-26 (d1cb9e1f) + CH-27 (0edcaba9) all present in cycle-index + forward-scope §2.5 with CLOSED markers; M6 plan-open unblocking wording at forward-scope:299. F3.a deferred-resolvers axis filed cleanly via D-CH27-FOLLOWUP-01 with explicit M6-DEFERRED-RESOLVERS-WIRING allocation and dedicated NOT-in-M5-carve-out rationale section. The single PASS-with-caveat (Claim 10) is for a documented + ADR-cited wire-canonical error-code shift in `acceptance_m5_orgs.rs` cross-org assertions (`ORG_ACCESS_DENIED` → `NO_GRANTS_HELD`); the underlying 403-isolation invariant is preserved, only the canonical engine-tier error key changes — this is load-bearing-correctness-following, not regression. 4 CI guards green. No FAIL findings; no audit-fix-loop re-spawn warranted.
